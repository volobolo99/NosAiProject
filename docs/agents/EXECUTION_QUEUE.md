# NosAiProject — Coda di esecuzione multi-agente

**Scopo:** un solo file da cui partire. Ogni voce ha un ID, chi la esegue, da cosa dipende, e dove sta il comando dettagliato. Aggiornato da Claude ad ogni avanzamento — questo file è la fonte di verità su "cosa è il prossimo passo", non `docs/agents/AGENT_COMMAND_REGISTRY.md` (che resta la mappa di ownership per dominio, statica) né i singoli file in `docs/agents/phases/` (che restano i comandi dettagliati per singolo task).

**Come si usa:** guarda la prima voce con stato `PENDING`. Se l'esecutore è `Claude`, dillo a Claude di partire (es. "vai con Q-004"). Se è `DeepSeek`, apri il file comando indicato e passalo a DeepSeek. Quando un task finisce, l'esecutore aggiorna lo stato qui.

**Stati:** `PENDING` (non iniziato) · `IN_PROGRESS` · `DONE` · `BLOCKED` (dipendenza non soddisfatta).

---

## Coda attiva (AP-00 → AP-10)

| ID | Fase | Task | Esecutore | Dipende da | Comando | Stato |
|---|---|---|---|---|---|---|
| Q-001 | AP-00 | A1 — Contratti capacità hardware | Claude | — | `docs/agents/phases/AP-00/A1_CLAUDE_hardware_contracts.md` | **DONE** |
| Q-002 | AP-00 | A2 — Profiling Python hardware | Claude (Cursor non disp.) | — | `docs/agents/phases/AP-00/A2_CURSOR_runtime_profiling.md` | **DONE** |
| Q-003 | AP-00 | A3 — AI budget policy | Claude | — | `docs/agents/phases/AP-00/A3_CLAUDE_ai_budget_policy.md` | **DONE** |
| Q-004 | AP-00 | A4 — Runtime capability gate | Claude (Cursor non disp.) | Q-001 | `docs/agents/phases/AP-00/A4_CURSOR_runtime_gate.md` | **DONE** |
| Q-005 | AP-00 | A5 — Test/benchmark/doc | Claude | Q-001, Q-003, Q-004 | `docs/agents/phases/AP-00/A5_CLAUDE_tests_docs.md` | **DONE** |
| Q-006 | AP-00 | A6 — Integrazione finale (dedup `InferenceTier` Hardware/Scheduling, build/test completo, commit) | **Claude** | Q-005 | `docs/agents/phases/AP-00/A6_CLAUDE_integration_gate.md` | **DONE** |
| Q-007 | AP-01 | A1 — Contratti World Model (Player/Map/Tile/Portal/Mob/NPC/Drop/Quest/Inventory/Equipment/Skill/Buff/Debuff/Cooldown/Resource/Action/Goal) | **Claude** | Q-006 | `docs/agents/phases/AP-01/A1_CLAUDE_world_model_contracts.md` | **DONE** |
| Q-008 | AP-01 | A2 — Sensor Fusion (Network/Memory/Screen/Local → World Model) | Claude (DeepSeek/Cursor non ancora attivi) | Q-007 | `docs/agents/phases/AP-01/A2_DEEPSEEK_sensor_fusion.md` | **DONE** |
| Q-009 | AP-01 | A3 — Temporal belief (decadimento confidence continuo) + derived state (velocità stimata) + prediction (estrapolazione posizione, advisory-only) | **Claude** | Q-007 | `src/NosAi.Core/WorldModel/Temporal/` (nessun comando dedicato scritto: ambito chiarito in conversazione con l'utente, non serviva un file separato) | **DONE** |
| Q-010 | AP-01 | A4 — Runtime wiring (WorldModelFusionLoop, flag `--fuse-world-model`, ModuleReachability → Integrated) | Claude (agente in background) | Q-007, Q-008, Q-009 | (comando dato in linea all'agente, non un file separato — vedi AP-01_STATUS.md §9) | **DONE** |
| Q-011 | AP-01 | A5 — Test/benchmark/doc AP-01 (audit indipendente, 4 difetti reali trovati) | Claude (agente in background) | Q-007, Q-008, Q-009, Q-010 | `docs/agents/phases/AP-01/AP-01_A5_AUDIT.md` | **DONE** |
| Q-012 | AP-01 | A6 — Integrazione finale AP-01 (4 correzioni applicate, build/test combinati verdi) | **Claude** | Q-011 | (vedi AP-01_STATUS.md §11) | **DONE** |
| Q-013 | AP-02 | A1 — Contratto `VisualObservation` (osservazione visiva unificata con provenance) | **Claude** | Q-012 | (vedi AP-02_STATUS.md §3; nessun file comando separato — ricognizione approfondita ha sostituito la stesura preventiva) | **DONE** |
| Q-014 | AP-02 | A2 — Screen/OCR/CV adapters | Claude (Cursor non disp.) | Q-013 | in gran parte già presente da prima (DXGI capture, ROI, ONNX detector contract — vedi AP-02_STATUS.md §1); OCR reale/modello addestrato bloccati da asset ML non producibili in questo ambiente | **BLOCKED** (asset ML mancanti) |
| Q-015 | AP-02 | A3 — Fusione multimodale vitali (HP/MP schermo↔rete via FactFusion) | **Claude** | Q-013 | (vedi AP-02_STATUS.md §4) | **DONE** (ambito ristretto ai vitali; fusione entità Mob/Npc rimandata, stesso blocco ML di Q-014) |
| Q-016 | AP-02 | A4 — Wiring runtime (`ScreenVitalsCapture`: DXGI reale + `ScreenVitalReader` + `NullObjectDetector`, cablato in `WorldModelFusionLoop`/`Program.cs`) | Claude (agente in background) | Q-013, Q-015 | (vedi AP-02_STATUS.md §6) | **DONE** |
| Q-017 | AP-02 | A5 — Audit indipendente AP-02 (2 difetti reali trovati) | Claude (agente in background) | Q-013, Q-015 | `docs/agents/phases/AP-02/AP-02_A5_AUDIT.md` | **DONE** |
| Q-018 | AP-02 | A6 — Integrazione finale AP-02 (2 correzioni applicate, build/test combinati verdi) | **Claude** | Q-017 | (vedi AP-02_STATUS.md §8) | **DONE** |
| Q-019 | AP-03 | A1 — Contratto `MapObservationBatch` (evidenza tile/portali osservazione-side) | **Claude** | Q-018 | `docs/agents/phases/AP-03/AP-03_A1_CLAUDE_map_observation_contract.md` | **DONE** |
| Q-020 | AP-03 | A2 — Projector `MapGrid` (client-archive reale) → `MapObservationBatch`, classificato Cached | **Claude** | Q-019 | (nessun file comando separato, ambito piccolo e meccanico — vedi diff `MapGridObservationProjector.cs`) | **DONE** |
| Q-021 | AP-03 | A3 — Algoritmo di fusione incrementale `MapReconstructionFusion.Merge` (mai perdita di storia, bounds monotoni, idempotente) | **Claude** | Q-019 | (vedi diff `MapReconstructionFusion.cs`) | **DONE** |
| Q-022 | AP-03 | A4 — Persistenza SQLite (`MapModelStore`, WAL/FULL/busy_timeout=5000) + wiring runtime (`MapReconstructionSource` in `WorldModelFusionLoop`/`Program.cs`, dietro `--fuse-world-model`) | Claude (agente in background) | Q-020, Q-021 | `docs/agents/phases/AP-03/AP-03_A4_CLAUDE_persistence_and_wiring.md` | **DONE** |
| Q-023 | AP-03 | A5 — Audit indipendente AP-03 (1 difetto reale trovato: `Resolve` non exception-safe sul percorso di lettura store) | Claude (agente in background) | Q-022 | `docs/agents/phases/AP-03/AP-03_A5_AUDIT.md` | **DONE** |
| Q-024 | AP-03 | A6 — Integrazione finale AP-03 (1 correzione applicata, build/test combinati verdi) | **Claude** | Q-023 | `docs/agents/phases/AP-03/AP-03_STATUS.md` | **DONE** |
| Q-025 | AP-04 | A1 — Contratti esplorazione/navigazione (`ExplorationFootprint`, `FrontierCandidate`, `NavigationWaypoint`, `NavigationPlan`) | **Claude** | Q-024 (eccezione: avviato prima, dipende solo da AP-01) | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` | **DONE** |
| Q-026 | AP-04 | A3 — Algoritmo `ExplorationPlanner` (footprint, ranking frontiera, `NavigationPlan` a singolo waypoint, stessa mappa) | **Claude** | Q-025 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"A3 + contratto A1 mancante" | **DONE** |
| Q-027 | AP-04 | A1 mancante — Contratto `MovementExecutionEvidence`/`MovementExecutionResult` (evidenza esecuzione movimento, WorldFact-based) | **Claude** | Q-025 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"A3 + contratto A1 mancante" | **DONE** |
| Q-028 | AP-04 | A2 — `MovementVerificationProjector` (`MovementVerification` reale → `MovementExecutionEvidence`) | DeepSeek | Q-027 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_scout_command.md` | **DONE** |
| Q-029 | AP-04 | A4 — Comando operatore `--scout` (`ScoutCommand`, chiama `WalkCommand.Execute` con `ActuationAuthority.Commanded`) | DeepSeek | Q-026, Q-028 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_scout_command.md` | **DONE** (livello `Present`: build+test verdi; non ancora `Verified`: nessun client reale) |
| Q-030 | AP-05 | A1 — Contratti Combat Intelligence (`CombatActionCandidate`, `CombatConstraintCheck`, `CombatSimulationResult`, `ComboStep`/`ComboPlan`) | **Claude** | Q-018 (eccezione: dipende solo da AP-01) | `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` | **DONE** |
| Q-031 | AP-05 | A3 (parziale) — `CombatPlanner.GenerateCandidates`/`CheckHardConstraints` da dati reali; simulazione/combo rimandati (nessun dato reale di danno/costo skill in AP-01) | **Claude** | Q-030 | `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` §"A3 (parziale)" | **DONE** |
| Q-032 | AP-06 | A1 — Contratti Quest Graph (`QuestObjectiveKind`/`QuestObjectiveTarget`, `EnrichedQuestObjective`, `QuestReward`, `QuestNode`/`QuestGraph`) + A3 (`QuestGraphPlanner`: avviabilità quest, obiettivi soddisfatti) | **Claude** | Q-018 (eccezione: dipende solo da AP-01) | `docs/agents/phases/AP-06/AP-06_A1_STATUS.md` | **DONE** |
| Q-033 | AP-07 | A1 — Contratti Loadout (`LoadoutActionKind`/`LoadoutActionCandidate`, `LoadoutConstraintCheck`, `LoadoutEvaluation`) + A3 (parziale) — `LoadoutPlanner`: Unequip/Upgrade reali, hard constraints per tutti e tre i `Kind`, `CountActiveQuestNeedsFor` incrociato con AP-06 | **Claude** | Q-018, Q-032 (eccezione: dipende solo da AP-01/AP-06) | `docs/agents/phases/AP-07/AP-07_A1_STATUS.md` | **DONE** |
| Q-034 | AP-08 | A1 — Contratti Strategy (`StrategicGoalKind`, `EnrichedGoal`, `StrategicSignal`, `StrategicPlan`) + A3 (parziale) — `StrategyPlanner`: urgenza Survival/QuestUrgency/Exploration reale, selezione deterministica; Recovery/Progression/Farming/Optimization deliberatamente non assessati (dati mancanti) | **Claude** | Q-018, Q-025, Q-032 (eccezione: dipende solo da AP-01/AP-04/AP-06) | `docs/agents/phases/AP-08/AP-08_A1_STATUS.md` | **DONE** |
| Q-035 | AP-09 | A1 — Scoperta infrastruttura Knowledge/Memory già reale e testata (non rifatta); estensione additiva `MemoryType` (5 categorie mancanti) + A3 — `ActionOutcomeLedgerEntry`/`LocalOutcomeSimulator` (previsione empirica deterministica da storico reale) | **Claude** | Q-018 (eccezione: dipende solo da AP-01) | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` | **DONE** |
| Q-036 | AP-10 | A1 — Contratti Certification (`VerificationLevel`, `CertificationStage`, `CertificationStageResult`, `CertificationReport`) + report onesto e citato dei 14 stadi della DoD: nessuno stadio raggiunge `Verified` per lavoro di questa sessione, `OverallLevel` = `Present` | **Claude** | Tutte le fasi precedenti (report di sintesi, non costruzione) | `docs/agents/phases/AP-10/AP-10_A1_STATUS.md` | **DONE** |
| Q-037 | AP-05 | A1 mancante — Decisione percorso (a) verifica combattimento solo-vitali-player + contratto `CombatExecutionEvidence`/`CombatExecutionResult` | **Claude** | Q-031 | `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` §"Decisione presa: percorso (a)" | **DONE** |
| Q-038 | AP-05 | A2 — `CombatVerificationProjector` (`PlayerVitalsReading` prima/dopo reale → `CombatExecutionEvidence`) | DeepSeek | Q-037 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_engage_command.md` | **DONE** |
| Q-039 | AP-05 | A4 — Comando operatore `--engage <targetEntityId> <skillId>` (`EngageCommand`, esecuzione via `KeybindMap`+`GatedInputBackend.KeyPress`, verifica via `ClientMemorySession.TryReadPlayerVitals`, `ActuationAuthority.Commanded`) | DeepSeek | Q-031, Q-038 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_engage_command.md` | **DONE** (livello `Present`: build+test verdi; non ancora `Verified`: nessun client reale) |
| Q-040 | AP-06 | A2 — `QuestGraphPlanner.AssessCollectProgress` (conteggio osservato per obiettivi `Collect`, da `Player.Inventory` già fuso in AP-01/A2 `Integrated`; nessun lavoro DeepSeek necessario, dati già reali) | **Claude** | Q-032 | `docs/agents/phases/AP-06/AP-06_A1_STATUS.md` §"AP-06/A2+A4" | **DONE** |
| Q-041 | AP-06 | A4 — Comando operatore `--collect <x> <y> <vnum> [<requiredCount>]` (`CollectCommand`, cammina via `WalkCommand.Execute`, verifica via `LiveObservationGateway.Capture()`+`GameplayObservationProjector`+`AssessCollectProgress`, `ActuationAuthority.Commanded`) | DeepSeek | Q-025, Q-040 | `docs/agents/phases/AP-06/AP-06_A2A4_DEEPSEEK_collect_command.md` | **DONE** (livello `Present`: build+test verdi, 5 test nuovi; non ancora `Verified`: serve un client reale + WinDivert elevato) |
| Q-042 | AP-07 | A2+A4 — Indagine mirata: genuinamente bloccato (nessuna primitiva di esecuzione equip/unequip/upgrade in nessuno dei due sistemi, nessun canale di verifica rete per lo stato equipaggiato, nessuna calibrazione UI pannello inventario). Non specificabile per DeepSeek finché non esiste una calibrazione screen-space del pannello o un opcode di rete identificato per l'equip. | **Claude** | Q-033 | `docs/agents/phases/AP-07/AP-07_A1_STATUS.md` §"AP-07/A2+A4" | **DONE** (indagine, non un blocco chiuso) |
| Q-043 | AP-04 | A5 — Audit indipendente `--scout` (1 difetto reale trovato: `onStepVerified` invocato anche per un passo mai emesso, origine nella specifica) | Claude (agente in background) | Q-028, Q-029 | `docs/agents/phases/AP-04/AP-04_A5_AUDIT.md` | **DONE** |
| Q-044 | AP-04 | A6 — Integrazione finale AP-04 (1 correzione applicata: `onStepVerified` spostato dentro `report.Emitted`, test di regressione aggiunto, build/test combinati verdi) | **Claude** | Q-043 | `docs/agents/phases/AP-04/AP-04_STATUS.md` | **DONE** |
| Q-045 | AP-05 | A5 — Audit indipendente `--engage` (1 difetto reale trovato: `Run` lanciava un'eccezione non gestita su un argomento vuoto invece di `[REFUSED]`) | Claude (agente in background) | Q-038, Q-039 | `docs/agents/phases/AP-05/AP-05_A5_AUDIT.md` | **DONE** |
| Q-046 | AP-05 | A6 — Integrazione finale AP-05 (1 correzione applicata: convalida argomenti di `Run` sostituita con `[REFUSED]` pulito, 3 test di regressione aggiunti, build/test combinati verdi) | **Claude** | Q-045 | `docs/agents/phases/AP-05/AP-05_STATUS.md` | **DONE** |
| Q-047 | AP-06 | A5 — Audit indipendente `--collect` (1 difetto reale trovato, stesso pattern di AP-05: `Run` lanciava un'eccezione non gestita su `vnum` vuoto invece di `[REFUSED]`) | **Claude** | Q-040, Q-041 | `docs/agents/phases/AP-06/AP-06_A5_AUDIT.md` | **DONE** |
| Q-048 | AP-06 | A6 — Integrazione finale AP-06 (1 correzione applicata: stesso schema `[REFUSED]` di AP-05, 2 test di regressione aggiunti, build/test combinati verdi) | **Claude** | Q-047 | `docs/agents/phases/AP-06/AP-06_STATUS.md` | **DONE** |
| Q-049 | AP-08 | A2+A4 — Indagine mirata: risposta Survival reale trovata (`UseConsumable` via keybind slot, stesso meccanismo di `--engage` — lavoro AP-05, non nuova infrastruttura AP-08); gap vero è l'assenza di un orchestratore che scelga un'azione da `StrategyPlanner.SelectStrategicPlan` — prima componente che decide invece di eseguire un atto nominato dall'operatore. Non specificato senza decisione esplicita dell'utente su ambito e cautele. | **Claude** | Q-034 | `docs/agents/phases/AP-08/AP-08_A1_STATUS.md` §"AP-08/A2+A4" | **DONE** (indagine, decisione in sospeso) |
| Q-050 | AP-05 | A1 addendum — `CombatExecutionResult.ResourceGainConfirmed` (controparte di recupero di `ResourceCostConfirmed`, per la risposta Survival) | **Claude** | Q-037 | `src/NosAi.Core/WorldModel/Combat/CombatExecutionContracts.cs` | **DONE** |
| Q-051 | AP-05 | A2 — `CombatVerificationProjector.ProjectRecovery` (Health prima/dopo per `UseConsumable`, direzione asserita dall'operatore) | DeepSeek | Q-050 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_recover_command.md` | **DONE** |
| Q-052 | AP-05 | A4 — Comando operatore `--recover <slot>` (`RecoverCommand`, stesso schema di `EngageCommand`, keybind `consumable.{slot}`) | DeepSeek | Q-051 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_recover_command.md` | **DONE** |
| Q-053 | AP-08 | A2+A4 — Comando operatore `--autoplay [--cycles <n>] [--recover-slot <slot>]` (`AutoplayCommand`: orchestratore minimo, dispatch solo Survival→`RecoverCommand`/Exploration→`ScoutCommand`, tetto rigido di cicli, nessun bypass di Guard/Safety) — approvato esplicitamente dall'utente con ambito delimitato | DeepSeek | Q-034, Q-052 | `docs/agents/phases/AP-08/AP-08_A2A4_DEEPSEEK_autoplay_command.md` | **DONE** |
| Q-054 | AP-05 | A5 — Audit indipendente `--recover` (nessun difetto trovato) | **Claude** | Q-051, Q-052 | `docs/agents/phases/AP-05/AP-05_A5_AUDIT.md` §7 | **DONE** |
| Q-055 | AP-05 | A6 — Integrazione finale addendum `--recover` (nessuna correzione necessaria) | **Claude** | Q-054 | `docs/agents/phases/AP-05/AP-05_STATUS.md` §8bis | **DONE** |
| Q-056 | AP-08 | A5 — Audit indipendente `--autoplay` (1 difetto reale trovato: footprint di esplorazione non propagato tra cicli) | **Claude** | Q-053 | `docs/agents/phases/AP-08/AP-08_A5_AUDIT.md` | **DONE** |
| Q-057 | AP-08 | A6 — Integrazione finale AP-08 (1 correzione applicata: `AutoplayCycleResult.UpdatedFootprint`, 2 test di regressione, build/test combinati verdi) | **Claude** | Q-056 | `docs/agents/phases/AP-08/AP-08_STATUS.md` | **DONE** |
| Q-058 | AP-09 | Indagine mirata A2+A4 (ledger): trovato genuinamente bloccato per gap di contratto, non wiring — nessun produttore di `WorldAction`, `ActionOutcomeLedgerEntry.Outcome` senza spazio per Unknown | **Claude** | Q-035 | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` §"AP-09/A2+A4 — indagine mirata" | **DONE** |
| Q-059 | AP-10 | Aggiornamento report certificazione dopo `--recover`/`--autoplay`: stadi 8/12 `Present`→`Integrated, parziale`; `OverallLevel` invariato (`Present`); nessun secondo passo A2+A4 per AP-10 (orchestratore candidato già consegnato da AP-08, bloccato solo dalla Real-environment rule) | **Claude** | Q-036, Q-057 | `docs/agents/phases/AP-10/AP-10_A1_STATUS.md` §"Aggiornamento" | **DONE** |
| Q-060 | AP-09 | Correzione di contratto: `ActionOutcomeLedgerEntry.Outcome` wrappato in `WorldFact<ActionOutcome>` + `WorldActionProjector` (nuovo, produttore reale di `WorldAction` da `CombatExecutionEvidence`/`MovementExecutionEvidence`) — sblocca la scrivibilità di un futuro A2+A4, non ancora specificato | **Claude** | Q-058 | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` §"Correzione di contratto" | **DONE** |
| Q-061 | AP-09 | A2+A4 — persistenza (`ActionOutcomeLedgerStore`, stile `MapModelStore`) + wiring in `--engage`/`--recover`/`--scout`/`--autoplay` (solo `RunWindows`, nessuna firma di `ExecuteOneRound`/`ExecuteOneCycle` toccata); `--collect` esplicitamente escluso (forma evidenza incompatibile) | **DeepSeek** | Q-060 | `docs/agents/phases/AP-09/AP-09_A2A4_DEEPSEEK_ledger_wiring.md` | **DONE** |
| Q-063 | AP-09 | A5+A6 — Audit indipendente + integrazione ledger wiring (Q-061): **nessun difetto reale trovato** (seconda consegna DeepSeek pulita di questa sessione, dopo `--recover`), una nota minore non bloccante su `RecordedAtUtc` | **Claude** | Q-061 | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` §"A2+A4 (Q-061, DeepSeek)" | **DONE** |
| Q-062 | AP-10 | Verifica dedicata stadio 14 (Evidence): `SqliteEventJournal`/hash-chain confermato reale e cablato nel vero `NosAiHost` (Gate 1), test rieseguiti freschi (`SqliteEventJournalTests` 6/6, `NosAiHostTests` 2/2). Stadi 8/13/14 aggiornati; `OverallLevel` invariato (`Present`) | **Claude** | Q-059 | `docs/agents/phases/AP-10/AP-10_A1_STATUS.md` §"Aggiornamento 2" | **DONE** |
| Q-064 | Trasversale | Indagine riconciliazione duplicati (su richiesta esplicita dell'utente): `KnowledgeScope` (Memory/Knowledge) unificabile in sicurezza; `KnowledgeStatus`/`KnowledgeLifecycle` **non** fondibili (concetti distinti) ma con un bug reale nella proiezione ad-hoc; `DataSourceKind` triplicato confermato **duplicazione intenzionale** per bounded context (vincolo `NosAi.Core` zero-dipendenze) | **Claude** (2 agenti in background) | — | vedi Q-065/Q-066 | **DONE** |
| Q-065 | Trasversale | Riconciliazione `KnowledgeScope`: dichiarazione in `NosAi.Core.Knowledge` rimossa, unificata su `Memory.KnowledgeScope`; entrambi i `MapScope` rimossi. Nuovo `KnowledgeLifecycleProjection.ToKnowledgeStatus` (totale, esplicito) sostituisce lo switch ad-hoc che collassava silenziosamente `Deprecated`/`Forbidden`/`RevalidationRequired` su `Candidate` — bug reale trovato dall'indagine, corretto | **Claude** | Q-064 | `src/NosAi.Core/Knowledge/KnowledgeLifecycleProjection.cs`, `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` | **DONE** |
| Q-066 | Trasversale | `DataSourceKind` triplicato: **non unificato**, chiuso con ADR invece che con codice — confermato duplicazione intenzionale (bounded context, vincolo reale `NosAi.Core.csproj` zero-dipendenze), due bridge di conversione già esistenti e corretti nei due punti dove i domini si incontrano | **Claude** | Q-064 | `docs/adr/ADR-0026-datasourcekind-intentional-bounded-context-duplication.md` | **DONE** |
| Q-067 | AP-02 | A2+A4 — cablare `TargetStateComposer` in `ScreenVitalsCapture`: gap dichiarato non bloccato da OCR/ONNX (`TargetRoiCalibration`/`ScreenTargetFrameSource`/`TargetStateComposer` già reali, già usati da `TargetAwareGameplayProvider` lato wire). Nuovo `SingleFrameSource` preserva l'invarianza "un frame, più lettori"; `wire: null` esplicito (nessun `IPlayerAttackObserver` disponibile al punto di composizione) | **DeepSeek** | — | `docs/agents/phases/AP-02/AP-02_A2A4_DEEPSEEK_target_state_wiring.md` | **DONE** |
| Q-068 | AP-02 | A5 — Audit indipendente wiring `TargetStateComposer` (Q-067): **nessun difetto trovato** (terza consegna DeepSeek pulita di questa sessione, dopo `--recover` e il ledger AP-09) | **Claude** | Q-067 | `docs/agents/phases/AP-02/AP-02_STATUS.md` §11 | **DONE** |
| Q-069 | Trasversale | Indagine (in parallelo a Q-067, su richiesta esplicita dell'utente): statistiche reali skill/item — **genuinamente bloccato** (`GameReferenceDatabase` decodifica solo byte-esatta, nessun campo con significato semantico dichiarato); fonte dati portali — **percorso reale trovato** ("osserva mentre attraversi" da `ClientMemorySession`, non parsing file client) | **Claude** (2 agenti in background) | — | vedi Q-070 | **DONE** |
| Q-070 | AP-04 | A1+A3 — `PortalCrossingDetector` (nuovo, puro): deriva un `Portal` reale da due letture consecutive mappa+posizione quando la mappa cambia; `Portal.Id` da mappa sorgente + posizione arrotondata (stabile per `MapReconstructionFusion.MergeByKey`). Si integra nel campo `MapObservationBatch.Portals` già esistente (AP-03), nessun nuovo meccanismo di persistenza. A2+A4 (wiring del polling) non specificato in questa sessione | **Claude** | Q-069 | `src/NosAi.Core/WorldModel/Reconstruction/PortalCrossingDetector.cs`, `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Seconda indagine sui portali" | **DONE** |
| Q-071 | AP-04 | A2+A4 — wiring `PortalCrossingDetector`: nuovo `MapReconstructionSource.RecordPortalCrossing` (merge indipendente dal `Resolve` del ciclo corrente, mai tocca la cache di una mappa diversa) + hook di polling in `ScoutCommand`/`AutoplayCommand.RunWindows` (riusa il polling mappa/posizione già esistente, nessuna nuova infrastruttura). `--collect`/`--engage`/`--recover` esplicitamente esclusi (non pollano map id per round) | **DeepSeek** | Q-070 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_portal_crossing_wiring.md` | **DONE** |
| Q-072 | AP-04 | A5 — Audit indipendente wiring `RecordPortalCrossing` (Q-071): **nessun difetto trovato** (quarta consegna DeepSeek pulita di questa sessione) | **Claude** | Q-071 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Audit A5 (Claude) — Q-071" | **DONE** |
| Q-073 | AP-02 | A1+A3 — Rilevamento presenza/assenza finestra di dialogo (nuovo, puro): `DialogRoiCalibration` (ROI + baseline colore confermata da operatore), `DialogWindowReader` (delta euclideo media B/G/R vs baseline, soglia `DefaultPresentThreshold=24.0` dichiarata scelta di giudizio), `DialogWindowStateComposer` (nessuna riconciliazione wire: nessun opcode dialogo/quest text esiste), `ScreenDialogWindowSource` (stesso ordine di guardie di `ScreenTargetFrameSource`). Su richiesta esplicita dell'utente, nessun client reale per verificare la tecnica | **Claude** | — | `docs/agents/phases/AP-02/AP-02_STATUS.md` §12 | **DONE** |
| Q-074 | AP-02 | A1 — Contratto `VisualObservation` esteso con `HasDialogWindow` (stesso schema di `HasTarget`); `ScreenVitalsCapture.Capture()` lo popola con placeholder onesto `Unknown(dialog_window_composer_not_wired_in_this_pass)`, stesso schema che `HasTarget` portava prima di Q-067 | **Claude** | Q-073 | `docs/agents/phases/AP-02/AP-02_STATUS.md` §12 | **DONE** |
| Q-075 | AP-02 | A2+A4 — wiring reale del rilevamento dialog-window in `ScreenVitalsCapture`/`Program.cs`: nuovo `ScreenDialogWindowSource` sul frame già acquisito (`SingleFrameSource`), `DialogWindowStateComposer.Compose` senza argomento wire (nessun opcode dialogo/quest text), caricamento `DialogRoiCalibration` in `Program.cs` identico a `TargetRoiCalibration`. Specifica mirror esatto di Q-067 | **DeepSeek** | Q-074 | `docs/agents/phases/AP-02/AP-02_A2A4_DEEPSEEK_dialog_window_wiring.md` | **DONE** |
| Q-076 | AP-02 | A5 — Audit indipendente wiring dialog-window (Q-075): **nessun difetto trovato** (quinta consegna DeepSeek pulita consecutiva di questa sessione) | **Claude** | Q-075 | `docs/agents/phases/AP-02/AP-02_STATUS.md` §13 | **DONE** |
| Q-077 | AP-04 | A3 — `MultiMapRoutePlanner` (nuovo, puro): BFS su `Portal` osservati (`Portal.SourceMap`→`DestinationMap`), mai bidirezionale per assunzione, mai un arco sintetizzato su un `IsActive` non confermato. Popola `NavigationPlan`/`NavigationWaypoint` (contratto AP-04/A1 già multi-mappa-capace) da dati reali invece di restare vincolato a una sola mappa | **Claude** | Q-072 | `src/NosAi.Core/WorldModel/Exploration/MultiMapRoutePlanner.cs`, `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Terza indagine" | **DONE** |
| Q-078 | AP-04 | A2+A4 — enumerazione mappe (`MapModelStore.ListMapIds`, `MapReconstructionSource.LoadAllKnownMaps`, entrambi best-effort) + nuovo comando diagnostico read-only `--route <mapId> <x> <y>` (`RouteProbe`, stesso schema di `TargetChainProbe`: pianifica e stampa, non cammina) | **DeepSeek** | Q-077 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_route_command.md` | **DONE** |
| Q-079 | AP-04 | A5 — Audit indipendente `--route`/enumerazione mappe (Q-078): **nessun difetto trovato** (sesta consegna DeepSeek pulita consecutiva di questa sessione) | **Claude** | Q-078 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Audit A5 (Claude) — Q-078" | **DONE** |
| Q-080 | Trasversale | A1+A3 — `SkillReferenceDecoder` (nuovo, puro): promuove a colonne tipizzate `COST`/`LEVEL`/`TARGET` e i soli campi `DATA` risolti con sicurezza (`CastTime`/`Cooldown`/`MpCost`) del record skill, più i riferimenti `BASIC`→`BCardApplication` (struttura, non interpretazione). Campi codificati senza mappatura verificata (elemento, tipo attacco, ...) restano onestamente non decodificati, mai indovinati. Su richiesta esplicita dell'utente, nessuna fonte esterna citata in codice/documenti | **Claude** | — | `src/NosAi.Runtime/GameData/SkillReferenceDecoder.cs` | **DONE** |
| Q-081 | Trasversale | A1+A3 — `SkillReferenceDecoder` esteso: `TYPE` (SkillType/CastId/JobClass/AttackType/SecondaryWeapon/Element) e `DATA` per intero (UpgradeSkill/PartnerSkillId/DashSpeed/RequiredItemVnum/DataRange/DataTargetRange, oltre a CastTime/Cooldown/MpCost già presenti). Mappatura da https://nt-research.github.io/ (documentazione community dei formati client) — istruzione esplicita dell'utente: citare la fonte questa volta, in codice e qui. Resta provvisorio finché non incrociato con un valore reale del client (nessun confronto ancora eseguito) | **Claude** | Q-080 | `src/NosAi.Runtime/GameData/SkillReferenceDecoder.cs`, `tests/NosAi.Runtime.Tests/GameData/SkillReferenceDecoderTests.cs` | **DONE** |
| Q-082 | Trasversale | A1+A3+A6 — Rilevamento aggiornamenti client come funzionalità di base: nuova tabella `client_inventory` in `GameReferenceDatabase` con diff added/changed/removed; nuovo comando operatore `--client-updates` (`ClientUpdateCommand`) che rilancia sia `ReferenceImporter.ImportAll` sia una scansione dell'intera directory client e riporta cosa è cambiato. Valutata prima un'integrazione di `taletool` (github.com/imxeno/taletool, AGPL-3.0-or-later) come processo esterno vendorizzato; sostituita su istruzione esplicita dell'utente con `ClientDirectoryScanner` (nuovo, puro): classifica ogni file riusando `NosArchive`, il lettore nativo già in uso da `ReferenceImporter` — nessun processo esterno, nessuna dipendenza AGPL | **Claude** | — | `src/NosAi.Runtime/GameData/ClientDirectoryScanner.cs`, `src/NosAi.Runtime/GameData/GameReferenceDatabase.cs`, `src/NosAi.Runtime/Observability/ClientUpdateCommand.cs`, `src/NosAi.Runtime/Program.cs`, `third_party/sources/taletool/UPSTREAM.md` | **DONE** |
| Q-083 | Trasversale | A3+A6 — Rilevamento aggiornamenti client eseguito automaticamente a ogni avvio reale di `NosAi.Runtime` (non solo su richiesta esplicita con `--client-updates`): `ClientUpdateCommand.RunDetailed` (nuovo, aggiunge `ClientUpdateReport.AnyChange` accanto al testo già esistente) invocato in `Program.Main` subito dopo l'avvio di Gate 1. Silenzioso quando nulla è cambiato; stampa il report solo quando rileva una differenza reale. Verificato sul client reale dall'utente: prima esecuzione con SSD `NOSAI-SSD` vuoto → tutto `+N` (2705 monster, 7727 item, 1958 skill, 2763 card, 131 bcard, 314 file); seconda esecuzione immediata, nessuna modifica al client → tutto `=N`, conferma che la persistenza/diff funziona | **Claude** | Q-082 | `src/NosAi.Runtime/Observability/ClientUpdateCommand.cs`, `src/NosAi.Runtime/Program.cs`, `tests/NosAi.Runtime.Tests/ClientUpdateCommandTests.cs` | **DONE — Verified (evidenza utente sul client reale)** |
| Q-084 | AP-07 | A1 — Seconda indagine mirata AP-07/A2+A4 (equip/inventario), re-verifica dei tre blocchi dichiarati: primitiva di esecuzione (confermato assente, non prioritario), calibrazione screen-space del pannello (confermato assente, **consegnato** `InventoryPanelRoiCalibration` — ritagli, uno per `EquipmentSlot`, stesso schema di `DialogRoiCalibration`/`TargetRoiCalibration`, tutti insieme o nessuno), canale di verifica (**parzialmente confutato** — `InventoryKind` ora documentato da fonte esterna citata OpenNos/GPL, un valore incrociato con una cattura reale di questo repository `Etc=2`, `Wear=8` candidato per "equipaggiato" ma non ancora incrociato). Specifica DeepSeek per la sola calibrazione (`--calibrate-inventory-panel`), non ancora esecuzione | **Claude** | Q-035 | `src/NosAi.Runtime/Perception/InventoryPanelRoiCalibration.cs`, `src/NosAi.Runtime/Perception/Network/GameTrafficObserver.cs`, `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs`, `docs/agents/phases/AP-07/AP-07_A1_STATUS.md`, `docs/agents/phases/AP-07/AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md` | **DONE** |
| Q-085 | Trasversale | A1 — Analisi approfondita di `itempicker.atlagaming.eu` (istruzione esplicita dell'utente) ha trovato un difetto reale in Q-084: `EquipmentSlot` (`InventoryContracts.cs`) portava un segnaposto sbagliato a 8 valori (`Weapon, Shield, Helmet, Armor, Gloves, Boots, Accessory1, Accessory2`) invece dei 18 valori reali di NosTale. Corretto a `Weapon, Armor, Hat, Gloves, Boots, SecondaryWeapon, Necklace, Ring, Bracelet, Mask, Fairy, Amulet, SpecialistCard, CostumeSuit, CostumeHat, WeaponSkin, CostumeWings, MiniPet`, derivato dal campo `equipmentSlot` di `items.json` incrociato con nomi item italiani reali; due file di test che referenziavano `.Helmet` aggiornati a `.Hat` (stesso slot, nome corretto). Stesso cross-check ha anche verificato campo-per-campo (skill vnum 201) il layout tag di `SkillReferenceDecoder` per 8 tag — doc-comment aggiornato da "provvisorio" a "posizione confermata per questi 8, il resto resta provvisorio". Corretto anche `AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md`, che citava ancora "otto slot"/l'enum sbagliato (v1, scritto prima di questa scoperta). **Nota per l'integrazione**: l'utente ha avviato DeepSeek su Q-084 con la versione non corretta dello spec — l'output di DeepSeek per `--calibrate-inventory-panel` va verificato in A5/A6 per riferimenti a nomi/enum rimossi o al conteggio "8" invece di "18" token | **Claude** | Q-084 | `src/NosAi.Core/WorldModel/InventoryContracts.cs`, `src/NosAi.Runtime/GameData/SkillReferenceDecoder.cs`, `tests/NosAi.Core.Tests/WorldModel/Loadout/LoadoutContractsTests.cs`, `tests/NosAi.Core.Tests/WorldModel/Loadout/LoadoutPlannerTests.cs`, `docs/agents/phases/AP-07/AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md`, `docs/research/NOSTALE_COMMUNITY_KNOWLEDGE_2026-09-05.md` | **DONE** |
| Q-086 | AP-07 | A2+A4 — Comando operatore `--calibrate-inventory-panel` (specifica `AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md`): `InventoryPanelCalibrationProbe.TryParseSlots` (un token `<EquipmentSlot>:<x>,<y>,<w>,<h>` per ogni valore dichiarato dell'enum, conta derivata da `Enum.GetValues`, mai hardcoded), `Run` console che rispecchia `HudProbe` (attach client → frame DXGI → area client → preview `inventory_panel_latest.bmp` via nuovo `HudCropWriter.TrySavePanelPreview` additivo → `Confirmed`+`Save` solo su conferma operatore, `[REFUSED]` senza file parziale), wiring in `Program.cs` + `KnownProbeFlags`. Zero token = solo report stato. Testata la parità col client reale della preview via smoke run sul desktop di sviluppo: preview BMP valida 1920x1200 scritta e gitignored, nessuna calibrazione scritta senza conferma (report-only non tocca il file). **Correzione in integrazione (A5/A6, nota Q-085)**: la prima stesura era contro la specifica v1 (8 slot pre-Q-085); riallineata all'enum a 18 valori reale — guida operatore e rifiuti ora stampano i nomi/conteggi dall'enum, e tutti i test sono scritti su `Enum.GetValues` così nessuna assunzione sul numero di slot può rientrare. Build 0 errori/0 warning (1 warning xUnit preesistente non collegato), Runtime full 2178/2183 (5 fallimenti non correlati e dipendenti dall'ambiente host — vedi nota in fondo alla coda), Core 643/643, filtri calibrazione tutti verdi | **DeepSeek** | Q-085 | `src/NosAi.Runtime/Perception/InventoryPanelCalibrationProbe.cs`, `src/NosAi.Runtime/Perception/HudCropWriter.cs`, `src/NosAi.Runtime/Program.cs`, `tests/NosAi.Runtime.Tests/InventoryPanelCalibrationProbeTests.cs` | **DONE — Present** |
| Q-087 | Trasversale | A1 — Analisi approfondita di `monsters.json`/`maps.json` (istruzione esplicita dell'utente). Trovata documentazione reale del formato `monster.dat` (`nt-research.github.io`, stesso sito già citato per Skill.dat — quella pagina cita a sua volta Itempicker e NosApki come risorse collegate). Nuovo `MonsterReferenceDecoder` (mirror esatto dello stile di `SkillReferenceDecoder`): decodifica 21 dei 23 tag documentati, drop table (`ITEM`×20 → `MonsterDrop.ItemVnum/Chance/Amount`) corroborata da `monsters.json` (stessi 3 campi, stesso ordine, `chance` coerente con la scala grezza documentata); `PARTNER` (sempre zero, nessun significato noto) e `MODE` (struttura ambigua nella sola documentazione, nessun file client reale da ispezionare) omessi di proposito con motivazione esplicita, non indovinati. **Non cross-checked contro un client reale** (nessun client in questo sandbox) — dichiarato provvisorio, stesso standard di `SkillReferenceDecoder` prima della sua verifica. `maps.json` (dimensioni mappa): nessun gap trovato — `MapGrid.Width`/`Height` derivano già dai file `.NSTC` reali del client (fonte di prima mano più autorevole); `MapModel` (World Model) non porta un campo dimensione fisso (confini osservati incrementalmente) — nessuna integrazione fatta, sarebbe dato ridondante | **Claude** | Q-085 | `src/NosAi.Runtime/GameData/MonsterReferenceDecoder.cs`, `tests/NosAi.Runtime.Tests/GameData/MonsterReferenceDecoderTests.cs`, `docs/research/NOSTALE_COMMUNITY_KNOWLEDGE_2026-09-05.md` | **DONE — Present (non cross-checked contro client reale)** |
| Q-088 | Trasversale | A1 — Terzo decoder della stessa famiglia (dopo Skill/Monster): `ItemReferenceDecoder` per `Item.dat` (fonte `nt-research.github.io`, che a sua volta cita `NosCore.Parser/Parsers/ItemParser.cs`, `OpenNos.Import.Console/ImportFactory.cs` e `itempicker.atlagaming.eu`). Decodifica VNUM/Price, INDEX (InventoryType/ItemType/ItemSubType/Slot/IconId/VisualChangeId), TYPE (AttackType/RequiredClass), i 23 FLAG nominati, i 20 DATA grezzi (il formato dichiara esplicitamente che il significato dipende dal tipo item — mai interpretati), BUFF (5 BCard) e LINEDESC. **Scoperta di rilievo**: la pagina Item.dat documenta il proprio campo `EquipmentSlot` citando `OpenNos.Domain/EquipmentType.cs` con gli stessi identici 18 valori nello stesso ordine di `EquipmentSlot` (Q-085) — seconda conferma indipendente dello stesso schema, oltre a quella già fatta via nomi item italiani; per questo `ItemReference.Slot` è l'unico campo tipizzato (sugli altri resta int grezzo, stesso standard di cautela di Skill/Monster). Non cross-checked contro un client reale, stesso limite dichiarato per `MonsterReferenceDecoder` | **Claude** | Q-087 | `src/NosAi.Runtime/GameData/ItemReferenceDecoder.cs`, `tests/NosAi.Runtime.Tests/GameData/ItemReferenceDecoderTests.cs`, `src/NosAi.Core/WorldModel/InventoryContracts.cs` | **DONE — Present (non cross-checked contro client reale)** |
| Q-089 | AP-07 | T-12 — Prima verifica reale su client vivo di `--calibrate-inventory-panel` (Q-086). L'operatore ha aperto il pannello "Info Personaggio" (finestra `TNosTaleMainF`, area client 1024x768), confermato la griglia 3×6 dei 18 `EquipmentSlot` (nessuna fonte online ha mai documentato l'esatta disposizione a schermo — Fandom/wiki italiana/gameguide ufficiale irraggiungibili, elitepvpers blocca i bot; usata una guida Steam Community reale come lead per l'ordine di lettura, incrociata con 2 icone riconoscibili nella schermata dell'operatore — pugnali incrociati per `Weapon`, ritratto per `SpecialistCard` — poi confermata per intero dall'operatore stesso) ed eseguito il comando con tutti e 18 i token: `InventoryPanelRoiCalibration` scritta con successo (nessun `[REFUSED]`). **Verified** per la sola calibrazione screen-space (evidenza reale, non simulata); resta `Present` il resto di AP-07/A2+A4 — la conferma `InventoryKind` per un equip reale (T-12, seconda metà) non è ancora fatta | **Claude** | Q-086 | `docs/TEST_RIMANDATI.md` | **DONE — Verified (calibrazione, evidenza operatore su client reale)** |
| Q-090 | Trasversale | A6 — Istruzione esplicita dell'utente: mai più far digitare comandi per un test reale, solo bottoni. Nuova vista "Equipaggiamento" in `NosAi.ControlPanel` (`MainWindow`), stesso schema già in uso per Percezione/Certificazione (bottone → `ToolRunner.RunAsync` in sottoprocesso → log). Card per il test T-12 rimasto (conferma `InventoryKind` da un equip reale): riusa il rilevamento endpoint già esistente (`OnDetectObserveGame`), bottone "Registra equip (30s)" (`--record-wire <endpoint> data/equip_test.noscap --watch 30`), bottone "Analizza registrazione" (`--world-replay`, filtra e mostra solo le righe con `kind=`). `RunToolAsync` esteso in modo additivo (parametro opzionale `onLine`, ritorna `ToolResult` invece di scartarlo) — nessuna firma di chiamante esistente rotta. Pattern pensato per essere riusato: ogni test reale futuro è una nuova card nella stessa vista. Build verde (0 errori/0 warning); i test di `NosAi.ControlPanel.Tests` non eseguibili in questo sandbox Linux (richiedono il runtime Desktop Windows, limite ambientale pre-esistente, non regressione) — verifica reale rimandata all'operatore | **Claude** | Q-089 | `src/NosAi.ControlPanel/MainWindow.xaml`, `src/NosAi.ControlPanel/MainWindow.xaml.cs` | **DONE — Present (build verificata, esecuzione reale rimandata all'operatore)** |
| Q-091 | AP-07 | A2+A4 — Registrazione reale mostrò 6 pacchetti `ivn` mai decodificati (`inventory (ivn): 0 reason=inventory_slot_empty`) e opcode `eq`/`equip`/`bf`/`pairy`/`sc`/`dir`/`eff` mai gestiti — causa non ancora nota, non indovinabile senza vedere i campi reali. Istruzione esplicita dell'utente: decoder in tempo reale dentro NosAiProject, non solo analisi offline. Nuovo `LiveWireMonitor` (`--live-decode <ip:porta> [--watch N]`, stesso `WinDivertPacketSource` sniff-only di `WireRecorder`): per ogni frame in arrivo stampa subito la riga `RETE` grezza (`NosTaleWorldDecoder.Decode`, qualunque opcode, nessuna interpretazione) e, quando l'opcode è tra i 12 letti da `NosTaleWorldProtocolDecoder`, una riga `CLIENT` con la lettura semantica (vitali/avvistamenti/eventi/inventario/...); un frame outbound (cifrato) resta `<non decifrabile>`, mai bytes indovinati. Logica pura isolata in `LiveWireMonitor.Monitor(IPacketSource, TextWriter, CancellationToken)`, testabile con `InMemoryPacketSource` senza driver (stessa separazione di `WireRecorder.RecordFrom`). Nuova card "Decodifica dal vivo" in Equipaggiamento (`OnStartLiveDecode`, 180s, bottone unico): ogni riga finisce nel Diario in tempo reale via `RunToolAsync`, la card mostra solo il riepilogo finale. Non risolve ancora il gap `ivn`/`eq` — serve l'operatore che lanci il test e mandi le righe reali osservate | **Claude** | Q-090 | `src/NosAi.Runtime/LiveIntegration/Capture/LiveWireMonitor.cs`, `src/NosAi.Runtime/Program.cs`, `src/NosAi.ControlPanel/MainWindow.xaml`, `src/NosAi.ControlPanel/MainWindow.xaml.cs`, `tests/NosAi.Runtime.Tests/LiveWireMonitorTests.cs` | **DONE — Present (build 0 errori, Runtime 2149/2149 + 4 nuovi test; verifica reale contro il client rimandata all'operatore)** |
| Q-092 | AP-08 | A1+A3 — Istruzione esplicita dell'utente: accelerare, dare a DeepSeek lavoro reale non fabbricato. Indagine mirata su `AP-10_A1_STATUS.md` (stadio 12, Recovery): il gap "nessun assessor per `StrategicGoalKind.Recovery`" è reale ma sbloccabile senza nuovo canale di rete/elevazione — un calo HP osservato su due letture consecutive già lette ogni ciclo da `--autoplay` per Survival basta come fatto "in combattimento adesso" onesto. `CombatRecencyTracker` (nuovo, puro, `NosAi.Core`): deriva un `WorldFact<bool>` dal calo HP con finestra di decadimento 15s, `Unknown("insufficient_history")` al primo poll, mai "non in combattimento" per assunzione. `StrategyPlanner.AssessRecoveryUrgency` (nuovo, puro): stessa formula di urgenza di `AssessSurvivalUrgency` ma `null` mentre `inCombat.Value==true`, cosi' non duplica il segnale di Survival sotto altro nome — la ragione esatta per cui Recovery era rimasto non valutato finora. 11 nuovi test (6 tracker + 5 planner), tutti verdi; nessuna modifica a `AutoplayCommand` in questo passaggio (resta A2+A4 vero, nessun chiamante runtime) — specifica DeepSeek scritta per il solo collegamento meccanico, nessun nuovo flag CLI necessario | **Claude** | Q-090 | `src/NosAi.Core/WorldModel/Strategy/CombatRecencyTracker.cs`, `src/NosAi.Core/WorldModel/Strategy/StrategyPlanner.cs`, `src/NosAi.Runtime/Tactical/AutoplayCommand.cs` (solo doc-comment), `tests/NosAi.Core.Tests/WorldModel/Strategy/CombatRecencyTrackerTests.cs`, `tests/NosAi.Core.Tests/WorldModel/Strategy/StrategyPlannerTests.cs`, `docs/agents/phases/AP-08/AP-08_A2A4_DEEPSEEK_recovery_signal.md` | **DONE — Present (A1+A3 Claude: build 0 errori/0 warning, Core 654/654; A2+A4 DeepSeek consegnato 2026-09-06, commit `a5e587e`+`06987bc` push diretti su `main` — `AutoplayCommand.ExecuteOneCycle` dispatcha `Recovery` come `Survival` via `DispatchRecovery` condiviso, `RunWindows` traccia `CombatRecencyTracker.State` per ciclo. Audit indipendente Claude ha trovato 2 difetti reali nella consegna, entrambi corretti nello stesso passaggio: (1) `tests/.../AutoplayCommandTests.cs:342` elencava ancora `Recovery` come kind non-dispatchabile — teoria stantia dopo il wiring, rimossa la riga; (2) commento copiato dalla spec in `AutoplayCommand.cs` affermava "recovery/survival never both non-null at once", falso (verificato su `StrategyPlanner.cs`: fuori combattimento con HP nota sotto il massimo entrambi tornano non-null, caso comune) — riscritto per dire la vera ragione (list-order decide il tie-break). Rationale corretto anche nella spec storica. Build 0 errori/0 warning, Core 654/654, Runtime 2150/2150 (60 skip pre-esistenti, client reale). Resta `Present`, non `Verified`: nessun operatore ha ancora osservato un dispatch Recovery reale su client)** |

## Regola per Q-014/Q-015/Q-016/Q-017/Q-018 e per tutte le fasi successive

I comandi dettagliati per i task oltre Q-008 **non sono ancora scritti di proposito**: scriverli ora, prima che i contratti di AP-01 esistano davvero, rischierebbe di fissare dettagli sbagliati che poi vanno disfatti (esattamente il tipo di "casino" da evitare). La regola è: **quando una voce `PENDING` diventa la prima della coda, Claude scrive il suo comando dettagliato (stile dei file già prodotti oggi, non gli stub telegrafici originali) prima di farla partire**, poi la esegue lui stesso o la assegna a DeepSeek a seconda della colonna Esecutore.

## Fasi successive — nessuna: AP-00→AP-10 hanno tutte almeno un task A1 in coda

Dalla roadmap canonica (`docs/ROADMAP_ESECUTIVA.md`). AP-03 è `Integrated` (Q-024); AP-04 ha A1+A3 `Present` (Q-025/Q-026/Q-027, eccezione dichiarata) e **A2+A4 consegnati da DeepSeek il 2026-09-06** (Q-028/Q-029: `MovementVerificationProjector` + comando operatore `--scout` — `ActuationAuthority.Commanded`, chiama `WalkCommand.Execute` reale con il solo parametro opzionale additivo `onStepVerified`). Routing multi-mappa via portali resta rimandato (nessuna fonte dati reale, vedi `AP-04_A1_STATUS.md`). Non è un bridge verso `Gate3Runtime` (indagine conclusa: quella pipeline è chiusa/hardcoded e le mancano tre pezzi reali, lavoro futuro di AP-08 "Strategic Autonomy + HTN", non di AP-04). **A5+A6 completati (Q-043/Q-044, 2026-09-07)**: audit indipendente ha trovato 1 difetto reale (`onStepVerified` invocato anche per un passo mai emesso, un rifiuto guard a metà cammino — origine nella specifica di Claude, non una deviazione di DeepSeek), corretto in integrazione con un test di regressione dedicato. **AP-04 è ora `Integrated`.** Vedi `AP-04_STATUS.md`.

AP-05 ha A1 `Present` (Q-030) e A3 parziale `Present` (Q-031: generazione candidati + hard constraints reali; simulazione/combo rimandati, nessun dato reale di danno/costo skill). Indagine su `Gate3Runtime` per skill/attacco **conclusa**: stessa classe di problema del movimento (selezione skill hardcoded, predizione placeholder, Guard che decide su rischio fabbricato) — nessun bridge verso `Gate3Runtime`. A differenza del movimento, non esiste un `WalkCommand`-equivalente già reale per l'esecuzione combattimento, e la verifica reale che esiste (`PostConditions.cs`) è accoppiata a metodi privati di `Gate3Runtime` non riusabili; verificare un attacco riuscito richiederebbe anche l'HP del mob bersaglio, mai fuso nel World Model canonico (gap AP-02, OCR/ML). **Decisione presa (Q-037): percorso (a)** — verifica combattimento solo-vitali-player, onesta ma parziale (conferma il costo risorsa, non il colpo sul bersaglio), scelta perché il percorso (b) resta bloccato indefinitamente sul gap OCR/ONNX. Contratto mancante `CombatExecutionEvidence`/`CombatExecutionResult` scritto e testato; **A2+A4 consegnati da DeepSeek il 2026-09-06 (Q-038/Q-039, `--engage`)** — `CombatVerificationProjector` + `EngageCommand` (esecuzione `UseSkill` via keybind confermato + `GatedInputBackend`, verifica `TryReadPlayerVitals` prima/dopo). Limiti dichiarati nel comando: `BasicAttack` rifiutato per design (nessun costo risorsa osservabile), nessun ponte al Safety Gate reale per un atto senza pixel (il gate con commit point richiede uno scope per atti puntuali; il round riporta il rifiuto onestamente — verificato vero da A5, non solo dichiarato). **A5+A6 completati (Q-045/Q-046, 2026-09-07)**: audit indipendente ha trovato 1 difetto reale (`Run` lanciava un'eccezione non gestita su un argomento vuoto invece di `[REFUSED]`), corretto in integrazione con 3 test di regressione dedicati. **AP-05 è ora `Integrated`.** **Addendum `--recover` (Q-050/Q-051/Q-052/Q-054/Q-055)**: risposta Survival reale (`UseConsumable`, sibling di `--engage`), consegnata da DeepSeek e integrata senza correzioni necessarie. Vedi `AP-05_STATUS.md`.

AP-06 ha A1+A3 `Present` (Q-032, eccezione dichiarata: dipende solo da AP-01). Grafo quest (obiettivi tipizzati, prerequisiti, reward) e logica di avviabilità/soddisfazione obiettivi reali, senza dati inventati. `Quest`/`QuestObjective` (AP-01) lasciati invariati per lo stesso principio già seguito in AP-04/AP-05. Indagine mirata su "semantic extraction OCR/UI/**network**" (Q-040): il canale network per `Collect` è reale e già fuso (`ivn`/`get`/`drop` decodificati, `GameplayObservationProjector` già `Integrated`) — non bloccato come l'OCR/ML lo è per gli altri obiettivi. `Travel` non richiede nulla di nuovo (`--walk` già lo fa); `Kill`/`Dialogue`/`Interact`/`Deliver` restano bloccati (nessuna classificazione mob/NPC da rete, nessuna primitiva di interazione NPC). `CollectCommand` consegnato da DeepSeek (Q-041) e **integrato (Q-047/Q-048)**: A5 ha trovato 1 difetto reale (stesso pattern di AP-05: `Run` lanciava un'eccezione su `vnum` vuoto), corretto in A6. **AP-06 è ora `Integrated`.** Limite dichiarato: nessuna scoperta automatica di ground item (estensione futura), e lo stesso limite di occupazione condiviso con `--walk`/`--scout` (nessun feed live, vedi `AP-06_A5_AUDIT.md`). Vedi `AP-06_STATUS.md`.

AP-07 ha A1+A3 (parziale) `Present` (Q-033, eccezione dichiarata). `LoadoutPlanner.GenerateUnequipCandidates`/`GenerateUpgradeCandidates`/`CheckHardConstraints` reali da dati già noti; generazione automatica di candidati Equip bloccata (nessun dato di categoria item in `InventoryItem`); `LoadoutEvaluation` resta quasi tutta non popolata (nessuna statistica reale item/upgrade, stesso gap dati di AP-05) tranne `QuestRelevance`, calcolata incrociando onestamente il Quest Graph di AP-06. Indagine mirata su A2+A4 (Q-042): a differenza di AP-05/AP-06, qui **nessun** percorso onesto parziale esiste — né una primitiva di esecuzione (equip/unequip/upgrade non modellati nemmeno nel vecchio `ActionType`, nessuna hotkey, nessuna calibrazione UI pannello) né un canale di verifica rete (`InventoryKind` non distingue equipaggiato da zaino). Non specificabile per DeepSeek; serve prima infrastruttura nuova (calibrazione screen-space del pannello, o un opcode di rete equip mai identificato). **Seconda indagine (Q-084, 2026-09-06)**: un varco reale trovato — la calibrazione screen-space del pannello è ora specificabile con lo stesso schema di `TargetRoiCalibration`/`DialogRoiCalibration`; consegnato `InventoryPanelRoiCalibration` (Claude/A1). **Q-085**: `EquipmentSlot` corretto da segnaposto 8 valori ai 18 valori reali NosTale (fonte esterna incrociata). **Q-086 (2026-09-06, DeepSeek, livello `Present`)**: comando `--calibrate-inventory-panel` consegnato e riallineato ai 18 valori in integrazione — vedi riga Q-086 per l'evidenza completa. **Q-089 (2026-09-06): la calibrazione è ora `Verified`** — l'operatore ha confermato sul client reale la griglia 3×6 dei 18 slot ed eseguito il comando con successo (`InventoryPanelRoiCalibration` scritta, nessun `[REFUSED]`). Resta aperta solo la seconda metà di T-12: una cattura dedicata deve ancora confermare quale `InventoryKind` (`Wear=8` candidato) produce un equip reale; `--equip`/`--unequip` restano non specificabili finché quella non esiste. Vedi `AP-07_A1_STATUS.md`.

AP-08 ha A1+A3 (parziale) `Present` (Q-034, eccezione dichiarata): solo la tappa "Strategic Utility" (selezione del contesto strategico più urgente), da 3 dei 7 contesti della DoD con dati reali oggi (Survival, QuestUrgency, Exploration). Indagine mirata su A2+A4 (Q-049): la risposta Survival reale è lavoro AP-05 (`--recover`), il vero gap era l'assenza di un orchestratore — costruito su decisione esplicita dell'utente con ambito delimitato (Q-053, `--autoplay`, dispatch solo Survival/Exploration, tetto rigido di 20 cicli). **A5+A6 completati (Q-056/Q-057)**: 1 difetto reale trovato (footprint di esplorazione non propagato tra cicli), corretto. **AP-08 è ora `Integrated`.** HTN/GOAP restano non affrontati di proposito: dipendono dai gap di esecuzione ancora aperti in AP-07. Vedi `AP-08_STATUS.md`.

AP-09 ha A1+A3 `Present` (Q-035, eccezione dichiarata). Scoperta importante: `src/NosAi.Core/Memory/`+`Knowledge/` esistevano già, reali e testati (Adaptive Knowledge Expansion della DoD in gran parte già coperta) — non rifatti. Colmato solo un gap reale non coperto (Action-outcome ledger + simulazione locale deterministica onesta) ed estesa additivamente `MemoryType`. Segnalata, non risolta, una duplicazione preesistente (`KnowledgeScope`/lifecycle in due namespace diversi, stesso genere del gap `DataSourceKind` di AP-01/A5). **Indagine A2+A4 (Q-058)**: genuinamente bloccato per gap di contratto (nessun produttore di `WorldAction`, `Outcome` senza spazio per Unknown), non per wiring mancante. **Correzione applicata (Q-060)**: `ActionOutcomeLedgerEntry.Outcome` ora `WorldFact<ActionOutcome>`; nuovo `WorldActionProjector` proietta `CombatExecutionEvidence`/`MovementExecutionEvidence` in un `WorldAction` reale. **A2+A4 consegnato e integrato (Q-061/Q-063)**: `ActionOutcomeLedgerStore` (persistenza, stile `MapModelStore`) + wiring in `--engage`/`--recover`/`--scout`/`--autoplay` — solo nei rispettivi `RunWindows`, nessuna firma di `ExecuteOneRound`/`ExecuteOneCycle` toccata. `--collect` escluso di proposito (la sua evidenza prima/dopo non è né `CombatExecutionEvidence` né `MovementExecutionEvidence`). Nessun percorso di lettura wired in questo task. **A5 (Q-063): nessun difetto reale trovato** — seconda consegna DeepSeek pulita di questa sessione dopo `--recover`. **AP-09/A2+A4 è ora `Integrated`.** Vedi `AP-09_A1_STATUS.md`.

AP-10 ha A1 `Present` (Q-036): contratti di certificazione (`VerificationLevel`/`CertificationStage`/`CertificationStageResult`/`CertificationReport`) e il report onesto e citato dei 14 stadi della DoD — nessuno `Verified` per lavoro di questa sessione (i due marcati `Verified` ereditano quella classificazione dal sistema Gate 1-6 preesistente, dichiarato come non riverificato qui). `OverallLevel` complessivo = `Present`. **Aggiornato (Q-059)** dopo `--recover`/`--autoplay`: stadi 8 (Combat)/12 (Recovery) rivisti a `Integrated, parziale`; `OverallLevel` invariato (l'anello debole resta `Present`, sugli stadi 5/6/7/9/10/11); nessun secondo passo A2+A4 per AP-10 stessa — l'orchestratore candidato esiste già come `AutoplayCommand` (AP-08), il blocco reale resta l'assenza di un client Windows reale in questo ambiente. **Verifica dedicata stadio 14 (Q-062)**: `SqliteEventJournal` (hash-chain SHA256) confermato reale e cablato nel vero composition root Gate 1 (`NosAiHost`), non una libreria isolata — test rieseguiti freschi in questa sessione (`SqliteEventJournalTests` 6/6 incluso rilevamento manomissione, `NosAiHostTests` 2/2). Stadio 14: `Integrated`, non `Verified` (nessuna sessione hardware di questa fase ha ri-verificato un journal fisico — `STATO_IMPLEMENTAZIONE.md` stesso non promuove mai questo sottosistema oltre `Present`/`Integrated`). `OverallLevel` invariato. Stadi 1-2 restano non riverificabili in questo ambiente (Real-environment rule). Vedi `AP-10_A1_STATUS.md` per la tabella completa e le citazioni.

**Con Q-036 la sequenza AP-00→AP-10 ha almeno un task A1 (contratti fondanti) completato per ogni fase. AP-04, AP-05, AP-06 e AP-08 hanno A2+A4 consegnati e ora integrati (AP-04: `--scout`; AP-05: `--engage`+`--recover`; AP-06: `--collect`; AP-08: `--autoplay`, il primo orchestratore — tutte e quattro `Integrated`, un difetto reale trovato e corretto in ciascuna consegna DeepSeek tranne `--recover`).** Il lavoro reale che resta non è "quale fase manca" ma i gap dati/esecuzione già segnalati in ogni singolo status doc (OCR/ONNX, dati skill/item reali, fonte portali, riconciliazione `KnowledgeScope`/`DataSourceKind`, validazione contro un client reale per tutto ciò che è ancora `Present`/`Integrated`) — nessuno di questi si chiude scrivendo altro codice di fase, servono dati reali, hardware reale o una decisione esplicita dell'utente. AP-07/A2+A4 (Q-042) è stato indagato e trovato genuinamente bloccato — nessuna specifica DeepSeek prodotta, serve prima nuova infrastruttura (calibrazione UI pannello o un opcode equip identificato). **Q-086 (2026-09-06, DeepSeek, livello `Present`)**: il comando `--calibrate-inventory-panel` (calibrazione screen-space del pannello, 18 `EquipmentSlot`) è consegnato e riallineato all'enum reale in integrazione — riga Q-086 sopra, follow-up operatore in T-12 (`docs/TEST_RIMANDATI.md`).

## Nota ambiente (2026-09-06, Q-086) — suite Runtime: 5 fallimenti non correlati al diff

Su questo host la suite completa `NosAi.Runtime.Tests` riporta 2178/2183: 5 test rossi, **tutti estranei a Q-086** e nessuno tocca i file di Q-085/Q-086:

- `ScreenVitalsCaptureTests.Capture_WithAProcessId_OnANonWindowsHost_FailsClosed_BeforeTouchingClientWindowLocator` e `TryEnsurePipeline_OnANonWindowsHost_FailsClosed_WithDxgisOwnReason`: asseriscono `OperatingSystem.IsWindows() == false` (percorso fail-closed non-Windows), ma l'host è Windows — falliscono per costruzione qui; verdi su CI non-Windows.
- `CommitPointTests.InsideAScopeWithEveryConditionMetTheActReachesTheBackend`: fallito in una sola esecuzione su 3 (flaky; in alcune run rientrava tra i 3 non-Windows e non compariva tra i `[FAIL]`).
- `MapReconstructionSourceTests.Resolve_NoStoreAvailable_StillReconstructsInMemoryForTheSession`: il suo commento assume che il volume `NOSAI-SSD` **non** sia attaccato ("which is not attached on this test host"), ma su questo host `D:` risulta etichettata `NOSAI-SSD`: la costruzione apre davvero il DB sul volume, non logga alcun errore, e `Assert.Single(logger.Errors)` fallisce. Non è una regressione di Q-086 né di Q-085.

Da ri-verificare con il file in un checkout pulito (`c145e9e`/`f94e0be`) quando un operatore lo riterrà utile; il lavoro Q-086 resta `Present` (limite dichiarato nella specifica: serve comunque l'operatore per `Verified`).

## Criterio di avanzamento fase

Una fase (AP-XX) non inizia finché la precedente non è almeno `Integrated` (build+test combinati verdi) — mai `Verified` come precondizione di partenza, dato che `Verified` richiede hardware reale non disponibile in questo ambiente di sviluppo. Questo è il criterio già in uso oggi (AP-00 Q-006 come gate prima di iniziare AP-01 Q-007).

**Eccezione dichiarata (2026-09-05):** i soli contratti A1 di una fase possono partire prima che la fase precedente sia `Integrated`, quando dipendono solo da contratti di una fase ancora più a monte già `Integrated` (non da come la fase precedente li popola a runtime) — questo per permettere a Claude e DeepSeek di lavorare in parallelo invece che in sequenza rigida (istruzione esplicita dell'utente: "programmare tutti insieme e in multi-agente per finire prima"). Esempio concreto: AP-04/A1 dipende da `MapModel`/`Tile`/`TileCoordinate`/`TileTraversability`/`Portal`, stabili da AP-01, non dal lavoro ancora aperto in AP-03/A5-A6. L'implementazione A2/A4 di una fase resta invece gated: non parte finché la fase precedente non è `Integrated`, perché quella sì dipende dal comportamento a runtime, non solo dalla forma dei contratti.

## Chi fa cosa, in breve

**Principio (istruzione esplicita dell'utente, 2026-09-05): Claude gestisce il lavoro — ownership, contratti fondanti, audit, integrazione — ma continua anche lei a scrivere codice reale, su compiti più piccoli; DeepSeek riceve il carico più pesante di sviluppo/compilazione file. Lavorano in parallelo, non in sequenza rigida, per finire prima.** Dettaglio operativo completo in `docs/agents/DEEPSEEK_TASKS.md`.

- **DeepSeek: ATTIVO dal 2026-09-05, prende il ruolo di Cursor INTERAMENTE** — ogni riga "A2 Cursor"/"A4 Cursor" in `docs/agents/AGENT_COMMAND_REGISTRY.md`, per qualunque fase, è ora "A2 DeepSeek"/"A4 DeepSeek" (v1.1). Storicamente in questo progetto A2+A4 sono anche stati il carico di lavoro più pesante per fase (es. AP-03/A4 da solo: 8 file, 1421 righe) — coerente con l'assegnare a DeepSeek il lavoro più pesante. Appena il contratto A1 minimo di cui A2/A4 hanno bisogno esiste, DeepSeek parte subito, in parallelo col resto del lavoro di Claude sulla stessa fase — non in coda dopo. I task già completati da Claude "al posto di Cursor/DeepSeek" prima dell'attivazione (Q-002, Q-004, Q-008, Q-014→Q-018, Q-020, Q-022) restano `DONE`/`Integrated` così come sono — non vengono rifatti. **Vincolo assoluto (istruzione esplicita dell'utente, 2026-09-06, `DEEPSEEK_TASKS.md` "REGOLA ASSOLUTA #2"): DeepSeek scrive solo ed esclusivamente codice/test, compila, testa, e riporta il minimo richiesto — mai documentazione, mai analisi, mai aggiornamenti a questo file o a `DEEPSEEK_TASKS.md`.** Aggiornare questa coda dopo una consegna DeepSeek resta sempre compito di Claude.
- **Claude**: scrive i comandi dettagliati per ogni task; scrive/esegue A1/A3 (contratti/algoritmi, tipicamente i pezzi più piccoli e per primi, per sbloccare DeepSeek il prima possibile) e A5/A6 (audit indipendente, integrazione finale). Continua a programmare in ogni fase, non solo a scrivere specifiche.
- **Cursor**: non più assegnato a nulla di nuovo dalla data sopra. Se torna disponibile, i ruoli possono essere ridistribuiti su richiesta esplicita dell'utente.
