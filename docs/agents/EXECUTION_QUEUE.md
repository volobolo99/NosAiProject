# NosAiProject — Coda di esecuzione multi-agente

**Scopo:** un solo file da cui partire. Ogni voce ha un ID, chi la esegue, da cosa dipende, e dove sta il comando dettagliato. Aggiornato da Claude ad ogni avanzamento — questo file è la fonte di verità su "cosa è il prossimo passo", non `docs/agents/AGENT_COMMAND_REGISTRY.md` (che resta la mappa di ownership per dominio, statica) né i singoli file in `docs/agents/phases/` (che restano i comandi dettagliati per singolo task).

**Come si usa:** guarda la prima voce con stato `PENDING`. Se l'esecutore è `Claude`, dillo a Claude di partire (es. "vai con Q-004"). Se è `DeepSeek`, apri il file comando indicato e passalo a DeepSeek. Quando un task finisce, l'esecutore aggiorna lo stato qui.

**Stati:** `PENDING` (non iniziato) · `IN_PROGRESS` · `DONE` · `BLOCKED` (dipendenza non soddisfatta).

---

## Coda attiva (AP-00 → AP-10)

| ID | Fase | Task | Esecutore | Dipende da | Comando | Stato |
|---|---|---|---|---|---|---|
| Q-001 | AP-00 | A1 — Contratti capacità hardware | Claude | — | `docs/agents/phases/AP-00/A1_CLAUDE_hardware_contracts.md` | **DONE** |
| Q-002 | AP-00 | A2 — Profiling Python hardware | Claude (Cursor non disp.) | — | `docs/agents/phases/AP-00/A2_CURSOR_runtime_profiling.md` | **DONE** |
| Q-003 | AP-00 | A3 — AI budget policy | Claude | — | `docs/agents/phases/AP-00/A3_CLAUDE_ai_budget_policy.md` | **DONE** |
| Q-004 | AP-00 | A4 — Runtime capability gate | Claude (Cursor non disp.) | Q-001 | `docs/agents/phases/AP-00/A4_CURSOR_runtime_gate.md` | **DONE** |
| Q-005 | AP-00 | A5 — Test/benchmark/doc | Claude | Q-001, Q-003, Q-004 | `docs/agents/phases/AP-00/A5_CLAUDE_tests_docs.md` | **DONE** |
| Q-006 | AP-00 | A6 — Integrazione finale (dedup `InferenceTier` Hardware/Scheduling, build/test completo, commit) | **Claude** | Q-005 | `docs/agents/phases/AP-00/A6_CLAUDE_integration_gate.md` | **DONE** |
| Q-007 | AP-01 | A1 — Contratti World Model (Player/Map/Tile/Portal/Mob/NPC/Drop/Quest/Inventory/Equipment/Skill/Buff/Debuff/Cooldown/Resource/Action/Goal) | **Claude** | Q-006 | `docs/agents/phases/AP-01/A1_CLAUDE_world_model_contracts.md` | **DONE** |
| Q-008 | AP-01 | A2 — Sensor Fusion (Network/Memory/Screen/Local → World Model) | Claude (DeepSeek/Cursor non ancora attivi) | Q-007 | `docs/agents/phases/AP-01/A2_DEEPSEEK_sensor_fusion.md` | **DONE** |
| Q-009 | AP-01 | A3 — Temporal belief (decadimento confidence continuo) + derived state (velocità stimata) + prediction (estrapolazione posizione, advisory-only) | **Claude** | Q-007 | `src/NosAi.Core/WorldModel/Temporal/` (nessun comando dedicato scritto: ambito chiarito in conversazione con l'utente, non serviva un file separato) | **DONE** |
| Q-010 | AP-01 | A4 — Runtime wiring (WorldModelFusionLoop, flag `--fuse-world-model`, ModuleReachability → Integrated) | Claude (agente in background) | Q-007, Q-008, Q-009 | (comando dato in linea all'agente, non un file separato — vedi AP-01_STATUS.md §9) | **DONE** |
| Q-011 | AP-01 | A5 — Test/benchmark/doc AP-01 (audit indipendente, 4 difetti reali trovati) | Claude (agente in background) | Q-007, Q-008, Q-009, Q-010 | `docs/agents/phases/AP-01/AP-01_A5_AUDIT.md` | **DONE** |
| Q-012 | AP-01 | A6 — Integrazione finale AP-01 (4 correzioni applicate, build/test combinati verdi) | **Claude** | Q-011 | (vedi AP-01_STATUS.md §11) | **DONE** |
| Q-013 | AP-02 | A1 — Contratto `VisualObservation` (osservazione visiva unificata con provenance) | **Claude** | Q-012 | (vedi AP-02_STATUS.md §3; nessun file comando separato — ricognizione approfondita ha sostituito la stesura preventiva) | **DONE** |
| Q-014 | AP-02 | A2 — Screen/OCR/CV adapters | Claude (Cursor non disp.) | Q-013 | in gran parte già presente da prima (DXGI capture, ROI, ONNX detector contract — vedi AP-02_STATUS.md §1); OCR reale/modello addestrato bloccati da asset ML non producibili in questo ambiente | **BLOCKED** (asset ML mancanti) |
| Q-015 | AP-02 | A3 — Fusione multimodale vitali (HP/MP schermo↔rete via FactFusion) | **Claude** | Q-013 | (vedi AP-02_STATUS.md §4) | **DONE** (ambito ristretto ai vitali; fusione entità Mob/Npc rimandata, stesso blocco ML di Q-014) |
| Q-016 | AP-02 | A4 — Wiring runtime (`ScreenVitalsCapture`: DXGI reale + `ScreenVitalReader` + `NullObjectDetector`, cablato in `WorldModelFusionLoop`/`Program.cs`) | Claude (agente in background) | Q-013, Q-015 | (vedi AP-02_STATUS.md §6) | **DONE** |
| Q-017 | AP-02 | A5 — Audit indipendente AP-02 (2 difetti reali trovati) | Claude (agente in background) | Q-013, Q-015 | `docs/agents/phases/AP-02/AP-02_A5_AUDIT.md` | **DONE** |
| Q-018 | AP-02 | A6 — Integrazione finale AP-02 (2 correzioni applicate, build/test combinati verdi) | **Claude** | Q-017 | (vedi AP-02_STATUS.md §8) | **DONE** |
| Q-019 | AP-03 | A1 — Contratto `MapObservationBatch` (evidenza tile/portali osservazione-side) | **Claude** | Q-018 | `docs/agents/phases/AP-03/AP-03_A1_CLAUDE_map_observation_contract.md` | **DONE** |
| Q-020 | AP-03 | A2 — Projector `MapGrid` (client-archive reale) → `MapObservationBatch`, classificato Cached | **Claude** | Q-019 | (nessun file comando separato, ambito piccolo e meccanico — vedi diff `MapGridObservationProjector.cs`) | **DONE** |
| Q-021 | AP-03 | A3 — Algoritmo di fusione incrementale `MapReconstructionFusion.Merge` (mai perdita di storia, bounds monotoni, idempotente) | **Claude** | Q-019 | (vedi diff `MapReconstructionFusion.cs`) | **DONE** |
| Q-022 | AP-03 | A4 — Persistenza SQLite (`MapModelStore`, WAL/FULL/busy_timeout=5000) + wiring runtime (`MapReconstructionSource` in `WorldModelFusionLoop`/`Program.cs`, dietro `--fuse-world-model`) | Claude (agente in background) | Q-020, Q-021 | `docs/agents/phases/AP-03/AP-03_A4_CLAUDE_persistence_and_wiring.md` | **DONE** |
| Q-023 | AP-03 | A5 — Audit indipendente AP-03 (1 difetto reale trovato: `Resolve` non exception-safe sul percorso di lettura store) | Claude (agente in background) | Q-022 | `docs/agents/phases/AP-03/AP-03_A5_AUDIT.md` | **DONE** |
| Q-024 | AP-03 | A6 — Integrazione finale AP-03 (1 correzione applicata, build/test combinati verdi) | **Claude** | Q-023 | `docs/agents/phases/AP-03/AP-03_STATUS.md` | **DONE** |
| Q-025 | AP-04 | A1 — Contratti esplorazione/navigazione (`ExplorationFootprint`, `FrontierCandidate`, `NavigationWaypoint`, `NavigationPlan`) | **Claude** | Q-024 (eccezione: avviato prima, dipende solo da AP-01) | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` | **DONE** |
| Q-026 | AP-04 | A3 — Algoritmo `ExplorationPlanner` (footprint, ranking frontiera, `NavigationPlan` a singolo waypoint, stessa mappa) | **Claude** | Q-025 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"A3 + contratto A1 mancante" | **DONE** |
| Q-027 | AP-04 | A1 mancante — Contratto `MovementExecutionEvidence`/`MovementExecutionResult` (evidenza esecuzione movimento, WorldFact-based) | **Claude** | Q-025 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"A3 + contratto A1 mancante" | **DONE** |
| Q-028 | AP-04 | A2 — `MovementVerificationProjector` (`MovementVerification` reale → `MovementExecutionEvidence`) | DeepSeek | Q-027 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_scout_command.md` | **DONE** |
| Q-029 | AP-04 | A4 — Comando operatore `--scout` (`ScoutCommand`, chiama `WalkCommand.Execute` con `ActuationAuthority.Commanded`) | DeepSeek | Q-026, Q-028 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_scout_command.md` | **DONE** (livello `Present`: build+test verdi; non ancora `Verified`: nessun client reale) |
| Q-030 | AP-05 | A1 — Contratti Combat Intelligence (`CombatActionCandidate`, `CombatConstraintCheck`, `CombatSimulationResult`, `ComboStep`/`ComboPlan`) | **Claude** | Q-018 (eccezione: dipende solo da AP-01) | `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` | **DONE** |
| Q-031 | AP-05 | A3 (parziale) — `CombatPlanner.GenerateCandidates`/`CheckHardConstraints` da dati reali; simulazione/combo rimandati (nessun dato reale di danno/costo skill in AP-01) | **Claude** | Q-030 | `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` §"A3 (parziale)" | **DONE** |
| Q-032 | AP-06 | A1 — Contratti Quest Graph (`QuestObjectiveKind`/`QuestObjectiveTarget`, `EnrichedQuestObjective`, `QuestReward`, `QuestNode`/`QuestGraph`) + A3 (`QuestGraphPlanner`: avviabilità quest, obiettivi soddisfatti) | **Claude** | Q-018 (eccezione: dipende solo da AP-01) | `docs/agents/phases/AP-06/AP-06_A1_STATUS.md` | **DONE** |
| Q-033 | AP-07 | A1 — Contratti Loadout (`LoadoutActionKind`/`LoadoutActionCandidate`, `LoadoutConstraintCheck`, `LoadoutEvaluation`) + A3 (parziale) — `LoadoutPlanner`: Unequip/Upgrade reali, hard constraints per tutti e tre i `Kind`, `CountActiveQuestNeedsFor` incrociato con AP-06 | **Claude** | Q-018, Q-032 (eccezione: dipende solo da AP-01/AP-06) | `docs/agents/phases/AP-07/AP-07_A1_STATUS.md` | **DONE** |
| Q-034 | AP-08 | A1 — Contratti Strategy (`StrategicGoalKind`, `EnrichedGoal`, `StrategicSignal`, `StrategicPlan`) + A3 (parziale) — `StrategyPlanner`: urgenza Survival/QuestUrgency/Exploration reale, selezione deterministica; Recovery/Progression/Farming/Optimization deliberatamente non assessati (dati mancanti) | **Claude** | Q-018, Q-025, Q-032 (eccezione: dipende solo da AP-01/AP-04/AP-06) | `docs/agents/phases/AP-08/AP-08_A1_STATUS.md` | **DONE** |
| Q-035 | AP-09 | A1 — Scoperta infrastruttura Knowledge/Memory già reale e testata (non rifatta); estensione additiva `MemoryType` (5 categorie mancanti) + A3 — `ActionOutcomeLedgerEntry`/`LocalOutcomeSimulator` (previsione empirica deterministica da storico reale) | **Claude** | Q-018 (eccezione: dipende solo da AP-01) | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` | **DONE** |
| Q-036 | AP-10 | A1 — Contratti Certification (`VerificationLevel`, `CertificationStage`, `CertificationStageResult`, `CertificationReport`) + report onesto e citato dei 14 stadi della DoD: nessuno stadio raggiunge `Verified` per lavoro di questa sessione, `OverallLevel` = `Present` | **Claude** | Tutte le fasi precedenti (report di sintesi, non costruzione) | `docs/agents/phases/AP-10/AP-10_A1_STATUS.md` | **DONE** |
| Q-037 | AP-05 | A1 mancante — Decisione percorso (a) verifica combattimento solo-vitali-player + contratto `CombatExecutionEvidence`/`CombatExecutionResult` | **Claude** | Q-031 | `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` §"Decisione presa: percorso (a)" | **DONE** |
| Q-038 | AP-05 | A2 — `CombatVerificationProjector` (`PlayerVitalsReading` prima/dopo reale → `CombatExecutionEvidence`) | DeepSeek | Q-037 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_engage_command.md` | **DONE** |
| Q-039 | AP-05 | A4 — Comando operatore `--engage <targetEntityId> <skillId>` (`EngageCommand`, esecuzione via `KeybindMap`+`GatedInputBackend.KeyPress`, verifica via `ClientMemorySession.TryReadPlayerVitals`, `ActuationAuthority.Commanded`) | DeepSeek | Q-031, Q-038 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_engage_command.md` | **DONE** (livello `Present`: build+test verdi; non ancora `Verified`: nessun client reale) |
| Q-040 | AP-06 | A2 — `QuestGraphPlanner.AssessCollectProgress` (conteggio osservato per obiettivi `Collect`, da `Player.Inventory` già fuso in AP-01/A2 `Integrated`; nessun lavoro DeepSeek necessario, dati già reali) | **Claude** | Q-032 | `docs/agents/phases/AP-06/AP-06_A1_STATUS.md` §"AP-06/A2+A4" | **DONE** |
| Q-041 | AP-06 | A4 — Comando operatore `--collect <x> <y> <vnum> [<requiredCount>]` (`CollectCommand`, cammina via `WalkCommand.Execute`, verifica via `LiveObservationGateway.Capture()`+`GameplayObservationProjector`+`AssessCollectProgress`, `ActuationAuthority.Commanded`) | DeepSeek | Q-025, Q-040 | `docs/agents/phases/AP-06/AP-06_A2A4_DEEPSEEK_collect_command.md` | **DONE** (livello `Present`: build+test verdi, 5 test nuovi; non ancora `Verified`: serve un client reale + WinDivert elevato) |
| Q-042 | AP-07 | A2+A4 — Indagine mirata: genuinamente bloccato (nessuna primitiva di esecuzione equip/unequip/upgrade in nessuno dei due sistemi, nessun canale di verifica rete per lo stato equipaggiato, nessuna calibrazione UI pannello inventario). Non specificabile per DeepSeek finché non esiste una calibrazione screen-space del pannello o un opcode di rete identificato per l'equip. | **Claude** | Q-033 | `docs/agents/phases/AP-07/AP-07_A1_STATUS.md` §"AP-07/A2+A4" | **DONE** (indagine, non un blocco chiuso) |
| Q-043 | AP-04 | A5 — Audit indipendente `--scout` (1 difetto reale trovato: `onStepVerified` invocato anche per un passo mai emesso, origine nella specifica) | Claude (agente in background) | Q-028, Q-029 | `docs/agents/phases/AP-04/AP-04_A5_AUDIT.md` | **DONE** |
| Q-044 | AP-04 | A6 — Integrazione finale AP-04 (1 correzione applicata: `onStepVerified` spostato dentro `report.Emitted`, test di regressione aggiunto, build/test combinati verdi) | **Claude** | Q-043 | `docs/agents/phases/AP-04/AP-04_STATUS.md` | **DONE** |
| Q-045 | AP-05 | A5 — Audit indipendente `--engage` (1 difetto reale trovato: `Run` lanciava un'eccezione non gestita su un argomento vuoto invece di `[REFUSED]`) | Claude (agente in background) | Q-038, Q-039 | `docs/agents/phases/AP-05/AP-05_A5_AUDIT.md` | **DONE** |
| Q-046 | AP-05 | A6 — Integrazione finale AP-05 (1 correzione applicata: convalida argomenti di `Run` sostituita con `[REFUSED]` pulito, 3 test di regressione aggiunti, build/test combinati verdi) | **Claude** | Q-045 | `docs/agents/phases/AP-05/AP-05_STATUS.md` | **DONE** |
| Q-047 | AP-06 | A5 — Audit indipendente `--collect` (1 difetto reale trovato, stesso pattern di AP-05: `Run` lanciava un'eccezione non gestita su `vnum` vuoto invece di `[REFUSED]`) | **Claude** | Q-040, Q-041 | `docs/agents/phases/AP-06/AP-06_A5_AUDIT.md` | **DONE** |
| Q-048 | AP-06 | A6 — Integrazione finale AP-06 (1 correzione applicata: stesso schema `[REFUSED]` di AP-05, 2 test di regressione aggiunti, build/test combinati verdi) | **Claude** | Q-047 | `docs/agents/phases/AP-06/AP-06_STATUS.md` | **DONE** |
| Q-049 | AP-08 | A2+A4 — Indagine mirata: risposta Survival reale trovata (`UseConsumable` via keybind slot, stesso meccanismo di `--engage` — lavoro AP-05, non nuova infrastruttura AP-08); gap vero è l'assenza di un orchestratore che scelga un'azione da `StrategyPlanner.SelectStrategicPlan` — prima componente che decide invece di eseguire un atto nominato dall'operatore. Non specificato senza decisione esplicita dell'utente su ambito e cautele. | **Claude** | Q-034 | `docs/agents/phases/AP-08/AP-08_A1_STATUS.md` §"AP-08/A2+A4" | **DONE** (indagine, decisione in sospeso) |
| Q-050 | AP-05 | A1 addendum — `CombatExecutionResult.ResourceGainConfirmed` (controparte di recupero di `ResourceCostConfirmed`, per la risposta Survival) | **Claude** | Q-037 | `src/NosAi.Core/WorldModel/Combat/CombatExecutionContracts.cs` | **DONE** |
| Q-051 | AP-05 | A2 — `CombatVerificationProjector.ProjectRecovery` (Health prima/dopo per `UseConsumable`, direzione asserita dall'operatore) | DeepSeek | Q-050 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_recover_command.md` | **DONE** |
| Q-052 | AP-05 | A4 — Comando operatore `--recover <slot>` (`RecoverCommand`, stesso schema di `EngageCommand`, keybind `consumable.{slot}`) | DeepSeek | Q-051 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_recover_command.md` | **DONE** |
| Q-053 | AP-08 | A2+A4 — Comando operatore `--autoplay [--cycles <n>] [--recover-slot <slot>]` (`AutoplayCommand`: orchestratore minimo, dispatch solo Survival→`RecoverCommand`/Exploration→`ScoutCommand`, tetto rigido di cicli, nessun bypass di Guard/Safety) — approvato esplicitamente dall'utente con ambito delimitato | DeepSeek | Q-034, Q-052 | `docs/agents/phases/AP-08/AP-08_A2A4_DEEPSEEK_autoplay_command.md` | **DONE** |
| Q-054 | AP-05 | A5 — Audit indipendente `--recover` (nessun difetto trovato) | **Claude** | Q-051, Q-052 | `docs/agents/phases/AP-05/AP-05_A5_AUDIT.md` §7 | **DONE** |
| Q-055 | AP-05 | A6 — Integrazione finale addendum `--recover` (nessuna correzione necessaria) | **Claude** | Q-054 | `docs/agents/phases/AP-05/AP-05_STATUS.md` §8bis | **DONE** |
| Q-056 | AP-08 | A5 — Audit indipendente `--autoplay` (1 difetto reale trovato: footprint di esplorazione non propagato tra cicli) | **Claude** | Q-053 | `docs/agents/phases/AP-08/AP-08_A5_AUDIT.md` | **DONE** |
| Q-057 | AP-08 | A6 — Integrazione finale AP-08 (1 correzione applicata: `AutoplayCycleResult.UpdatedFootprint`, 2 test di regressione, build/test combinati verdi) | **Claude** | Q-056 | `docs/agents/phases/AP-08/AP-08_STATUS.md` | **DONE** |
| Q-058 | AP-09 | Indagine mirata A2+A4 (ledger): trovato genuinamente bloccato per gap di contratto, non wiring — nessun produttore di `WorldAction`, `ActionOutcomeLedgerEntry.Outcome` senza spazio per Unknown | **Claude** | Q-035 | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` §"AP-09/A2+A4 — indagine mirata" | **DONE** |
| Q-059 | AP-10 | Aggiornamento report certificazione dopo `--recover`/`--autoplay`: stadi 8/12 `Present`→`Integrated, parziale`; `OverallLevel` invariato (`Present`); nessun secondo passo A2+A4 per AP-10 (orchestratore candidato già consegnato da AP-08, bloccato solo dalla Real-environment rule) | **Claude** | Q-036, Q-057 | `docs/agents/phases/AP-10/AP-10_A1_STATUS.md` §"Aggiornamento" | **DONE** |
| Q-060 | AP-09 | Correzione di contratto: `ActionOutcomeLedgerEntry.Outcome` wrappato in `WorldFact<ActionOutcome>` + `WorldActionProjector` (nuovo, produttore reale di `WorldAction` da `CombatExecutionEvidence`/`MovementExecutionEvidence`) — sblocca la scrivibilità di un futuro A2+A4, non ancora specificato | **Claude** | Q-058 | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` §"Correzione di contratto" | **DONE** |
| Q-061 | AP-09 | A2+A4 — persistenza (`ActionOutcomeLedgerStore`, stile `MapModelStore`) + wiring in `--engage`/`--recover`/`--scout`/`--autoplay` (solo `RunWindows`, nessuna firma di `ExecuteOneRound`/`ExecuteOneCycle` toccata); `--collect` esplicitamente escluso (forma evidenza incompatibile) | **DeepSeek** | Q-060 | `docs/agents/phases/AP-09/AP-09_A2A4_DEEPSEEK_ledger_wiring.md` | **DONE** |
| Q-063 | AP-09 | A5+A6 — Audit indipendente + integrazione ledger wiring (Q-061): **nessun difetto reale trovato** (seconda consegna DeepSeek pulita di questa sessione, dopo `--recover`), una nota minore non bloccante su `RecordedAtUtc` | **Claude** | Q-061 | `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` §"A2+A4 (Q-061, DeepSeek)" | **DONE** |
| Q-062 | AP-10 | Verifica dedicata stadio 14 (Evidence): `SqliteEventJournal`/hash-chain confermato reale e cablato nel vero `NosAiHost` (Gate 1), test rieseguiti freschi (`SqliteEventJournalTests` 6/6, `NosAiHostTests` 2/2). Stadi 8/13/14 aggiornati; `OverallLevel` invariato (`Present`) | **Claude** | Q-059 | `docs/agents/phases/AP-10/AP-10_A1_STATUS.md` §"Aggiornamento 2" | **DONE** |
| Q-064 | Trasversale | Indagine riconciliazione duplicati (su richiesta esplicita dell'utente): `KnowledgeScope` (Memory/Knowledge) unificabile in sicurezza; `KnowledgeStatus`/`KnowledgeLifecycle` **non** fondibili (concetti distinti) ma con un bug reale nella proiezione ad-hoc; `DataSourceKind` triplicato confermato **duplicazione intenzionale** per bounded context (vincolo `NosAi.Core` zero-dipendenze) | **Claude** (2 agenti in background) | — | vedi Q-065/Q-066 | **DONE** |
| Q-065 | Trasversale | Riconciliazione `KnowledgeScope`: dichiarazione in `NosAi.Core.Knowledge` rimossa, unificata su `Memory.KnowledgeScope`; entrambi i `MapScope` rimossi. Nuovo `KnowledgeLifecycleProjection.ToKnowledgeStatus` (totale, esplicito) sostituisce lo switch ad-hoc che collassava silenziosamente `Deprecated`/`Forbidden`/`RevalidationRequired` su `Candidate` — bug reale trovato dall'indagine, corretto | **Claude** | Q-064 | `src/NosAi.Core/Knowledge/KnowledgeLifecycleProjection.cs`, `docs/agents/phases/AP-09/AP-09_A1_STATUS.md` | **DONE** |
| Q-066 | Trasversale | `DataSourceKind` triplicato: **non unificato**, chiuso con ADR invece che con codice — confermato duplicazione intenzionale (bounded context, vincolo reale `NosAi.Core.csproj` zero-dipendenze), due bridge di conversione già esistenti e corretti nei due punti dove i domini si incontrano | **Claude** | Q-064 | `docs/adr/ADR-0026-datasourcekind-intentional-bounded-context-duplication.md` | **DONE** |
| Q-067 | AP-02 | A2+A4 — cablare `TargetStateComposer` in `ScreenVitalsCapture`: gap dichiarato non bloccato da OCR/ONNX (`TargetRoiCalibration`/`ScreenTargetFrameSource`/`TargetStateComposer` già reali, già usati da `TargetAwareGameplayProvider` lato wire). Nuovo `SingleFrameSource` preserva l'invarianza "un frame, più lettori"; `wire: null` esplicito (nessun `IPlayerAttackObserver` disponibile al punto di composizione) | **DeepSeek** | — | `docs/agents/phases/AP-02/AP-02_A2A4_DEEPSEEK_target_state_wiring.md` | **DONE** |
| Q-068 | AP-02 | A5 — Audit indipendente wiring `TargetStateComposer` (Q-067): **nessun difetto trovato** (terza consegna DeepSeek pulita di questa sessione, dopo `--recover` e il ledger AP-09) | **Claude** | Q-067 | `docs/agents/phases/AP-02/AP-02_STATUS.md` §11 | **DONE** |
| Q-069 | Trasversale | Indagine (in parallelo a Q-067, su richiesta esplicita dell'utente): statistiche reali skill/item — **genuinamente bloccato** (`GameReferenceDatabase` decodifica solo byte-esatta, nessun campo con significato semantico dichiarato); fonte dati portali — **percorso reale trovato** ("osserva mentre attraversi" da `ClientMemorySession`, non parsing file client) | **Claude** (2 agenti in background) | — | vedi Q-070 | **DONE** |
| Q-070 | AP-04 | A1+A3 — `PortalCrossingDetector` (nuovo, puro): deriva un `Portal` reale da due letture consecutive mappa+posizione quando la mappa cambia; `Portal.Id` da mappa sorgente + posizione arrotondata (stabile per `MapReconstructionFusion.MergeByKey`). Si integra nel campo `MapObservationBatch.Portals` già esistente (AP-03), nessun nuovo meccanismo di persistenza. A2+A4 (wiring del polling) non specificato in questa sessione | **Claude** | Q-069 | `src/NosAi.Core/WorldModel/Reconstruction/PortalCrossingDetector.cs`, `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Seconda indagine sui portali" | **DONE** |
| Q-071 | AP-04 | A2+A4 — wiring `PortalCrossingDetector`: nuovo `MapReconstructionSource.RecordPortalCrossing` (merge indipendente dal `Resolve` del ciclo corrente, mai tocca la cache di una mappa diversa) + hook di polling in `ScoutCommand`/`AutoplayCommand.RunWindows` (riusa il polling mappa/posizione già esistente, nessuna nuova infrastruttura). `--collect`/`--engage`/`--recover` esplicitamente esclusi (non pollano map id per round) | **DeepSeek** | Q-070 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_portal_crossing_wiring.md` | **DONE** |
| Q-072 | AP-04 | A5 — Audit indipendente wiring `RecordPortalCrossing` (Q-071): **nessun difetto trovato** (quarta consegna DeepSeek pulita di questa sessione) | **Claude** | Q-071 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Audit A5 (Claude) — Q-071" | **DONE** |
| Q-073 | AP-02 | A1+A3 — Rilevamento presenza/assenza finestra di dialogo (nuovo, puro): `DialogRoiCalibration` (ROI + baseline colore confermata da operatore), `DialogWindowReader` (delta euclideo media B/G/R vs baseline, soglia `DefaultPresentThreshold=24.0` dichiarata scelta di giudizio), `DialogWindowStateComposer` (nessuna riconciliazione wire: nessun opcode dialogo/quest text esiste), `ScreenDialogWindowSource` (stesso ordine di guardie di `ScreenTargetFrameSource`). Su richiesta esplicita dell'utente, nessun client reale per verificare la tecnica | **Claude** | — | `docs/agents/phases/AP-02/AP-02_STATUS.md` §12 | **DONE** |
| Q-074 | AP-02 | A1 — Contratto `VisualObservation` esteso con `HasDialogWindow` (stesso schema di `HasTarget`); `ScreenVitalsCapture.Capture()` lo popola con placeholder onesto `Unknown(dialog_window_composer_not_wired_in_this_pass)`, stesso schema che `HasTarget` portava prima di Q-067 | **Claude** | Q-073 | `docs/agents/phases/AP-02/AP-02_STATUS.md` §12 | **DONE** |
| Q-075 | AP-02 | A2+A4 — wiring reale del rilevamento dialog-window in `ScreenVitalsCapture`/`Program.cs`: nuovo `ScreenDialogWindowSource` sul frame già acquisito (`SingleFrameSource`), `DialogWindowStateComposer.Compose` senza argomento wire (nessun opcode dialogo/quest text), caricamento `DialogRoiCalibration` in `Program.cs` identico a `TargetRoiCalibration`. Specifica mirror esatto di Q-067 | **DeepSeek** | Q-074 | `docs/agents/phases/AP-02/AP-02_A2A4_DEEPSEEK_dialog_window_wiring.md` | **DONE** |
| Q-076 | AP-02 | A5 — Audit indipendente wiring dialog-window (Q-075): **nessun difetto trovato** (quinta consegna DeepSeek pulita consecutiva di questa sessione) | **Claude** | Q-075 | `docs/agents/phases/AP-02/AP-02_STATUS.md` §13 | **DONE** |
| Q-077 | AP-04 | A3 — `MultiMapRoutePlanner` (nuovo, puro): BFS su `Portal` osservati (`Portal.SourceMap`→`DestinationMap`), mai bidirezionale per assunzione, mai un arco sintetizzato su un `IsActive` non confermato. Popola `NavigationPlan`/`NavigationWaypoint` (contratto AP-04/A1 già multi-mappa-capace) da dati reali invece di restare vincolato a una sola mappa | **Claude** | Q-072 | `src/NosAi.Core/WorldModel/Exploration/MultiMapRoutePlanner.cs`, `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Terza indagine" | **DONE** |
| Q-078 | AP-04 | A2+A4 — enumerazione mappe (`MapModelStore.ListMapIds`, `MapReconstructionSource.LoadAllKnownMaps`, entrambi best-effort) + nuovo comando diagnostico read-only `--route <mapId> <x> <y>` (`RouteProbe`, stesso schema di `TargetChainProbe`: pianifica e stampa, non cammina) | **DeepSeek** | Q-077 | `docs/agents/phases/AP-04/AP-04_A2A4_DEEPSEEK_route_command.md` | **DONE** |
| Q-079 | AP-04 | A5 — Audit indipendente `--route`/enumerazione mappe (Q-078): **nessun difetto trovato** (sesta consegna DeepSeek pulita consecutiva di questa sessione) | **Claude** | Q-078 | `docs/agents/phases/AP-04/AP-04_A1_STATUS.md` §"Audit A5 (Claude) — Q-078" | **DONE** |
| Q-080 | Trasversale | A1+A3 — `SkillReferenceDecoder` (nuovo, puro): promuove a colonne tipizzate `COST`/`LEVEL`/`TARGET` e i soli campi `DATA` risolti con sicurezza (`CastTime`/`Cooldown`/`MpCost`) del record skill, più i riferimenti `BASIC`→`BCardApplication` (struttura, non interpretazione). Campi codificati senza mappatura verificata (elemento, tipo attacco, ...) restano onestamente non decodificati, mai indovinati. Su richiesta esplicita dell'utente, nessuna fonte esterna citata in codice/documenti | **Claude** | — | `src/NosAi.Runtime/GameData/SkillReferenceDecoder.cs` | **DONE** |
| Q-081 | Trasversale | A1+A3 — `SkillReferenceDecoder` esteso: `TYPE` (SkillType/CastId/JobClass/AttackType/SecondaryWeapon/Element) e `DATA` per intero (UpgradeSkill/PartnerSkillId/DashSpeed/RequiredItemVnum/DataRange/DataTargetRange, oltre a CastTime/Cooldown/MpCost già presenti). Mappatura da https://nt-research.github.io/ (documentazione community dei formati client) — istruzione esplicita dell'utente: citare la fonte questa volta, in codice e qui. Resta provvisorio finché non incrociato con un valore reale del client (nessun confronto ancora eseguito) | **Claude** | Q-080 | `src/NosAi.Runtime/GameData/SkillReferenceDecoder.cs`, `tests/NosAi.Runtime.Tests/GameData/SkillReferenceDecoderTests.cs` | **DONE** |
| Q-082 | Trasversale | A1+A3+A6 — Rilevamento aggiornamenti client come funzionalità di base: nuova tabella `client_inventory` in `GameReferenceDatabase` con diff added/changed/removed; nuovo comando operatore `--client-updates` (`ClientUpdateCommand`) che rilancia sia `ReferenceImporter.ImportAll` sia una scansione dell'intera directory client e riporta cosa è cambiato. Valutata prima un'integrazione di `taletool` (github.com/imxeno/taletool, AGPL-3.0-or-later) come processo esterno vendorizzato; sostituita su istruzione esplicita dell'utente con `ClientDirectoryScanner` (nuovo, puro): classifica ogni file riusando `NosArchive`, il lettore nativo già in uso da `ReferenceImporter` — nessun processo esterno, nessuna dipendenza AGPL | **Claude** | — | `src/NosAi.Runtime/GameData/ClientDirectoryScanner.cs`, `src/NosAi.Runtime/GameData/GameReferenceDatabase.cs`, `src/NosAi.Runtime/Observability/ClientUpdateCommand.cs`, `src/NosAi.Runtime/Program.cs`, `third_party/sources/taletool/UPSTREAM.md` | **DONE** |
| Q-083 | Trasversale | A3+A6 — Rilevamento aggiornamenti client eseguito automaticamente a ogni avvio reale di `NosAi.Runtime` (non solo su richiesta esplicita con `--client-updates`): `ClientUpdateCommand.RunDetailed` (nuovo, aggiunge `ClientUpdateReport.AnyChange` accanto al testo già esistente) invocato in `Program.Main` subito dopo l'avvio di Gate 1. Silenzioso quando nulla è cambiato; stampa il report solo quando rileva una differenza reale. Verificato sul client reale dall'utente: prima esecuzione con SSD `NOSAI-SSD` vuoto → tutto `+N` (2705 monster, 7727 item, 1958 skill, 2763 card, 131 bcard, 314 file); seconda esecuzione immediata, nessuna modifica al client → tutto `=N`, conferma che la persistenza/diff funziona | **Claude** | Q-082 | `src/NosAi.Runtime/Observability/ClientUpdateCommand.cs`, `src/NosAi.Runtime/Program.cs`, `tests/NosAi.Runtime.Tests/ClientUpdateCommandTests.cs` | **DONE — Verified (evidenza utente sul client reale)** |
| Q-084 | AP-07 | A1 — Seconda indagine mirata AP-07/A2+A4 (equip/inventario), re-verifica dei tre blocchi dichiarati: primitiva di esecuzione (confermato assente, non prioritario), calibrazione screen-space del pannello (confermato assente, **consegnato** `InventoryPanelRoiCalibration` — ritagli, uno per `EquipmentSlot`, stesso schema di `DialogRoiCalibration`/`TargetRoiCalibration`, tutti insieme o nessuno), canale di verifica (**parzialmente confutato** — `InventoryKind` ora documentato da fonte esterna citata OpenNos/GPL, un valore incrociato con una cattura reale di questo repository `Etc=2`, `Wear=8` candidato per "equipaggiato" ma non ancora incrociato). Specifica DeepSeek per la sola calibrazione (`--calibrate-inventory-panel`), non ancora esecuzione | **Claude** | Q-035 | `src/NosAi.Runtime/Perception/InventoryPanelRoiCalibration.cs`, `src/NosAi.Runtime/Perception/Network/GameTrafficObserver.cs`, `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs`, `docs/agents/phases/AP-07/AP-07_A1_STATUS.md`, `docs/agents/phases/AP-07/AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md` | **DONE** |
| Q-085 | Trasversale | A1 — Analisi approfondita di `itempicker.atlagaming.eu` (istruzione esplicita dell'utente) ha trovato un difetto reale in Q-084: `EquipmentSlot` (`InventoryContracts.cs`) portava un segnaposto sbagliato a 8 valori (`Weapon, Shield, Helmet, Armor, Gloves, Boots, Accessory1, Accessory2`) invece dei 18 valori reali di NosTale. Corretto a `Weapon, Armor, Hat, Gloves, Boots, SecondaryWeapon, Necklace, Ring, Bracelet, Mask, Fairy, Amulet, SpecialistCard, CostumeSuit, CostumeHat, WeaponSkin, CostumeWings, MiniPet`, derivato dal campo `equipmentSlot` di `items.json` incrociato con nomi item italiani reali; due file di test che referenziavano `.Helmet` aggiornati a `.Hat` (stesso slot, nome corretto). Stesso cross-check ha anche verificato campo-per-campo (skill vnum 201) il layout tag di `SkillReferenceDecoder` per 8 tag — doc-comment aggiornato da "provvisorio" a "posizione confermata per questi 8, il resto resta provvisorio". Corretto anche `AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md`, che citava ancora "otto slot"/l'enum sbagliato (v1, scritto prima di questa scoperta). **Nota per l'integrazione**: l'utente ha avviato DeepSeek su Q-084 con la versione non corretta dello spec — l'output di DeepSeek per `--calibrate-inventory-panel` va verificato in A5/A6 per riferimenti a nomi/enum rimossi o al conteggio "8" invece di "18" token | **Claude** | Q-084 | `src/NosAi.Core/WorldModel/InventoryContracts.cs`, `src/NosAi.Runtime/GameData/SkillReferenceDecoder.cs`, `tests/NosAi.Core.Tests/WorldModel/Loadout/LoadoutContractsTests.cs`, `tests/NosAi.Core.Tests/WorldModel/Loadout/LoadoutPlannerTests.cs`, `docs/agents/phases/AP-07/AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md`, `docs/research/NOSTALE_COMMUNITY_KNOWLEDGE_2026-09-05.md` | **DONE** |
| Q-086 | AP-07 | A2+A4 — Comando operatore `--calibrate-inventory-panel` (specifica `AP-07_A2A4_DEEPSEEK_inventory_panel_calibration.md`): `InventoryPanelCalibrationProbe.TryParseSlots` (un token `<EquipmentSlot>:<x>,<y>,<w>,<h>` per ogni valore dichiarato dell'enum, conta derivata da `Enum.GetValues`, mai hardcoded), `Run` console che rispecchia `HudProbe` (attach client → frame DXGI → area client → preview `inventory_panel_latest.bmp` via nuovo `HudCropWriter.TrySavePanelPreview` additivo → `Confirmed`+`Save` solo su conferma operatore, `[REFUSED]` senza file parziale), wiring in `Program.cs` + `KnownProbeFlags`. Zero token = solo report stato. Testata la parità col client reale della preview via smoke run sul desktop di sviluppo: preview BMP valida 1920x1200 scritta e gitignored, nessuna calibrazione scritta senza conferma (report-only non tocca il file). **Correzione in integrazione (A5/A6, nota Q-085)**: la prima stesura era contro la specifica v1 (8 slot pre-Q-085); riallineata all'enum a 18 valori reale — guida operatore e rifiuti ora stampano i nomi/conteggi dall'enum, e tutti i test sono scritti su `Enum.GetValues` così nessuna assunzione sul numero di slot può rientrare. Build 0 errori/0 warning (1 warning xUnit preesistente non collegato), Runtime full 2178/2183 (5 fallimenti non correlati e dipendenti dall'ambiente host — vedi nota in fondo alla coda), Core 643/643, filtri calibrazione tutti verdi | **DeepSeek** | Q-085 | `src/NosAi.Runtime/Perception/InventoryPanelCalibrationProbe.cs`, `src/NosAi.Runtime/Perception/HudCropWriter.cs`, `src/NosAi.Runtime/Program.cs`, `tests/NosAi.Runtime.Tests/InventoryPanelCalibrationProbeTests.cs` | **DONE — Present** |
| Q-087 | Trasversale | A1 — Analisi approfondita di `monsters.json`/`maps.json` (istruzione esplicita dell'utente). Trovata documentazione reale del formato `monster.dat` (`nt-research.github.io`, stesso sito già citato per Skill.dat — quella pagina cita a sua volta Itempicker e NosApki come risorse collegate). Nuovo `MonsterReferenceDecoder` (mirror esatto dello stile di `SkillReferenceDecoder`): decodifica 21 dei 23 tag documentati, drop table (`ITEM`×20 → `MonsterDrop.ItemVnum/Chance/Amount`) corroborata da `monsters.json` (stessi 3 campi, stesso ordine, `chance` coerente con la scala grezza documentata); `PARTNER` (sempre zero, nessun significato noto) e `MODE` (struttura ambigua nella sola documentazione, nessun file client reale da ispezionare) omessi di proposito con motivazione esplicita, non indovinati. **Non cross-checked contro un client reale** (nessun client in questo sandbox) — dichiarato provvisorio, stesso standard di `SkillReferenceDecoder` prima della sua verifica. `maps.json` (dimensioni mappa): nessun gap trovato — `MapGrid.Width`/`Height` derivano già dai file `.NSTC` reali del client (fonte di prima mano più autorevole); `MapModel` (World Model) non porta un campo dimensione fisso (confini osservati incrementalmente) — nessuna integrazione fatta, sarebbe dato ridondante | **Claude** | Q-085 | `src/NosAi.Runtime/GameData/MonsterReferenceDecoder.cs`, `tests/NosAi.Runtime.Tests/GameData/MonsterReferenceDecoderTests.cs`, `docs/research/NOSTALE_COMMUNITY_KNOWLEDGE_2026-09-05.md` | **DONE — Present (non cross-checked contro client reale)** |
| Q-088 | Trasversale | A1 — Terzo decoder della stessa famiglia (dopo Skill/Monster): `ItemReferenceDecoder` per `Item.dat` (fonte `nt-research.github.io`, che a sua volta cita `NosCore.Parser/Parsers/ItemParser.cs`, `OpenNos.Import.Console/ImportFactory.cs` e `itempicker.atlagaming.eu`). Decodifica VNUM/Price, INDEX (InventoryType/ItemType/ItemSubType/Slot/IconId/VisualChangeId), TYPE (AttackType/RequiredClass), i 23 FLAG nominati, i 20 DATA grezzi (il formato dichiara esplicitamente che il significato dipende dal tipo item — mai interpretati), BUFF (5 BCard) e LINEDESC. **Scoperta di rilievo**: la pagina Item.dat documenta il proprio campo `EquipmentSlot` citando `OpenNos.Domain/EquipmentType.cs` con gli stessi identici 18 valori nello stesso ordine di `EquipmentSlot` (Q-085) — seconda conferma indipendente dello stesso schema, oltre a quella già fatta via nomi item italiani; per questo `ItemReference.Slot` è l'unico campo tipizzato (sugli altri resta int grezzo, stesso standard di cautela di Skill/Monster). Non cross-checked contro un client reale, stesso limite dichiarato per `MonsterReferenceDecoder` | **Claude** | Q-087 | `src/NosAi.Runtime/GameData/ItemReferenceDecoder.cs`, `tests/NosAi.Runtime.Tests/GameData/ItemReferenceDecoderTests.cs`, `src/NosAi.Core/WorldModel/InventoryContracts.cs` | **DONE — Present (non cross-checked contro client reale)** |
| Q-089 | AP-07 | T-12 — Prima verifica reale su client vivo di `--calibrate-inventory-panel` (Q-086). L'operatore ha aperto il pannello "Info Personaggio" (finestra `TNosTaleMainF`, area client 1024x768), confermato la griglia 3×6 dei 18 `EquipmentSlot` (nessuna fonte online ha mai documentato l'esatta disposizione a schermo — Fandom/wiki italiana/gameguide ufficiale irraggiungibili, elitepvpers blocca i bot; usata una guida Steam Community reale come lead per l'ordine di lettura, incrociata con 2 icone riconoscibili nella schermata dell'operatore — pugnali incrociati per `Weapon`, ritratto per `SpecialistCard` — poi confermata per intero dall'operatore stesso) ed eseguito il comando con tutti e 18 i token: `InventoryPanelRoiCalibration` scritta con successo (nessun `[REFUSED]`). **Verified** per la sola calibrazione screen-space (evidenza reale, non simulata); resta `Present` il resto di AP-07/A2+A4 — la conferma `InventoryKind` per un equip reale (T-12, seconda metà) non è ancora fatta | **Claude** | Q-086 | `docs/TEST_RIMANDATI.md` | **DONE — Verified (calibrazione, evidenza operatore su client reale)** |
| Q-090 | Trasversale | A6 — Istruzione esplicita dell'utente: mai più far digitare comandi per un test reale, solo bottoni. Nuova vista "Equipaggiamento" in `NosAi.ControlPanel` (`MainWindow`), stesso schema già in uso per Percezione/Certificazione (bottone → `ToolRunner.RunAsync` in sottoprocesso → log). Card per il test T-12 rimasto (conferma `InventoryKind` da un equip reale): riusa il rilevamento endpoint già esistente (`OnDetectObserveGame`), bottone "Registra equip (30s)" (`--record-wire <endpoint> data/equip_test.noscap --watch 30`), bottone "Analizza registrazione" (`--world-replay`, filtra e mostra solo le righe con `kind=`). `RunToolAsync` esteso in modo additivo (parametro opzionale `onLine`, ritorna `ToolResult` invece di scartarlo) — nessuna firma di chiamante esistente rotta. Pattern pensato per essere riusato: ogni test reale futuro è una nuova card nella stessa vista. Build verde (0 errori/0 warning); i test di `NosAi.ControlPanel.Tests` non eseguibili in questo sandbox Linux (richiedono il runtime Desktop Windows, limite ambientale pre-esistente, non regressione) — verifica reale rimandata all'operatore | **Claude** | Q-089 | `src/NosAi.ControlPanel/MainWindow.xaml`, `src/NosAi.ControlPanel/MainWindow.xaml.cs` | **DONE — Present (build verificata, esecuzione reale rimandata all'operatore)** |
| Q-091 | AP-07 | A2+A4 — Registrazione reale mostrò 6 pacchetti `ivn` mai decodificati (`inventory (ivn): 0 reason=inventory_slot_empty`) e opcode `eq`/`equip`/`bf`/`pairy`/`sc`/`dir`/`eff` mai gestiti — causa non ancora nota, non indovinabile senza vedere i campi reali. Istruzione esplicita dell'utente: decoder in tempo reale dentro NosAiProject, non solo analisi offline. Nuovo `LiveWireMonitor` (`--live-decode <ip:porta> [--watch N]`, stesso `WinDivertPacketSource` sniff-only di `WireRecorder`): per ogni frame in arrivo stampa subito la riga `RETE` grezza (`NosTaleWorldDecoder.Decode`, qualunque opcode, nessuna interpretazione) e, quando l'opcode è tra i 12 letti da `NosTaleWorldProtocolDecoder`, una riga `CLIENT` con la lettura semantica (vitali/avvistamenti/eventi/inventario/...); un frame outbound (cifrato) resta `<non decifrabile>`, mai bytes indovinati. Logica pura isolata in `LiveWireMonitor.Monitor(IPacketSource, TextWriter, CancellationToken)`, testabile con `InMemoryPacketSource` senza driver (stessa separazione di `WireRecorder.RecordFrom`). Nuova card "Decodifica dal vivo" in Equipaggiamento (`OnStartLiveDecode`, 180s, bottone unico): ogni riga finisce nel Diario in tempo reale via `RunToolAsync`, la card mostra solo il riepilogo finale. Non risolve ancora il gap `ivn`/`eq` — serve l'operatore che lanci il test e mandi le righe reali osservate | **Claude** | Q-090 | `src/NosAi.Runtime/LiveIntegration/Capture/LiveWireMonitor.cs`, `src/NosAi.Runtime/Program.cs`, `src/NosAi.ControlPanel/MainWindow.xaml`, `src/NosAi.ControlPanel/MainWindow.xaml.cs`, `tests/NosAi.Runtime.Tests/LiveWireMonitorTests.cs` | **DONE — Present (build 0 errori, Runtime 2149/2149 + 4 nuovi test; verifica reale contro il client rimandata all'operatore)** |
| Q-092 | AP-08 | A1+A3 — Istruzione esplicita dell'utente: accelerare, dare a DeepSeek lavoro reale non fabbricato. Indagine mirata su `AP-10_A1_STATUS.md` (stadio 12, Recovery): il gap "nessun assessor per `StrategicGoalKind.Recovery`" è reale ma sbloccabile senza nuovo canale di rete/elevazione — un calo HP osservato su due letture consecutive già lette ogni ciclo da `--autoplay` per Survival basta come fatto "in combattimento adesso" onesto. `CombatRecencyTracker` (nuovo, puro, `NosAi.Core`): deriva un `WorldFact<bool>` dal calo HP con finestra di decadimento 15s, `Unknown("insufficient_history")` al primo poll, mai "non in combattimento" per assunzione. `StrategyPlanner.AssessRecoveryUrgency` (nuovo, puro): stessa formula di urgenza di `AssessSurvivalUrgency` ma `null` mentre `inCombat.Value==true`, cosi' non duplica il segnale di Survival sotto altro nome — la ragione esatta per cui Recovery era rimasto non valutato finora. 11 nuovi test (6 tracker + 5 planner), tutti verdi; nessuna modifica a `AutoplayCommand` in questo passaggio (resta A2+A4 vero, nessun chiamante runtime) — specifica DeepSeek scritta per il solo collegamento meccanico, nessun nuovo flag CLI necessario | **Claude** | Q-090 | `src/NosAi.Core/WorldModel/Strategy/CombatRecencyTracker.cs`, `src/NosAi.Core/WorldModel/Strategy/StrategyPlanner.cs`, `src/NosAi.Runtime/Tactical/AutoplayCommand.cs` (solo doc-comment), `tests/NosAi.Core.Tests/WorldModel/Strategy/CombatRecencyTrackerTests.cs`, `tests/NosAi.Core.Tests/WorldModel/Strategy/StrategyPlannerTests.cs`, `docs/agents/phases/AP-08/AP-08_A2A4_DEEPSEEK_recovery_signal.md` | **DONE — Present (A1+A3 Claude: build 0 errori/0 warning, Core 654/654; A2+A4 DeepSeek consegnato 2026-09-06, commit `a5e587e`+`06987bc` push diretti su `main` — `AutoplayCommand.ExecuteOneCycle` dispatcha `Recovery` come `Survival` via `DispatchRecovery` condiviso, `RunWindows` traccia `CombatRecencyTracker.State` per ciclo. Audit indipendente Claude ha trovato 2 difetti reali nella consegna, entrambi corretti nello stesso passaggio: (1) `tests/.../AutoplayCommandTests.cs:342` elencava ancora `Recovery` come kind non-dispatchabile — teoria stantia dopo il wiring, rimossa la riga; (2) commento copiato dalla spec in `AutoplayCommand.cs` affermava "recovery/survival never both non-null at once", falso (verificato su `StrategyPlanner.cs`: fuori combattimento con HP nota sotto il massimo entrambi tornano non-null, caso comune) — riscritto per dire la vera ragione (list-order decide il tie-break). Rationale corretto anche nella spec storica. Build 0 errori/0 warning, Core 654/654, Runtime 2150/2150 (60 skip pre-esistenti, client reale). Resta `Present`, non `Verified`: nessun operatore ha ancora osservato un dispatch Recovery reale su client)** |
| Q-093 | AP-06 | A1 — Audit indipendente (agente in background) trova un bug reale, piccolo e concreto in `NosTaleWorldProtocolDecoder.DecodeDeath`: il vnum dell'entità (confermato dal precedente pacchetto `in`, già tracciato in `TrackedEntity.Vnum`) veniva scartato con `_entities.Remove(entityId)` prima di costruire il `GameEvent`, quindi `GameEventKind.EntityDeath` non poteva mai portare "quale specie è morta" — anche una volta risolta la classificazione mostro/npc di AP-05, "ucciso un mostro di vnum X" resterebbe non confermabile da questo evento. `GameEvent` esteso con `int? Vnum = null` (parametro opzionale, nessuna chiamata esistente rotta — `SyntheticProtocolDecoder`/`ProtocolMap` restano `null`, onestamente, non avendo mai tracciato un vnum). `DecodeDeath` ora legge `_entities.GetValueOrDefault(entityId).Vnum` prima della `Remove` | **Claude** | Q-085 (audit) | `src/NosAi.Runtime/Perception/Network/GameTrafficObserver.cs`, `src/NosAi.Runtime/Perception/Network/NosTaleWorldProtocolDecoder.cs`, `tests/NosAi.Runtime.Tests/NosTaleWorldObservationTests.cs` | **DONE — Present (build 0 errori/0 warning, Runtime 32/32 sui test del file toccato, 2151/2151 sulla suite intera + 60 skip pre-esistenti; nessun operatore ha ancora osservato un vnum reale su una morte dal vivo)** |
| Q-094 | AP-07 | A1+A3 — Audit indipendente (stesso agente) trova un gap reale e ancora aperto: `LoadoutPlanner`'s class remarks dichiaravano "nothing in this repository decodes an item's equipment category yet", falso da quando `ItemReferenceDecoder` (Q-088) decodifica il vero `EquipmentSlot` da `Item.dat`. `LoadoutPlanner` vive in `NosAi.Core` (zero dipendenze) e non può referenziare direttamente il decoder in `NosAi.Runtime` — nuovo `GenerateEquipCandidates(Player, Func<ItemId, EquipmentSlot?> resolveSlot)` (puro, stesso pattern caller-supplied-fact di `AssessRecoveryUrgency`/Q-092): un item senza risposta dal lookup non produce candidato, mai indovinato. 4 nuovi test (risolvibile, non risolvibile, quantità zero, quantità sconosciuta). Specifica DeepSeek A2+A4 scritta per il comando **read-only** `--loadout-report` (nessuna esecuzione equip/unequip — resta fuori scope come da T-12 seconda metà): legge inventario/equipaggiamento reali via `GameplayObservationProjector.Project` sullo stesso `LiveObservationGateway` già usato da `CollectCommand`, risolve gli slot dal catalogo reale via `GameReferenceLocator`+`ItemReferenceDecoder`, stampa i tre elenchi di candidati con l'esito di `CheckHardConstraints` | **Claude** | Q-093 (audit) | `src/NosAi.Core/WorldModel/Loadout/LoadoutPlanner.cs`, `tests/NosAi.Core.Tests/WorldModel/Loadout/LoadoutPlannerTests.cs`, `docs/agents/phases/AP-07/AP-07_A2A4_DEEPSEEK_loadout_report.md` | **DONE — Present (A1+A3 Claude: build 0 errori/0 warning, Core 658/658. A2+A4 DeepSeek consegnato 2026-09-07, commit `349eab1` push diretto su `main` — `LoadoutReportCommand` + `BuildResolveSlot` + dispatch `--loadout-report`, scope rispettato esattamente (3 file, `Program.cs` solo dispatch). Audit indipendente Claude: **nessun bug funzionale**, null handling completo, round-trip `ItemId`↔vnum esatto, nessun placeholder, nessun test pre-esistente reso obsoleto. Nove difetti minori corretti nello stesso passaggio, sei dei quali della classe REGOLA #3 (affermazioni non verificate): (1) il doc di test diceva che nessun altro comando testa il proprio `Run()` — falso, `CollectCommandTests`/`EngageCommandTests`/`RecoverCommandTests`/`AutoplayCommandTests` lo fanno tutti; corretto e **aggiunto** il test del ramo off-Windows che mancava, piu' un test sulle costanti di rifiuto; (2) `BuildResolveSlot_KnownVnum_DecodesItsSeededSlot` seminava sei `"0"` in `INDEX`, quindi passava identico anche leggendo la posizione sbagliata — ora `Theory` su tre slot con valori vicini distinti (90/91/92/_/94/95), cosi' la posizione 3 e' davvero sotto test; (3) documentato un ramo di fallimento irraggiungibile su quel percorso (`Decode` senza VNUM, ma il vnum e' gia' parsato); (4) costante morta `SourceModule` con doc copiata da `CollectCommand` — rimossa; (5) `cref` irrisolvibile a `RuntimeComposition`; (7) forma della riga di riepilogo attribuita a `LiveWireMonitor` ma diversa; (8) analogia di purezza con `AutoplayCommand.ExecuteOneCycle`, che invece attua; (9) `Print` pubblico senza guardia su un `LoadoutReport` di default — aggiunte tre `ThrowIfNull`. Non corretto perche' conforme alla specifica: l'attach di memoria non necessario per un comando di sola lettura (6). Build 0 errori/0 warning, Runtime 2253/2255 (+4 test). Nota sul baseline Windows: in questa esecuzione `MapReconstructionSourceTests.Resolve_NoStoreAvailable_...` è passato, mentre nelle quattro precedenti era rosso — è quindi **flaky**, non un rosso stabile; restano stabilmente rossi solo i due test `OnANonWindowsHost`, che su Windows non possono passare per costruzione** |
| Q-095 | AP-05 | A1 — Indagine propria (mentre DeepSeek lavora su Q-094): il gap "AP-05 monster/NPC" segnalato dall'audit indipendente era mal diagnosticato. `TargetEstablishment.Assess` (`src/NosAi.Runtime/Autonomy/TargetEstablishment.cs`) risolve già la distinzione mostro/NPC per l'autorizzazione a colpire, via `GameReferenceDatabase.Exists("monster", vnum)`, ed è già cablato nel percorso reale (`Gate3Runtime.cs:475`) — non manca un "decoder npc.dat" come ipotizzato, `docs/TASTI_E_BERSAGLIO.md` § 5-6 lo documenta già come design deciso. Confermato anche che il vnum NON viene più scartato dal 2026: `NosTaleWorldProtocolDecoder.DecodeEnter` lo legge e lo porta fino a `SelectableEntity.Vnum` — `TASTI_E_BERSAGLIO.md` § 5.1 lo dichiarava ancora scartato, corretto. Restano stale solo due commenti che descrivevano il gap sbagliato: `GameplayObservationProjector.cs` (class remarks, perché `WorldModelSnapshot.Mobs`/`Npcs` restano vuoti) e `CombatExecutionContracts.cs` (perché `Mob.Status.Resources` non è popolato) — entrambi corretti per dire il vero gap residuo, più stretto: `SelectableEntity.HpRatio` porta già una frazione HP reale del bersaglio, ma `GameplayObservationProjector` non trasforma ancora i `SelectableEntity` confermati mostro in `Mob` reali per il World Model canonico (AP-01) — wiring meccanico non ancora tentato, richiede prima una decisione di design onesta su come rappresentare una frazione 0..1 senza massimo noto nel contratto `Resource` (Current/Maximum) esistente | **Claude** | Q-094 (audit proprio) | `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs` (solo doc-comment), `src/NosAi.Core/WorldModel/Combat/CombatExecutionContracts.cs` (solo doc-comment), `docs/TASTI_E_BERSAGLIO.md` | **DONE — Present (solo correzione documentazione/commenti, nessuna modifica a logica o test; build 0 errori/0 warning, Core 658/658, Runtime 2151/2151)** |
| Q-096 | AP-05 | A1+A3 — Istruzione esplicita dell'utente: "crea un decoder per npc se serve". Verificato via fonte esterna citata (nt-research.github.io, pagina `monster.dat`, fetch diretto 2026-09-07): **non esiste un file `Npc.dat` distinto** — la stessa pagina lo conferma esplicitamente e lista solo Item/monster/Skill/BCard sotto `NSgtdData`. Le NPC sono già dentro `monster.dat` stesso: `RaceType` (tag `RACE`, primo valore) `== 8` è documentato dalla fonte come "Special NPCs" con 9 `RaceSubType` nominati (0 trappole fisse, 1 sfere energetiche, 2 palle di cannone, **3 NPC parlanti**, 4 generici, 5 teleport, 6 spawner quest, 7 collezionabili, 8 non documentato) — nessuno di questi è un mostro da combattere. Nuovo `MonsterReference.IsSpecialNonMonsterEntity` (bool?, `RaceType==8`, null se `RaceType` ignoto — mai `false` per omissione), citato e motivato nel doc-comment. `TargetEstablishment.Assess` ora esclude questi vnum dall'evidenza `CataloguedMonster` (nuovo `IsSpecialNonMonsterEntity(catalogue, vnum)` privato) — sicuro anche da non ancora incrociato con un client reale per l'asimmetria già di design del metodo (§6.2: può solo restringere chi è attaccabile, mai allargarlo, quindi un errore costa un mostro vero rifiutato, mai un NPC colpito). 6 nuovi test (3 su `IsSpecialNonMonsterEntity`, 2 su `TargetEstablishment` con `GameReferenceDatabase.OpenInMemory()` reale seeded, 1 di aggiornamento dottext) | **Claude** | Q-095 | `src/NosAi.Runtime/GameData/MonsterReferenceDecoder.cs`, `src/NosAi.Runtime/Autonomy/TargetEstablishment.cs`, `tests/NosAi.Runtime.Tests/GameData/MonsterReferenceDecoderTests.cs`, `tests/NosAi.Runtime.Tests/GoalAndReactionTests.cs` | **DONE — Present (build 0 errori/0 warning, Runtime 2156/2156, Core 658/658 con l'unico rosso preesistente confermato flaky per contesa host; non ancora `Verified` — nessun operatore ha ancora osservato un vnum RaceType 8 reale rifiutato su client vivo)** |
| Q-097 | AP-05 | A1+A3 — Sblocco della decisione di design sospesa da Q-095 (`Resource` Current/Maximum non sapeva esprimere una frazione 0..1 senza massimo noto). Due scoperte reali cambiano la diagnosi: (1) il `Resource` ora porta `Fraction`, terzo fatto classificato indipendentemente, `Derived` dai due estremi quando esistono e `Live` quando solo la percentuale è osservata (`Resource.FromObservedFraction`); (2) **il massimo non è sempre ignoto** — `NosTaleWorldProtocolDecoder.DecodeOtherVitals` (`src/NosAi.Runtime/Perception/Network/NosTaleWorldProtocolDecoder.cs:162-168`) legge gia HP e maxHP **assoluti** di una entità non-giocatore dal pacchetto `st` (campi 7 e 9), li valida, e li divide via una riga dopo perché `EntitySighting` non ha un campo dove metterli. Cattura reale gia replicata nei test: `st 3 313816 8 0 66 100 198 52 310 52 0` = 198/310 per il mostro 313816, con doppio riscontro indipendente (198/310 = 63,87% contro il 66 del campo percentuale, che il decoder infatti ignora; e `docs/PROTOCOLLO_NOSTALE.md` § `su` registra 310 come max HP dello stesso mostro nella stessa cattura). Corretti i quattro commenti che dichiaravano il gap in modo ormai falso (`CombatExecutionContracts.cs`, `CombatVerificationProjector.cs` e `EngageCommand.cs` dicevano ancora "OCR/ONNX gap"). Scritta la specifica DeepSeek per il primo passo Runtime | **Claude** | Q-095 | `src/NosAi.Core/WorldModel/ResourceContracts.cs`, `src/NosAi.Core/WorldModel/Combat/CombatExecutionContracts.cs` (solo doc-comment), `tests/NosAi.Core.Tests/WorldModel/ResourceFractionTests.cs`, `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs` + `CombatVerificationProjector.cs` + `src/NosAi.Runtime/Tactical/EngageCommand.cs` (solo doc-comment), `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_mob_absolute_vitals.md` | **DONE — Present (build 0 errori/0 warning; Core 675/675, +17 test nuovi; Runtime 2213/2216 — i 3 rossi sono preesistenti e riprodotti identici su un worktree pulito a `aa8e233`: due test `OnANonWindowsHost` che su Windows non possono passare per costruzione, più `MapReconstructionSourceTests.Resolve_NoStoreAvailable_...`. A2+A4 non ancora consegnato da DeepSeek)** |
| Q-098 | AP-05 | A1+A3+A4 — Richiesta esplicita dell’utente: "continua con il passo 2, proietta i Mob nel World Model". `WorldModelSnapshot.Mobs`/`Npcs` erano vuoti in **ogni** percorso di produzione (nessun `new Mob(` esisteva fuori dai test; `CombatPlanner.GenerateCandidates` non ha tuttora chiamanti di produzione). Ora `GameplayObservationProjector` li costruisce da `GameplayObservation.Entities`, e il cablaggio arriva fino a `Program.cs`. Tre decisioni portanti: (1) **un solo predicato** — estratto `TargetEstablishment.ClassifyByCatalogue` (nuovo, pubblico, totale, non lancia) e `Assess` ora lo chiama, comportamento e stringhe di rifiuto invariati, così la risposta che autorizza un attacco e quella che riempie il World Model non possono divergere; (2) **solo le risposte positive proiettano** — `Mobs` solo per un vnum stabilito mostro, `Npcs` solo per una riga RaceType 8; vnum non osservato, assente o catalogo illeggibile non entrano in nessuna lista, perché dedurre gli NPC dal residuo invertirebbe l’asimmetria di sicurezza di `TargetEstablishment`; (3) **il classificatore è un `Func<int,CatalogueClass>` puro**, mai un handle `GameReferenceDatabase` — quel tipo è una singola `SqliteConnection` senza sincronizzazione né cache e una classificazione costa fino a 3 query, quindi `CatalogueClassifier` (nuovo) memoizza per vnum sotto lock e non memorizza mai `CatalogueUnreadable` (fallimento transitorio). In `Program.cs` il catalogo si apre **dopo** il controllo aggiornamenti client, che scrive sullo stesso file SQLite. Vita dei mob: frazione su due estremi Unknown, etichettata `Cached` e non `Live` perché `SelectableEntity.ObservedAtUtc` è l’istante della posizione; `IsHostile` resta Unknown (stare nella tabella dei mostri non è essere ostile). Corrette due affermazioni false trovate da audit indipendenti: `Gate3ExecutionOrchestrator` non riceve **mai** il catalogo in produzione (quindi la distinzione mostro/NPC non gira mai lì — gap reale ancora aperto; ma **non** è vero, come questa riga affermava, che `Assess` torni *sempre* `reference_catalogue_not_loaded`: risponde prima dai due cortocircuiti di evidenza, poi rifiuta con `target_vnum_not_observed`, che è il caso comune), e "no item-category decode exists" era falso dal commit `e597c0d` | **Claude** | Q-097 | `src/NosAi.Runtime/Autonomy/TargetEstablishment.cs`, `src/NosAi.Runtime/Autonomy/CatalogueClassifier.cs` (nuovo), `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs`, `src/NosAi.Runtime/WorldModel/Fusion/WorldModelFusionLoop.cs`, `src/NosAi.Runtime/Program.cs`, `tests/NosAi.Runtime.Tests/WorldModel/Fusion/GameplayObservationEntityProjectionTests.cs` (nuovo), `tests/NosAi.Runtime.Tests/CatalogueClassifierTests.cs` (nuovo), `tests/NosAi.Runtime.Tests/WorldModel/Fusion/WorldModelFusionLoopEntityWiringTests.cs` (nuovo), `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_mob_absolute_vitals.md` | **DONE — Present (build 0 errori/0 warning; Core 675/675; Runtime 2248/2251, +28 test nuovi, i 3 rossi sono il baseline preesistente su host Windows. Non `Verified`: nessun operatore ha ancora osservato un `Mob` reale nello snapshot fuso su client vivo con catalogo presente)** |
| Q-099 | AP-05 | A1+A3+A4 — Richiesta esplicita dell’utente: "collega la lista mob ai planner". Indagine: collegarla e basta **non avrebbe cambiato nulla**. `ExplorationPlanner.ComputeRisk` (`src/NosAi.Core/WorldModel/Exploration/ExplorationPlanner.cs:278-279`) e `CombatPlanner.IsViableTarget` (`src/NosAi.Core/WorldModel/Combat/CombatPlanner.cs:90`) scartano entrambi un mob con `IsHostile` Unknown, e ogni mob proiettato lo era. Tre pezzi, in ordine di dipendenza: (1) **ostilità** — `GameplayObservation.HitBy` è l’unica fonte onesta disponibile (il decoder lo pubblica solo dopo aver stabilito, dall’id di `cond`, che il `su` era diretto a questo personaggio); non usata `SelectedTarget` (aver mirato un’entità non la rende ostile, anche un NPC si seleziona) né `MonsterReference.Hostility` (campo di `monster.dat` mai incrociato con un valore reale osservato). `WorldModelTemporalEnricher` porta avanti l’ostilità stabilita come `Cached`, con l’istante originale che continua a invecchiare, mai al contrario e mai trasformandola in `false`. (2) **`LiveObservationScope`** — lo scope di cattura WinDivert esisteva in due copie private byte-identiche (`CollectCommand`, `LoadoutReportCommand`); estratto in un tipo condiviso, stesse stringhe di fallimento, stessa composizione, stesso ordine di dispose. (3) **alimentazione** — `ScoutCommand` e `AutoplayCommand` aprono lo scope e il catalogo in modo opportunistico (stessa forma dell’`ActionOutcomeLedgerStore` già presente: avverte e prosegue, mai un motivo di rifiuto) e proiettano i mob a ogni round/ciclo con `GameplayObservationProjector`. Senza cattura (serve Amministratore) o senza catalogo il ranker resta cieco al rischio, esattamente come prima. Corrette le class remarks di `ScoutCommand`, che dichiaravano il gap ormai chiuso | **Claude** | Q-098 | `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs`, `src/NosAi.Core/WorldModel/Temporal/WorldModelTemporalEnricher.cs`, `src/NosAi.Runtime/LiveIntegration/LiveObservationScope.cs` (nuovo), `src/NosAi.Runtime/Navigation/CollectCommand.cs`, `src/NosAi.Runtime/Tactical/LoadoutReportCommand.cs`, `src/NosAi.Runtime/Navigation/ScoutCommand.cs`, `src/NosAi.Runtime/Tactical/AutoplayCommand.cs`, `tests/NosAi.Core.Tests/WorldModel/Temporal/MobHostilityCarryForwardTests.cs` (nuovo), `tests/NosAi.Runtime.Tests/LiveObservationScopeTests.cs` (nuovo), `tests/NosAi.Runtime.Tests/WorldModel/Fusion/GameplayObservationEntityProjectionTests.cs` | **DONE — Present (build 0 errori/0 warning; Core 681/681, Runtime verde sul baseline Windows. Non `Verified`, e il limite è preciso: nessun operatore ha ancora eseguito `--scout`/`--autoplay` come Amministratore su client vivo per vedere un `Risk` diverso da zero. `CombatPlanner.GenerateCandidates` era ancora senza chiamanti di produzione al momento di questo commit; chiuso subito dopo da Q-100)** |
| Q-100 | AP-05 | A3+A4 — Richiesta esplicita dell’utente: "dai un chiamante di produzione a `CombatPlanner`". `GenerateCandidates`/`CheckHardConstraints` erano raggiungibili solo dai propri unit test: niente di ciò che quel planner decide era mai stato eseguito contro un client reale. Indagine sulle alternative: **non esiste** un `StrategicGoalKind` di combattimento (`StrategyContracts.cs:13-22`: Survival/Recovery/QuestUrgency/Progression/Farming/Exploration/Optimization), quindi non c’è un ramo di `AutoplayCommand.ExecuteOneCycle` da cablare; `EngageCommand` prende l’id bersaglio direttamente da un argomento dell’operatore e non verifica nulla su di esso (`EngageCommand.cs:213-216`); e **nessuno stadio di ranking consuma `CombatActionCandidate`** — `TacticalRankingEngine` lavora su `NosAi.Runtime.Contracts.ActionCandidate`, confine documentato in `CombatContracts.cs:6-11`. Quindi un primo chiamante può solo generare → vincoli duri → stampare, ed è esattamente la forma che `--loadout-report` ha già dato a `LoadoutPlanner`. Nuovo **`--combat-report`**: `Build(Player, mobs)` puro e testabile, composizione live sottile e non testata per design. Esercita l’intera catena AP-05 in un colpo solo — cattura pacchetti → catalogo → `Mob` con vita frazionaria e ostilità da `HitBy` → memoria client per posizione e vitali → `CombatPlanner` — ed è l’unico posto in cui un operatore può vederle tutte insieme su client vivo. Porta un `CombatMobCensus` perché un report vuoto dica **quale** dei mondi possibili sta riportando: nessun mostro osservato, mostri ma nessuno di ostilità stabilita, nessuna vita dichiarata, nessuna posizione, o tutto valido ma fuori portata. Corretta una ragione falsa in `AutoplayCommand`: `playerFacts.Position` era Unknown con motivo `not_read_this_cycle`, mentre la posizione **era** letta in quel ciclo (la stessa che alimenta `currentReading`) — ora è il fatto reale, nessun assessor la legge oggi quindi nessuna decisione cambia | **Claude** | Q-099 | `src/NosAi.Runtime/Tactical/CombatReportCommand.cs` (nuovo), `src/NosAi.Runtime/Program.cs`, `src/NosAi.Runtime/Tactical/AutoplayCommand.cs`, `src/NosAi.Runtime/Tactical/EngageCommand.cs` (solo doc-comment), `tests/NosAi.Runtime.Tests/CombatReportCommandTests.cs` (nuovo), `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` | **DONE — Present (build 0 errori/0 warning; +13 test nuovi. Non `Verified`: nessun operatore ha ancora eseguito `--combat-report` come Amministratore su client vivo. Restano due limiti dichiarati nel comando stesso: nessun canale legge la lista skill del personaggio, quindi solo candidati `BasicAttack` sono generabili; e un candidato nasce solo contro un mob già noto ostile, cioè oggi solo uno che la rete ha registrato mentre colpiva)** |
| Q-101 | AP-05 | A3+A4 — Richiesta esplicita dell’utente: "collega il verdetto di combat-report al rifiuto di `--engage`". Prima: l’id bersaglio arrivava da un argomento dell’operatore e **nulla** lo verificava — non che nominasse un’entità reale, non che fosse ostile, viva o a portata (`EngageCommand.cs:213-216`). Ostacolo trovato subito: `CheckHardConstraints` su un `Player` osservato dal vivo riporta **sempre** `skill_not_found`, perché `Player.Skills` è un `EquatableArray` senza modo di dire "non osservato" e nessun canale legge la lista skill — cablarlo così avrebbe rifiutato ogni atto per una ragione che parla del gap, non del personaggio. Aggiunto quindi `CombatPlanner.CheckTargetConstraints` (Core, pubblico): la metà bersaglio dei vincoli duri da sola, con `CheckHardConstraints` rifattorizzato per condividerne l’implementazione (19/19 test pre-esistenti invariati). La proprietà di sicurezza è fissata da un test: **ogni violazione che riporta è riportata anche dal check completo** — restringe cosa viene verificato, mai cosa viene permesso. `LiveCombatObserver` (nuovo) tiene aperta una sola cattura per invocazione e la condivide con `--combat-report`, così la fotografia mostrata all’operatore e quella da cui si calcola il rifiuto sono la stessa. Il verdetto si ri-prende **a ogni round**, non una volta: un mob esce e rientra a portata, e uno che non ha ancora attaccato diventa stabilito nell’istante in cui colpisce — per questo un rifiuto bersaglio non chiude l’invocazione come fa un rifiuto keybind, pur condividendo `Aborted`. **Scelta fail-closed dichiarata**: quando la cattura o la lettura del client falliscono, `--engage` rifiuta invece di proseguire non verificato (ADR-0016, "l’ignoto non autorizza un atto"); conseguenza pratica, `--engage` ora richiede Amministratore. Il round rifiutato passa dallo stesso canale di evidenza e finisce nello stesso ledger di uno eseguito | **Claude** | Q-100 | `src/NosAi.Core/WorldModel/Combat/CombatPlanner.cs`, `src/NosAi.Runtime/Tactical/LiveCombatObserver.cs` (nuovo), `src/NosAi.Runtime/Tactical/EngageCommand.cs`, `src/NosAi.Runtime/Tactical/CombatReportCommand.cs`, `tests/NosAi.Core.Tests/WorldModel/Combat/CombatTargetConstraintTests.cs` (nuovo), `tests/NosAi.Runtime.Tests/EngageCommandTests.cs` | **DONE — Present (build 0 errori/0 warning; +15 test nuovi. Non `Verified`: nessun operatore ha ancora visto `--engage` rifiutare un bersaglio reale su client vivo. **Da comunicare all’operatore**: `--engage` senza Amministratore ora rifiuta invece di premere il tasto)** |
| Q-102 | Trasversale | A5+A6 — Lavoro notturno a lista chiusa, autorizzato dall’utente prima di andare a dormire (nessun task inventato oltre questi). **(1) Due copie di lavoro.** `docs/agents/DEEPSEEK_TASKS.md` § "Cartella di lavoro" dichiarava ogni altra cartella `NosAiProject*` sul Desktop "materiale morto … non ha commit propri": la seconda metà è falsa ed è la metà pericolosa. `C:\Users\volob\Desktop\NosAiProject` contiene **46 commit che non esistono su nessun ref remoto**, su 12 branch locali — i maggiori `codex/cursor-perception-contract` (11 commit, 65 file, +9218 righe), `worktree-piano-controllo-gioco` (5, +1837), `feature/tactical-simulator-v1` (9, +839), `main-merge-tactical` (10, +822); di tactical-simulator-v1 il commit `0fe2a5a` aveva recuperato la sola documentazione. Sezione corretta con il comando che li elenca e la conseguenza: pushare quei branch **prima** che quella cartella sia considerata sacrificabile. Nessuna cartella toccata, nessun branch cancellato. **(2) Test instabili.** Tre classi chiamano `Console.SetOut`, che è globale al processo, e xUnit le faceva girare in parallelo: si contendevano un solo handle. Osservato oggi con la suite a 2314 test (`ProbePrintsThatACalibrationFromAnotherRegimeIsNotUsable` + `OutsideAnyScopeTheLoggerPrintsNone` rosse insieme, verdi 21/21 isolate). `ConsoleCaptureCollection` le serializza e lascia gli altri ~2300 test in parallelo. **(3) Ragione di rifiuto falsa.** `CheckSkillReady` diceva `skill_not_found` sia quando la lista skill è stata letta e la skill manca, sia quando la lista è vuota perché nessuno l’ha letta — e su client reale capita sempre la seconda, quindi il rifiuto dava la colpa al personaggio per un buco nei canali. Ora la lista vuota dice `skill_list_not_observed`; il test che asseriva la vecchia stringa è stato aggiornato per esercitare davvero "not found", più un test nuovo per il caso vuoto. **Resta aperto e non toccato**: `Player.Skills`/`Cooldowns`/`Inventory`/`Equipment` sono `EquatableArray` che non distinguono "vuoto" da "non osservato" — è una decisione di forma sul contratto `Player`, non una correzione locale, e va presa da sveglio. **Nota sul baseline**: il `NonWindowsFact` di DeepSeek (`4fc14ec`) ha chiuso i due rossi storici; il baseline Windows è ora **verde**, 2312 passati / 2 skip / 0 rossi | **Claude** | Q-101 | `docs/agents/DEEPSEEK_TASKS.md`, `tests/NosAi.Runtime.Tests/ConsoleCaptureCollection.cs` (nuovo), `tests/NosAi.Runtime.Tests/ClientWindowDpiProbeTests.cs`, `tests/NosAi.Runtime.Tests/MapGridCheckTests.cs`, `tests/NosAi.Runtime.Tests/RuntimeLoggerTests.cs`, `src/NosAi.Core/WorldModel/Combat/CombatPlanner.cs`, `src/NosAi.Runtime/Tactical/EngageCommand.cs` (solo doc-comment), `tests/NosAi.Core.Tests/WorldModel/Combat/CombatPlannerTests.cs`, `tests/NosAi.Core.Tests/WorldModel/Combat/CombatTargetConstraintTests.cs` | **DONE — Present (build 0 errori/0 warning; Core 695/695, Runtime 2312/2314 con 2 skip e zero rossi. Commit `12fbd1e`, `d907aa4`, `5e9bd93`)** |
| Q-103 | AP-05 | A5+A3+A4 — **Audit indipendente della catena AP-05 e correzione di ciò che ha trovato.** I sette commit della giornata erano stati scritti, rivisti e dichiarati verdi dallo stesso agente; un audit avversariale li ha riesaminati applicando REGOLA ASSOLUTA #3 all'autore. **Il difetto principale era di correttezza, non documentale:** `--combat-report` affermava due volte (`EngageCommand.cs:52`, `:148`) che il suo verdetto «reads identically» al rifiuto di `--engage`, ed era falso per costruzione. `--engage` giudica un candidato `UseSkill` a `DefaultSkillRange = 6.0`; il report chiamava `GenerateCandidates` su un `Player` con lista skill sempre vuota, che non può produrre altro che candidati `BasicAttack` a `DefaultBasicAttackRange = 2.0`. Per un mob a distanza 3–6 il report stampava `no_candidate_in_range` mentre `--engage` premeva il tasto: errato **nella direzione pericolosa**. In più, `GenerateCandidates` prefiltra esattamente sulle condizioni che `CheckHardConstraints` poi verifica, quindi le colonne `allowed=`/`violations=` erano strutturalmente sempre `True`/`none`. Il report ora ha due metà: righe `engage:` (una per mob **osservato**, dalla stessa chiamata `CheckTargetConstraints` che `EngageCommand` fa) e righe `candidate:` (le proposte del planner). Il verdetto lato bersaglio non dipende dalla skill nominata — `CheckTargetConstraints` legge `Kind` e `Target`, mai `Skill` — e un test fissa quell'indipendenza. **Altri difetti chiusi:** l'invariante di `Resource` era dichiarata ma non imposta (`Current`/`Maximum` erano `public init`, quindi un `with` esterno copiava la vecchia `Fraction`); un test poteva aprire una **cattura WinDivert reale** se l'host di test avesse avuto esattamente una connessione remota; il test che avrebbe dovuto coprire `JudgeTarget` costruiva l'evidence da sé e non poteva fallire (sostituito, ed estratto `DescribeTargetRefusal` per rendere testabile la decisione); sei test verdi dichiaravano nel nome di fallire; e otto affermazioni in commenti e documenti erano false o stantie. **Esiti negativi espliciti dell'audit:** nessun difetto di risorse o lifetime, nessun placeholder, nessun percorso in cui il nuovo codice agisce dove il vecchio non agiva, e la proprietà di sicurezza «ogni violazione riportata dal check ristretto è riportata anche dal completo» è vera sul codice, non solo sul test. | **Claude** | Q-101 | `src/NosAi.Runtime/Tactical/CombatReportCommand.cs`, `src/NosAi.Runtime/Tactical/EngageCommand.cs`, `src/NosAi.Runtime/Program.cs`, `src/NosAi.Core/WorldModel/ResourceContracts.cs`, `src/NosAi.Core/WorldModel/Combat/CombatPlanner.cs`, `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs`, `src/NosAi.Runtime/LiveIntegration/LiveObservationScope.cs`, `src/NosAi.Runtime/Navigation/CollectCommand.cs`, `src/NosAi.Runtime/Tactical/LoadoutReportCommand.cs`, `src/NosAi.Runtime/Tactical/AutoplayCommand.cs`, `tests/NosAi.Runtime.Tests/CombatReportCommandTests.cs`, `tests/NosAi.Runtime.Tests/EngageCommandTests.cs`, `tests/NosAi.Runtime.Tests/LiveObservationScopeTests.cs`, `tests/NosAi.Runtime.Tests/NoSingleRemoteConnectionFactAttribute.cs` (nuovo), `tests/NosAi.Runtime.Tests/WorldModel/Fusion/MultimodalPipelineDeterminismTests.cs`, `tests/NosAi.Core.Tests/WorldModel/Temporal/TemporalBeliefBoundaryTests.cs`, `tests/NosAi.Core.Tests/TransportLoopTests.cs`, `docs/CERTIFICAZIONI/gate1.md`, `docs/agents/phases/AP-02/AP-02_A5_AUDIT.md` | **DONE — Present** (build 0 errori/0 avvisi; Core 695 superati/1 saltato/0 falliti; Runtime 2323/3/0). Non `Verified`: la divergenza corretta è stata dimostrata su mondo costruito a mano, non ancora su client vivo — vedi `docs/TEST_RIMANDATI.md` T-13 |
| Q-104 | Trasversale | A1+A5 — **Lavoro precompilato per la giornata successiva**, su richiesta esplicita dell'utente («prepara tutto il necessario per la giornata di programmazione, anticipa e/o precompila più lavoro possibile»). Tre artefatti, nessuna decisione presa al posto dell'utente. **(1)** `docs/adr/ADR-0027` (**Proposed**): la forma di una collezione non osservata su `Player`. Misura completa del raggio d'impatto — 6 siti di costruzione (4 in `src/`, 2 in `tests/`) e **14** letture di produzione, non le 41 che un `grep` grezzo conta: le altre 27 sono righe `using` sul namespace `NosAi.Economy.Inventory`, membri di enum, letture di `observation.Inventory`/`profile.Inventory` e citazioni nei commenti. Le 14 vere stanno in tre soli file (`CombatPlanner` 5, `LoadoutPlanner` 7, `CollectCommand` 2) — e il fatto decisivo: **nessun sito di produzione popola `Skills` o `Equipment`**, quindi ogni snapshot mai prodotto afferma che il personaggio non ha abilità e non indossa nulla, e `WorldModelSnapshot.Unknown()` afferma quattro fatti positivi in uno snapshot il cui nome è «Unknown». Tre opzioni prezzate; raccomandata A (`WorldFact<EquatableArray<T>>`) perché trasforma 14 letture da riesaminare a mano in ~41 errori di compilazione. **(2)** `docs/TEST_RIMANDATI.md` T-13: il runbook di validazione su client reale che porta l'intera catena AP-05 da `Present` a `Verified` — cinque passi ordinati più un passo 0 sui keybind, con l'output atteso di ciascuno e cosa registrare. Include l'avvertenza che da `0fc7f55` `--engage` **richiede Amministratore** e rifiuta invece di premere. **(3)** `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_player_skill_list.md`: spec per DeepSeek sul buco di osservazione che ha già prodotto due difetti, con il vincolo esplicito di riportare l'esito negativo se il wire non dice affatto la lista, invece di dedurla da `sr` (che porta un numero di slot, non un'abilità). | **Claude** | Q-103 | `docs/adr/ADR-0027-an-unobserved-collection-is-not-an-empty-one.md` (nuovo), `docs/TEST_RIMANDATI.md`, `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_player_skill_list.md` (nuovo) | **DONE — Present** (documentazione; ADR-0027 attende la decisione dell'utente, T-13 attende l'operatore, la spec attende DeepSeek) |
| Q-105 | AP-05 | A1+A2+A3+A4 — **I compiti di DeepSeek eseguiti da Claude**, su istruzione esplicita dell'utente («DeepSeek rimarrà fermo visto che serve il mio comando scritto; lavora tu e porta avanti il progetto, se serve fai anche i compiti che avevi assegnato a DeepSeek»). **(1) Vitals assoluti dei mostri** (spec `AP-05_A2A4_DEEPSEEK_mob_absolute_vitals.md`): `DecodeOtherVitals` leggeva e validava i punti vita assoluti dai campi 7 e 9 del pacchetto `st` e una riga dopo li divideva via, perché `EntitySighting` non aveva dove metterli — tutto a valle vedeva `0.64` dove il filo aveva detto `198/310`. Ora la coppia viaggia da `EntitySighting` a `SelectableEntity` fino a `GameplayObservationProjector`, che costruisce una `Resource` con estremi reali e lascia che il contratto derivi la frazione; un'entità vista solo da `in` conserva la forma frazione-su-due-Unknown. Non è cablaggio inerte: `LiveCombatObserver` proietta per quella via, quindi `--combat-report` e `--engage` vedono i punti reali. **(2) ADR-0027 applicato**: i quattro `EquatableArray<T>` di `Player` sono ora `WorldFact<EquatableArray<T>>`. Il compilatore ha segnalato esattamente i 12 siti previsti; la migrazione ha fatto emergere tre cose non previste — un difetto latente in `EquatableArray<T>` (il `default` della struct non passa dal costruttore, e `GetHashCode` su uno snapshot con un fatto Unknown lanciava `NullReferenceException`), `AssessCollectProgress` che ora distingue inventario non osservato da inventario osservato e vuoto invece di riportare entrambi come Unknown, e due nuovi rifiuti fail-closed (`cooldown_list_not_observed`, `inventory_not_observed`/`equipment_not_observed`) per casi che con gli array nudi non erano esprimibili. | **Claude** | Q-104 | `src/NosAi.Runtime/Perception/Network/GameTrafficObserver.cs`, `src/NosAi.Runtime/Perception/Network/NosTaleWorldProtocolDecoder.cs`, `src/NosAi.Runtime/Autonomy/TargetSelector.cs`, `src/NosAi.Runtime/LiveIntegration/GameplayProvider.cs`, `src/NosAi.Runtime/WorldModel/Fusion/GameplayObservationProjector.cs`, `src/NosAi.Runtime/Observability/WorldReplayCommand.cs`, `src/NosAi.Runtime/LiveIntegration/Capture/LiveWireMonitor.cs`, `src/NosAi.Core/WorldModel/EntityContracts.cs`, `src/NosAi.Core/WorldModel/WorldModelSnapshot.cs`, `src/NosAi.Core/WorldModel/EquatableArray.cs`, `src/NosAi.Core/WorldModel/Combat/CombatPlanner.cs`, `src/NosAi.Core/WorldModel/Loadout/LoadoutPlanner.cs`, `src/NosAi.Core/WorldModel/Quests/QuestGraphPlanner.cs`, `src/NosAi.Runtime/Tactical/LiveCombatObserver.cs`, `src/NosAi.Runtime/Tactical/AutoplayCommand.cs`, `src/NosAi.Runtime/Navigation/CollectCommand.cs`, `tests/NosAi.Runtime.Tests/EntitySightingAbsoluteVitalsTests.cs` (nuovo), più 10 file di test migrati, `docs/adr/ADR-0027` | **DONE — Present** (build 0 errori/0 avvisi; Core 700 superati/1 saltato/0 falliti; Runtime 2335/3/0; pytest 185 superati). Non `Verified`: nessun operatore ha ancora letto `198/310` di un mostro reale su client vivo — `docs/TEST_RIMANDATI.md` T-13 |
| Q-106 | Trasversale | A5 — **Sweep del lavoro incompleto rimasto**, su richiesta dell'utente prima di consolidare su main. Audit indipendente su `src/`, `tests/`, `tools/`, `docs/` e `nosai/`. **Esito negativo sul fronte segnaposto**: zero `TODO`/`FIXME`/`HACK`/`XXX`, zero `NotImplementedException`, nessun codice commentato che sostituisca codice reale, nessun corpo vuoto usato come stub. L'incompletezza in questo repository non si presenta come segnaposto ma come codice completo che nessuno chiama. **Due difetti reali corretti:** il watchdog termico Python era **fail-open** — `any()` su una lista vuota è `False`, quindi l'assenza totale di dati termici era trattata come «nominale confermato»; ora rifiuta con `thermal_telemetry_unavailable`, e il test che ne fissava il comportamento sbagliato asserisce il rifiuto. E `STATO_IMPLEMENTAZIONE.md` riportava «99,61% decodificato» contraddicendo la propria riga due punti sopra («100,00%, zero UNKNOWN») e il test che asserisce `Assert.Equal(0, unknown)`. **Un falso allarme respinto:** l'audit segnalava 27 file orfani in `src/NosAi.GuardAi.App/` come lavoro a metà; ADR-0025 §3 stabilisce che quella sorgente è *deliberatamente* conservata come materiale storico fuori dalla soluzione — cancellarla avrebbe distrutto ciò che l'ADR dice di tenere. Corretto solo il suo README, che istruiva a compilare un `.csproj` inesistente. **Sette `_STATUS.md` di fase** portano ora una nota datata: dichiaravano mancante ciò che nel codice esiste. **Aperto, riportato e non toccato:** `SimulationEngine.Simulate` (`Gate3Runtime.cs:87-147`) è una tabella di costanti che non legge mai quale skill né quale bersaglio — viola «Do not hardcode a static macro where a model/planner is required»; il registro anti-codice-morto `ModuleReachability` copre solo `NosAi.Runtime`, ed è il motivo per cui `NosAi.Core.Planning` (HTN/GOAP, 10 file) e `NosAi.Adapter.DirectEngine` (15 file) sono irraggiungibili dalla produzione senza che nulla lo segnali. | **Claude** | Q-105 | `nosai/runtime/hardware_watchdog.py`, `tests/test_ap00_hardware_boundary_audit.py`, `src/NosAi.GuardAi.App/README.md`, `docs/STATO_IMPLEMENTAZIONE.md`, sette `docs/agents/phases/AP-*/AP-*_STATUS.md`, `docs/PROTOCOLLO_NOSTALE.md`, `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_player_skill_list.md` | **DONE — Present** (pytest 185 superati; le due voci aperte sopra sono riportate, non risolte) |
| Q-107 | Trasversale | A5 — **`ModuleReachability` esteso da un progetto a tutta la produzione**, su richiesta dell'utente. Il registro che dichiara quali moduli il runtime raggiunge davvero — e che un test ricalcola dal sorgente, fallendo se dichiarazione e realtà divergono — leggeva solo `src/NosAi.Runtime`. Ora legge tutto `src/` tranne `NosAi.GuardAi.App`, congelato da ADR-0025 §3. **Copertura: da 27 a 77 namespace, da ~50 000 a 94 698 righe.** 31 voci nuove, ciascuna con la motivazione di dove sta. **Uno scan parziale produceva verdetti falsi in entrambe le direzioni**, e ne aveva già prodotto uno: `NosAi.Host` era dichiarato `SuiteOnly` mentre il `Program.cs` del suo stesso eseguibile lo raggiunge — l'errore pessimista, quello che invita a cancellare codice funzionante. **Corretto anche il matcher**: contava un namespace come referenziato su `ns + "."`, che combacia anche con un namespace *più lungo*, quindi un genitore ereditava i referenti dei figli. Si è manifestato subito e in modo auto-referenziale: dichiarare `NosAi.Core.Planning.Goap` faceva sembrare raggiunto `NosAi.Core.Planning` dal registro stesso che doveva riportarlo irraggiungibile. Ora una menzione conta come uso di un tipo solo se il testo dopo il punto non completa un namespace dichiarato. **Risultato: 15,6% della produzione non è raggiunta da alcun percorso** (12 166 righe irraggiungibili, 2 625 raggiunte solo dal registro delle suite di certificazione). Le voci più pesanti: `NosAi.ControlPanel` (5 368, non è debito — è un'applicazione), `NosAi.Adapter.DirectEngine` (1 973, i due metodi centrali rifiutano con `NotImplemented`), `NosAi.GuardClient` (1 006, non è debito — il suo consumatore è l'app congelata), `NosAi.Core.Scheduling` (887), `NosAi.Core.Hardware` (747), `NosAi.Core.Planning` + `.Goap` (384, cioè lo strato HTN/GOAP che il flusso canonico di `CLAUDE.md` prescrive), `NosAi.Core.CharacterControl` (144, include `FailClosedCharacterActionGuard`, una guardia di sicurezza su nessun percorso). | **Claude** | Q-106 | `src/NosAi.Runtime/Observability/ModuleReachability.cs`, `tests/NosAi.Runtime.Tests/ModuleReachabilityTests.cs` | **DONE — Integrated** (build 0 errori/0 avvisi; Core 700 superati/1 saltato/0 falliti; Runtime 2335/3/0). Il registro è ora la fotografia dell'intera produzione e un test la tiene aderente al sorgente |
| Q-108 | AP-05 | A3+A4 — **`FailClosedCharacterActionGuard` cablato**, su richiesta dell'utente. Era una guardia di sicurezza completa e testata che nessuno chiamava, e una guardia che non rifiuta nulla è indistinguibile da una che funziona. **Il buco era reale e verificato prima di cablare:** né `CheckTargetConstraints` né il commit point dentro `GatedInputBackend` leggono `WorldFact.ObservedAtUtc` — il commit point rivalida geometria, foreground, proprietà del punto, scala e latenza di emissione, tutte proprietà di *dove finisce il click*, nessuna dell'età dell'osservazione da cui l'atto è stato deciso. Un bersaglio con posizione vecchia di minuti passava ogni controllo in silenzio. Ora `EngageCommand.JudgeTarget` chiede prima al mondo se permette l'atto (`CheckTargetConstraints`) e poi, per ultimo, se la fotografia da cui quella risposta è venuta è in grado di darla. **Ogni campo del contesto viene da un valore osservato**: `IsSafetyGateOpen` è `GatedInputBackend.IsLive`, l'età e la confidenza vengono dal fatto `Mob.Position` del bersaglio — fabbricarne uno avrebbe trasformato l'unico componente il cui scopo è rifiutare in uno che permette sempre. **Il limite di età è diventato un parametro del contesto** (default 500 ms invariato): 500 ms è giusto per un canale che ridichiara il mondo di continuo ed è sbagliato per uno che annuncia un cambiamento e poi tace, dove un mostro fermo non viene più nominato finché sta fermo. A 500 ms `--engage` avrebbe rifiutato ogni bersaglio stazionario: fail-closed e inutile. `--engage` passa i 30 s che `TargetSelectionPolicy` ha già argomentato per gli stessi avvistamenti, invece di inventare un secondo numero. Il rifiuto porta i valori misurati accanto ai loro limiti (`position_age_ms=45000/30000`), perché la guardia risponde `bool` e riscrivere qui le sue condizioni sarebbe una seconda copia della politica. **Il registro `ModuleReachability` l'ha colto al primo cambiamento**, come previsto: `NosAi.Core.CharacterControl` da Unreferenced a Integrated. `NosAi.Core.Game` lo segue ma **per eredità, non per uso** — il registro lavora a granularità di namespace, e `GameFunctionCatalog` resta chiamato solo da `CharacterActionPlanner`, che nessuno chiama; la nota nel registro lo dice. | **Claude** | Q-107 | `src/NosAi.Core/CharacterControl/CharacterControlContracts.cs`, `src/NosAi.Core/CharacterControl/FailClosedCharacterActionGuard.cs`, `src/NosAi.Runtime/Tactical/EngageCommand.cs`, `src/NosAi.Runtime/Observability/ModuleReachability.cs`, `tests/NosAi.Core.Tests/CharacterControl/CharacterControlTests.cs`, `tests/NosAi.Runtime.Tests/EngageCommandTests.cs` | **DONE — Present** (build 0 errori/0 avvisi; Core 702 superati/1 saltato/0 falliti; Runtime 2340/3/0, +7 test). Non `Verified`: nessun operatore ha ancora visto il rifiuto su client vivo — `docs/TEST_RIMANDATI.md` T-13 |
| Q-109 | AP-05 | A3+A4 — **`SimulationEngine` legge quale abilità sta prevedendo**, su richiesta dell'utente. **Prima correzione a quanto avevo riferito:** non era codice morto. `Gate3ExecutionOrchestrator` lo tiene come campo e lo chiama per ogni candidato del ciclo reale; il mio grep precedente aveva escluso proprio quelle righe. Il difetto era un altro e peggiore: assegnava `mpDelta = -35` e `risk = currentMp < 35 ? 0.90 : 0.10` a **ogni** `UseSkill`, quale che fosse l'abilità, e `GuardPolicyEngine` rifiuta quando `RiskScore > 0.75` — quindi l'unico controllo quantitativo del gate di sicurezza decideva su un numero che ignorava il candidato. Un'abilità dal costo reale molto superiore era prevista sicura e autorizzata. **Ora il costo viene da una sorgente fornita dal chiamante** (`Func<int, SkillCost?>`, pura, mai un handle `GameReferenceDatabase`: stesso schema del classificatore di vnum). Senza sorgente **non si inventa un numero sostitutivo**: `PredictedOutcome` porta `UnmeasuredReason` e `GuardPolicyEngine` rifiuta fail-closed — la decisione di sicurezza resta nel componente di sicurezza invece di essere codificata in un valore di rischio che la simulazione dovrebbe scegliere. Nessuna sorgente è fornita oggi, ed è deliberato: il catalogo decodifica le tabelle skill, ma **quale colonna sia il costo MP non è mai stato riscontrato con un numero osservato in gioco**, e `CLAUDE.md` vieta di fidarsi di una decodifica non verificata su un percorso non diagnostico — questo alimenta il gate. **Seconda scoperta: la stessa assunzione dei 35 MP esisteva in un secondo posto**, `ActionPlanner.Plan` (`if (playerMp >= 35)`), che decide se proporre l'abilità: due numeri diversi per la stessa cosa sono il modo in cui un candidato viene proposto e poi rifiutato per un costo che il proponente non aveva considerato. Ora planner e simulazione ricevono la stessa sorgente dallo stesso punto. **Terza: correzione a una mia verifica sbagliata.** Avevo annotato `AP-05_A1_STATUS.md` dicendo che il letterale `SkillOrItemId = 201` non esiste, avendo cercato l'identificatore — ma è un argomento posizionale, e il letterale è a `Gate3Runtime.cs:450`. Il documento aveva ragione; la nota si corregge da sé e il letterale ha ora un nome e un commento che lo dichiara non confermato. | **Claude** | Q-108 | `src/NosAi.Runtime/Contracts/PredictedOutcome.cs`, `src/NosAi.Runtime/Gate3/Gate3Runtime.cs`, `src/NosAi.Runtime/Safety/GuardPolicyEngine.cs`, `tests/NosAi.Runtime.Tests/SimulationCostTests.cs` (nuovo), `tests/NosAi.Runtime.Tests/Gate3Tests.cs`, `tests/NosAi.Runtime.Tests/PostConditionWiringTests.cs`, `docs/agents/phases/AP-05/AP-05_A1_STATUS.md` | **DONE — Present** (build 0 errori/0 avvisi; Core 702 superati/1 saltato/0 falliti; Runtime 2349/3/0, +9 test). Non `Verified`: serve una sorgente di costi reale, che dipende dal riscontro della colonna nel catalogo |
| Q-110 | AP-05 | **APERTO — il self-test di Gate 3 è rosso 7 su 21 e nessuna suite lo esegue.** Scoperto mentre si verificava Q-109. `--gate3-test` (registro `CertificationSuites`) fallisce 7 controlli, e la causa è **una sola**, verificata: `ActionPlanner.Plan` ha guadagnato la guardia `if (!_goals.HasActiveGoal) return list;` e quei self-test costruiscono `new ActionPlanner()` / `new Gate3ExecutionOrchestrator()` **senza goal stack**, quindi il planner non propone nulla, il ranking torna vuoto e `RankCandidates(...)[0]` lancia `ArgumentOutOfRangeException`. I test xUnit equivalenti passano `goals: Hunting()`; questi non sono mai stati aggiornati. **Verificato che i 7 rossi precedono Q-109**: eseguiti a `b1fcf9a` con le modifiche in stash, stesso elenco identico. Non corretti qui perché scegliere il goal giusto per ognuno dei sette è una decisione su cosa ciascun controllo intende dimostrare, non una riparazione meccanica. **Il difetto sistemico è che nulla lo esegue**: `Gate1Tests.Gate1SuitePasses` fa girare la suite del Gate 1 dentro xUnit, e per Gate 3 non esiste l'equivalente — aggiungerlo lo renderebbe visibile, ma sarebbe rosso subito, quindi va fatto insieme alla correzione. **CHIUSO il 2026-09-07: 21/21 verdi, e la suite ora gira dentro xUnit.** La causa dei goal mancanti spiegava 5 dei 7; gli altri due erano premesse invecchiate in modo più interessante. **(a)** `FixedObserver` costruisce il suo `ObservedState` nel costruttore e lo restituisce per tutto il ciclo, quindi la lettura porta un istante *precedente* all'azione: la post-condizione la rifiuta con `mp_not_observed_in_window`, ed è la verifica che funziona — una lettura presa prima di un atto non può verificarlo. Aggiunto `FreshVitalsObserver`, che timbra quando osserva, come fa già `PostConditionWiringTests.CachedVitalsObserver`; `FixedObserver` resta intatto per i controlli che lo usano. **(b)** «An observed mismatch is a discrepancy» leggeva hp 1 / mp 1 dopo aver agito da 800 / 100, cioè un disaccordo con i *delta previsti* — che è come funzionava la verifica prima. Non più: le remarks di `Gate3Observation` registrano l'abbandono del confronto di una previsione con se stessa, e oggi risponde una carta di post-condizione. `UseSkillPostCondition` asserisce che il mana **sia sceso**, non di quanto, quindi un crollo a 1 la soddisfa e il ciclo veniva confermato: il controllo pretendeva `Failed` su un mondo che il runtime ha ragione di confermare. La contraddizione che la carta può davvero vedere è il mana che non si muove — abilità autorizzata, tasto premuto, e il mondo dice che non è successo niente. **Metà sistemica chiusa:** `Gate3Tests.Gate3SuitePasses` esegue ora `Gate3TestRunner.RunAllTestsAsync()` dentro la suite, come `Gate1Tests.Gate1SuitePasses` fa per il Gate 1 — che è la ragione per cui la suite del Gate 1 non è potuta marcire allo stesso modo. Sta in `ConsoleCaptureCollection` perché scrive molto sulla console, non perché la catturi. | **Claude** | Q-109 | `src/NosAi.Runtime/Gate3/Gate3Runtime.cs` (regione del test runner), `tests/NosAi.Runtime.Tests/Gate3Tests.cs` | **DONE — Integrated** (build 0 errori/0 avvisi; Core 702 superati/1 saltato/0 falliti; Runtime 2350/3/0; `--gate3-test` 21/21) |
| Q-111 | Trasversale | A5 — **Un tipo vivo con un gemello morto dello stesso nome, e il controllo che impedisce di posare di nuovo la trappola.** `ModuleReachability` dice quali moduli il runtime raggiunge; non poteva dire il pericolo che ne segue. Un namespace irraggiungibile che condivide un nome con un tipo vivo significa che scrivere `using` su quel namespace e usare il nome **compila, gira, e prende silenziosamente la copia che nessuno chiama** — niente nel punto di chiamata sembra sbagliato. `TypeNameShadowingTests` (nuovo) scandisce tutta la produzione e fallisce su ogni ombreggiatura non dichiarata. **Trovate 12**, di cui 6 argomentate e deliberate (`DataSourceKind` e `ClassifiedValue` per ADR-0026; tre tipi di Gate 6, che è `SuiteOnly` per progetto; le due metà del bridge del Control Panel) e **6 che sono pericoli veri, elencati e non risolti**: `NosAi.Core.Planning` ne ombreggia tre da solo (`GoalId`, `GoalStack`, `RankedAction`), `NosAi.Core.Safety` ombreggia `RecoveryController` e `RecoveryState` — cioè proprio i due tipi che `Gate3ExecutionOrchestrator` compone — e `NosAi.Raids.Orchestration` ombreggia `ScreenPoint`. **Serve una decisione dell'utente, non del controllo:** in ogni caso il gemello morto è impalcatura che il runtime ha superato, ma `NosAi.Core.Planning` è l'unico codice HTN/GOAP del repository e il flusso canonico di `CLAUDE.md` nomina quello stadio, quindi cancellarlo è una scelta di prodotto; e i suoi test se ne andrebbero con lui (Planning 3 file, Safety 2, Raids.Orchestration 0). Il controllo resta verde e blocca ogni ombreggiatura **nuova**; un nome esce dalla lista solo quando la copia morta sparisce. **Esito, 2026-09-07.** Il controllo nuovo è stato **cancellato prima di consegnarlo**: `DuplicateTypeNameTests` esisteva già, con la stessa disciplina (lista dichiarata, motivo per voce, fallimento su una voce stantia), e consegnarne un secondo sarebbe stato il difetto che questa sessione passa il tempo a togliere — due posti dove la stessa politica è scritta. Allargato invece quello esistente, che aveva un difetto vero: `RuntimeTypes()` interrogava `typeof(DataSourceKind).Assembly`, cioè il solo `NosAi.Runtime`, e non poteva vedere `NosAi.Core` né `NosAi.ControlPanel` (che il progetto di test non referenzia, essendo un'applicazione WPF: nessuna reflection lo raggiunge). Ora legge il sorgente di tutto `src/`, lo stesso insieme di file di `ModuleReachabilityTests`. **Da 10 duplicati dichiarati a 18**, e la peggiore delle nuove è `SequenceGuard`: due politiche anti-replay diverse sotto un nome — contatore monotono stretto in `NosAi.Protocol`, finestra scorrevole da 1024 bit in `NosAi.Security`. È il danno che il commento di quella classe descrive con i due `SafetyGate`, su una primitiva di sicurezza, invisibile perché il controllo guardava un assembly solo. **Rimossi sei moduli** (scelta dell'utente): 356 righe con riferimenti a zero da produzione, test e suite. `ScreenPoint` è uscito dai duplicati da solo, e il test lo ha segnalato come voce stantia — il registro che funziona. **Restano aperti** i cinque gemelli morti che ombreggiano un tipo vivo (`GoalStack`, `GoalId`, `RankedAction`, `RecoveryController`, `RecoveryState`), `Goal` vivo in due namespace, e la decisione su `SequenceGuard`. | **Claude** | Q-110 | `tests/NosAi.Runtime.Tests/DuplicateTypeNameTests.cs`, `src/NosAi.Runtime/Observability/ModuleReachability.cs`, `docs/PIANO_DI_RIORDINO.md`, e i sei file rimossi | **DONE — Integrated per il registro; le rimozioni dei gemelli APERTE** (build 0 errori/0 avvisi; Core 702 superati/1 saltato/0 falliti; Runtime 2350/3/0; pytest 185) |
| Q-112 | Trasversale | A3+A4 — **Cablati i tre moduli irraggiungibili che colmavano un buco reale**, su richiesta dell'utente, dopo un inventario che per ognuno dei 21 ha verificato cosa fa, se esiste un concorrente gia' cablato e se c'e' un punto d'innesto. **(1) `Core.Statistics`**: `SimulationEngine` rispondeva `ExpectedTimeMs` con un letterale per tipo di azione mentre `Gate3Runtime.cs:723` misurava la durata reale a ogni round e la buttava. Il pezzo che rende il cablaggio non inerte e' che `ExpectedTimeMs` **non aveva alcun consumatore**: ora `TacticalRankingEngine` lo legge, ma solo quando e' misurato — ordinare sulla costante sarebbe stato ordinare su una preferenza fra tipi di azione travestita da misura. `ObservedActionDurations` e' limitato a 64 campioni per tipo (una `List` senza tetto era il difetto del modulo di telemetria rimosso lo stesso giorno) e rifiuta le durate non positive, che sono quelle di un effettore che non ha eseguito. **(2) `Runtime.Learning`**: il ciclo produceva previsione e verifica, le usava una volta e non ne teneva nessuna. Ora dichiara prima di ogni atto che la post-condizione reggera' e la salda contro la rilettura. Grandezza binaria con tolleranza mezza unita': prevedere una grandezza continua avrebbe voluto dire inventare una tolleranza. **Due difetti miei trovati dai test:** la provenienza si prendeva dalla *linea di base* della serie, cioe' da una lettura precedente all'atto, facendo sembrare Live una rilettura in cache — il laundering che la regola LIVE del ledger esiste per impedire; e un ritorno anticipato su esecuzione soppressa lasciava una previsione aperta per sempre in un loop che non si ferma (`PredictionLedger.Abandon` la chiude per identita'). **(3) `Core.WorldModel.Certification`**: la certificazione rispondeva un `bool` per l'intero prodotto. Ora `--certification-report` da' livello, evidenza e blocker per ciascuno dei 14 stadi, con due proprieta' strutturali: **nessuna esecuzione di questo processo puo' raggiungere `Verified`** (richiede il client reale, quindi il tetto e' `Integrated` e ogni stadio porta il blocker che lo dice), e **uno stadio che nessuna suite copre lo dichiara** invece di ereditare il verde di una suite che parla d'altro — cinque dei quattordici sono scoperti: Attach, MapDiscovery, Exploration, TargetRecognition, MultiStepQuest. | **Claude** | Q-111 | `src/NosAi.Runtime/Gate3/ObservedActionDurations.cs` (nuovo), `src/NosAi.Runtime/Testing/CertificationReportBuilder.cs` (nuovo), `src/NosAi.Runtime/Testing/CertificationReportCommand.cs` (nuovo), `src/NosAi.Runtime/Gate3/Gate3Runtime.cs`, `src/NosAi.Runtime/Learning/PredictionLedger.cs`, `src/NosAi.Runtime/Contracts/PredictedOutcome.cs`, `src/NosAi.Runtime/Program.cs`, `src/NosAi.Runtime/Observability/ModuleReachability.cs`, piu' tre file di test nuovi | **DONE — Integrated** (build 0 errori/0 avvisi; Core 702 superati/1 saltato/0 falliti; Runtime 2367/3/0, +17 test; `--certification-report` eseguito davvero: livello complessivo `Present`) |
| Q-113 | AP-10 | A3 — **Coperti i cinque stadi che il rapporto di certificazione dichiarava scoperti.** `ScenarioStageTestRunner` (13 controlli, suite `scenario`, `--scenario-test`) copre MapDiscovery, Exploration, TargetRecognition e MultiStepQuest; Attach resta scoperto per costruzione perché attaccarsi a un client reale non è simulabile, e il rapporto lo dice invece di ereditare il verde di un'altra suite. Il tetto resta `Integrated`: nessuna esecuzione senza client reale può dichiarare `Verified`. | **Claude** | Q-112 | `src/NosAi.Runtime/Testing/ScenarioStageTestRunner.cs` (nuovo), `src/NosAi.Runtime/Testing/CertificationReportBuilder.cs`, `src/NosAi.Runtime/Program.cs` | **DONE — Integrated** (`--scenario-test` 13/13; `--certification-report` 14/14 stadi `Integrated`, `IsFullyCertified` ancora falso) |
| Q-114 | Trasversale | A5 — **`SequenceGuard`: due politiche anti-replay sotto un nome, sui due capi dello stesso canale Gate 1.** `GuardAiClient` numera e valida col contatore monotono di `NosAi.Protocol` (rifiuta ogni salto); `NosAiHost` valida con la finestra scorrevole da 1024 bit di `NosAi.Security` (li accetta) — l'host è più permissivo di quanto quel client possa produrre. **Rinominati, non unificati** (`MonotonicSequenceGuard`, `SlidingWindowSequenceGuard`): quale politica imporre è una decisione di protocollo su un canale verificato una volta contro hardware reale non ritestabile, e stringere l'host alla cieca scambierebbe un'asimmetria nominata con un rischio non misurato. `SequenceGuardPolicyTests` la fissa. **Resta aperto** se l'host debba essere severo quanto il client. | **Claude** | Q-111 | `src/NosAi.Protocol/WireProtocol.cs`, `src/NosAi.Security/SequenceGuard.cs`, `src/NosAi.Host/NosAiHost.cs`, `src/NosAi.GuardClient/GuardAiClient.cs`, `tests/NosAi.Core.Tests/SequenceGuardPolicyTests.cs` (nuovo), `docs/PIANO_DI_RIORDINO.md` | **DONE — Integrated** (build 0/0; Core 706/1/0; Runtime 2367/3/0) |
| Q-115 | Trasversale | A5 — **Chiusi i cinque gemelli morti di Q-111, rinominando e non cancellando.** La cancellazione era la risposta ovvia ed era sbagliata: `NosAi.Core.Planning` è l'unico HTN/GOAP del repository e `NosAi.Core.Safety` il recovery reattivo — due stadi che il flusso canonico di `CLAUDE.md` nomina per esteso. Sono `PlannerGoalId`, `PlannerGoalStack`, `PlannerRankedAction`, `RetryBudgetState` e `RetryBudgetController`: il nome dice a che livello stanno, quindi un `using` su un namespace irraggiungibile non può più prendere in silenzio la copia che nessuno chiama. **Guasto trovato dalla rinomina**, in codice che nessuno leggeva perché nessuno lo chiamava: `_retries` è un `byte` e il controllo guardava solo il tetto — 256 fallimenti dopo il fermo riportavano il contatore a zero e `SafeStop` si scioglieva da solo, un fermo di sicurezza che cede quando le cose vanno peggio. Corretto leggendo lo stato prima del contatore. Registro dei duplicati **da 19 a 14**. **Resta aperto** se quei due strati vadano attaccati al runtime o tolti, e `Goal` vivo in due namespace. | **Claude** | Q-111 | `src/NosAi.Core/Planning/PlannerGoalId.cs`, `src/NosAi.Core/Planning/PlannerGoalStack.cs`, `src/NosAi.Core/Planning/IOrchestrator.cs`, `src/NosAi.Core/Planning/LexicographicOrchestrator.cs`, `src/NosAi.Core/Safety/RetryBudgetContracts.cs`, `tests/NosAi.Core.Tests/Planning/*`, `tests/NosAi.Runtime.Tests/DuplicateTypeNameTests.cs`, `src/NosAi.Runtime/Observability/ModuleReachability.cs`, `docs/PIANO_DI_RIORDINO.md` | **DONE — Integrated** |
| Q-116 | AP-05 | A2+A4 (**DeepSeek**, `54735af`) — **`--wire-inspect`: misurare un opcode invece di indovinarlo.** Nato da una mancanza sentita tre volte in un giorno: per specificare `lev` e `eq` serviva sapere cosa contengono davvero quei pacchetti, e non esisteva un modo di saperlo — `--world-replay` censisce ma non mostra una riga, `--live-decode` mostra le righe ma vuole il driver e una sessione viva. Ogni volta è servito un test usa-e-getta. Il comando ora fa due cose, entrambe offline e di sola lettura: stampa le righe grezze di un opcode (`--opcode`, `--max`), e censisce la **forma dei campi** — per ogni posizione, se il valore è sempre lo stesso o quanti distinti ne ha assunti. **Audit indipendente (Claude): il censimento coincide esattamente con la tabella calcolata a mano prima della consegna** — `cond 1=1 2=3443217 3=0 4=0 5=11`, `lev 1=56 2:23var 3=39 4:23var 5=18247900 6=185500 7=35106 8=7 9=0 10=0 11=1 12=0` — e i tre rifiuti (`recording_not_found`, `not_a_noscap`, `negative_max`) sono nominati. **Un difetto trovato e chiuso nello stesso passaggio**: `LiveWireMonitor.Monitor` ha ricevuto un parametro di provenienza (default `Live`, così il percorso col driver non cambia) perché dargli un file lo avrebbe fatto etichettare `Live` dei byte registrati — ma **nessuno lo passa**, perché il censimento ha bisogno delle righe nude e non del testo formattato che `Monitor` scrive. La specifica diceva «è a una riga di distanza, non riscriverla» e su quella metà **la specifica sbagliava**: le due forme d'uscita sono diverse e la duplicazione delle dodici righe di catena è la scelta giusta. Il parametro resta, e ora ha due prove che lo tengono onesto — il default è `Live`, e una sorgente registrata non esce mai etichettata `Live`. | **DeepSeek** + Claude (audit) | Q-115 | `src/NosAi.Runtime/Observability/WireInspectCommand.cs` (nuovo), `src/NosAi.Runtime/LiveIntegration/Capture/LiveWireMonitor.cs`, `src/NosAi.Runtime/Program.cs`, `tests/NosAi.Runtime.Tests/WireInspectTests.cs` (nuovo), `tests/NosAi.Runtime.Tests/LiveWireMonitorTests.cs` (+2, audit), `docs/PROTOCOLLO_NOSTALE.md` | **DONE — Integrated** (comando eseguito davvero su `nostale_combat.noscap`: censimento e `--opcode lev --max 3` verificati contro l'atteso) |
| Q-117 | AP-08 | A2+A4 (**DeepSeek**) — **`lev`: la progressione che il filo gia' portava.** `PROTOCOLLO_NOSTALE.md` diceva in una riga «level and XP from `lev` (catalogued, not yet published)», e il censimento contava quei 23 pacchetti fra quelli buttati. Ora `PlayerProgression` porta sei campi e `--world-replay` li stampa. **Verificato eseguendo (Claude): 23 letture, livello 56, esperienza 9688533..9690657, esperienza di lavoro 43226..43754, censimento da 8147/8211 a 8170/8211** — i numeri esatti che la specifica prevedeva. I campi dal settimo restano non decodificati: identici in tutti e 38 i pacchetti delle tre registrazioni che ne portano, mentre XP e job XP si muovono in ognuna. La specifica era stata corretta prima della consegna: la prima stesura diceva che `lev` stava in una sola cattura, e ne stava in tre. | **DeepSeek** | Q-116 | `NosTaleWorldProtocolDecoder.cs`, `GameTrafficObserver.cs`, `WorldChannelReplay.cs`, `WorldReplayCommand.cs`, `ProgressionFromLevTests.cs` (nuovo), `docs/PROTOCOLLO_NOSTALE.md` | **DONE — Integrated** |
| Q-118 | AP-07 | A2+A4 (**DeepSeek**) — **`eq`/`equip`: leggere dal filo cosa il personaggio indossa.** ADR-0027 aveva misurato che **nessun sito di produzione popola `Player.Equipment`**: ogni snapshot mai prodotto affermava che il personaggio non indossa nulla. Q-091 aveva concluso che serviva l'operatore a lanciare un test e mandare le righe reali — **quel test era gia' registrato**, `equip_test.noscap`, e i 43 pacchetti che il decoder scartava erano quel canale. **Verificato eseguendo (Claude): 12 letture, censimento da 3541/3584 a 3553/3584, e `slot equip visti: 0 2 4 5 6 8 9 10 11 12`** — l'unione mostra gli slot che sono andati e tornati, che e' l'informazione. La verifica non dipende da alcuna fonte esterna: tre vnum (309, 518, 284) escono da uno slot di `equip` ed entrano in uno di `ivn` dentro la stessa cattura, e un test fissa la corrispondenza. Cinque opcode restano deliberatamente non letti (`sc`, `pairy`, `bf`, `dir`, `inv`), ognuno col motivo misurato. **Errore nella specifica, di Claude**: diceva che `eq` produce sei slot, sono cinque — DeepSeek ha seguito i dati e non il documento, che e' la cosa giusta. | **DeepSeek** | Q-117 | `NosTaleWorldProtocolDecoder.cs`, `GameTrafficObserver.cs`, `WorldChannelReplay.cs`, `WorldReplayCommand.cs`, `WornEquipmentFromWireTests.cs` (nuovo) | **DONE — Integrated** |
| Q-119 | AP-09 | A2+A4 (**DeepSeek**) — **`--decide-replay --as-of-capture`: il comando non arrivava mai al pianificatore e non lo diceva.** Su 8211 pacchetti reali produceva sette cicli, cinque fermi a `player_vitals_stale`. La causa non era un difetto: `GameplayProvider.cs:555` sceglie di misurare la freschezza sull'ora del filo, e su una registrazione ogni lettura e' vecchia di mesi (ADR-0016). Il difetto era che l'uscita non lo diceva. `CaptureClock` avanza sui timestamp della cattura e non torna mai indietro; `Gate1ObservationChannel.FromPackets` prende il clock come parametro opzionale in coda, quindi i sei chiamanti non cambiano. **Il numero che la specifica non fissava di proposito, misurato ora: da `5 NoWorldState + 2 NoCandidate` a `7 NoCandidate`** — il pianificatore viene raggiunto in tutti i cicli e cio' che resta e' che non trova candidati. Le tre proprieta' reggono: provenienza `Cached`, `Acting enabled: False`, modalita' solo-file. Consegna rimasta non committata nell'albero, committata da Claude perche' non andasse persa. | **DeepSeek** | Q-116 | `CaptureClock.cs` (nuovo), `DecideReplayCommand.cs`, `Gate1ObservationChannel.cs`, `DecideReplayAsOfCaptureTests.cs` (nuovo) | **DONE — Integrated** |
| Q-120 | AP-09 | A2+A4 (**DeepSeek**) + A5 (**Claude**) — **`--outcome-report`: il registro degli esiti si scriveva e non si leggeva.** Quattro comandi scrivono in `ActionOutcomeLedgerStore`; `LoadByContext` non aveva alcun chiamante di produzione. Consegna corretta nella sostanza — `Contexts()` additivo, rifiuti nominati, esiti `Unknown` contati a parte con il contratto scritto accanto («Never folded into Failed»). **Un difetto trovato eseguendo (Claude), non leggendo: il comando creava il registro che diceva di leggere.** Aprire uno store SQLite lo crea, e `Run` apriva prima di guardare — quindi un comando di sola lettura scriveva sul volume dell'operatore, e la prima esecuzione trasformava «mai creato» in «esiste ed e' vuoto», cancellando una delle quattro distinzioni per cui il rapporto esiste. Ora il file si guarda prima di aprirlo, con `outcome_ledger_never_created` come risposta propria, e due prove lo fissano. Il `nosai.db` vuoto creato da quella esecuzione e' stato rimosso dal volume. | **DeepSeek** + Claude (audit) | Q-116 | `OutcomeReportCommand.cs` (nuovo), `ActionOutcomeLedgerStore.cs`, `Program.cs`, `OutcomeReportTests.cs` (nuovo, +2 di audit) | **DONE — Integrated** |
| Q-121 | AP-10 | A2+A4 (**DeepSeek**) — **la certificazione dichiara la natura della propria evidenza.** `--certification-report` dava 14 stadi su 14 a `Integrated`, ma sotto c'erano solo fixture: prova che la logica regge sulla forma del catalogo, non su cio' che il filo manda. I controlli sui byte registrati sono stati **affiancati** ai sintetici, non sostituiti, perche' i fixture provano la forma su ogni macchina anche senza `data/`. **Verificato eseguendo (Claude): `Exploration` e `TargetRecognition` riportano ora `evidenza: entrambe`, gli altri `sintetica`**, il tetto resta `Integrated` su tutti e ogni stadio porta il proprio blocker. | **DeepSeek** | Q-116 | `ScenarioStageTestRunner.cs`, `CertificationReportBuilder.cs`, `ScenarioOnRecordedBytesTests.cs` (nuovo) | **DONE — Integrated** |
| Q-122 | AP-05 | A2+A4 (**DeepSeek**) — **`CardReferenceDecoder`: il rimando esisteva, la destinazione no.** `SkillReferenceDecoder` decodificava gia' le `BCardApplication` che ogni abilita' applica, e delle cinque tabelle importate le due che spiegano gli effetti (`card`, `bcard`) erano le uniche senza decoder — proprio quelle a cui le altre puntano. Con il volume `NOSAI-SSD` collegato le 131 BCard sono nel catalogo reale, quindi l'incrocio si fa in casa. | **DeepSeek** | Q-116 | `CardReferenceDecoder.cs` (nuovo), `CardReferenceDecoderTests.cs` (nuovo) | **DONE — Integrated** |
| Q-123 | Trasversale | A5 (**Claude**) — **il volume dedicato, e cosa dipendeva dalla sua assenza.** Indagine partita da una riga che `--world-replay` stampava a ogni esecuzione e che nessuno aveva seguito: `reference catalog: nosai_ssd_not_found`. `Get-Volume` riportava il solo `C:`. Quattro cose dipendono dal volume — journal di Gate 1, mappe persistite, registro degli esiti, catalogo di riferimento — quindi finche' non era collegato **il progetto non ricordava niente fra un'esecuzione e l'altra**, e la capacita' che CLAUDE.md chiama *learn from failures* non aveva dove scrivere. Il codice si comportava bene (avvisa e prosegue, per scelta scritta); la conseguenza non era documentata da nessuna parte. L'operatore ha collegato il volume, e su sua istruzione i quattro database sono stati **allineati alla struttura documentata** (`<NOSAI-SSD>:\NosAi\data\db\`, `EXTERNAL_SSD_DEPLOYMENT.md` § 3): stavano in due posti, nessuno dei due quello descritto. **I file esistenti sono stati spostati, non abbandonati.** Trovati e corretti due difetti nel farlo: `OutcomeReportCommand` ricalcolava il percorso per conto suo e le due formule sono divergute (ora `VolumeLocator.TryResolveDatabasePath`, che risolve senza creare e senza lanciare), e `MapReconstructionSourceTests.Resolve_NoStoreAvailable_...` **passava per com'era fatto il PC** — il suo commento diceva che il volume «is not attached on this test host», e collegarlo lo ha reso rosso senza che una riga di produzione cambiasse. Ora l'etichetta del volume e' un GUID. | **Claude** | Q-116 | `VolumeLocator.cs`, `GameReferenceLocator.cs`, `OutcomeReportCommand.cs`, `MapReconstructionSourceTests.cs`, `EXTERNAL_SSD_DEPLOYMENT.md`, `TEST_RIMANDATI.md` | **DONE — Integrated** (build 0/0; Runtime 2451/9/0; Core 708/1/0; ControlPanel 105/0) |
| Q-124 | AP-10 | A2+A4 (**DeepSeek**) — **il registro dei rifiuti non coperti.** `src/NosAi.Runtime` dichiara ~190 costanti `…Reason`, il vocabolario con cui il runtime rifiuta per nome; 48 erano prodotte e nessun test le verificava, e 4 non erano prodotte da nulla. Nel gruppo scoperto c'erano i rifiuti del confine di attuazione — i percorsi che tengono chiusa la porta. Registro nello stile di `DuplicateTypeNameTests` (elenco dichiarato, un motivo per voce, rosso sia su una voce nuova sia su una diventata stantia) più copertura reale del gruppo dell'attuazione. | **DeepSeek** | Q-116 | `RefusalReasonRegisterTests.cs` (nuovo), `ActuationRefusalCoverageTests.cs` (nuovo) | **DONE — Integrated** |
| Q-125 | AP-05 | A2+A4 (**DeepSeek**) + A5 (**Claude**) — **i 481 pacchetti che non producevano nulla, e le 430 righe sane contate come rotte.** `--world-replay` stampava da sempre `senza osservazione: M` senza spiegarlo: 13,4 % su `equip_test` contro l'1,5–2 % di ogni altra cattura. DeepSeek l'ha scomposto in tre cause (opcode non letto, riga rifiutata, vuoto per progetto) più una quarta per ciò che non sa spiegare. **Audit eseguendo (Claude): 435 finivano in «riga rifiutata», che su una cattura sola sarebbe un difetto di decodifica grave.** Non lo era: 430 sono `mv` di **entità di tipo 2**, righe ben formate che il decoder salta di proposito, e ci finivano per il ramo `default` del classificatore. Quinta categoria «tipo entità non letto», e i numeri cambiano significato — `equip_test` passa a 431 saltate e **4** rifiutate, `nostale_combat` a **0** rifiutate: su 8211 pacchetti reali il decoder non sbaglia una riga, fatto che nessun numero diceva. **Corretto anche il commento di `IsReadableEntity`**, che affermava «Type 2 was never observed at all» mentre `equip_test.noscap` ne porta 430. | **DeepSeek** + Claude (audit) | Q-118 | `WorldChannelReplay.cs`, `WorldReplayCommand.cs`, `NosTaleWorldProtocolDecoder.cs`, `UnobservedBreakdownTests.cs` | **DONE — Integrated** |
| Q-126 | AP-05 | A1+A5 (**Claude**) — **i nomi del catalogo erano importabili e non importati.** `ReferenceImporter.ImportLanguage` esisteva, completo, con il rifiuto di sostituire una lingua con un'altra scritto nel commento — e **nessun chiamante di produzione**: la tabella `text` era vuota, ogni `name_key` (`zts10e`) irrisolvibile, e `--reference-info` non lo diceva. Ora lo dice, e `--client-updates --with-language IT` importa (opt-in di proposito: quel comando gira anche all'avvio e le tabelle di lingua stanno in un altro archivio). **Eseguito: 22 939 voci**, e i dodici vnum che le cinque registrazioni portano sul filo hanno un nome — Pecora felice, Pii morbido, Criceto felice, Volpe grande. **Un mio duplicato tolto prima di consegnarlo**: avevo scritto `LookupName`, e `DisplayName` esisteva già con lo stesso join — cercavo il nome che mi aspettavo invece del concetto. **E il conteggio doppio ha trovato subito un difetto**: 22 939 righe ma **102/2705 mostri risolti** (3,8 %). Le chiavi non combaciano — 2504 delle 2780 chiavi di testo portano un carattere di punteggiatura prima della `e` finale (`zts100*e`, `zts104 e`) che le `name_key` delle entità non hanno. Se sia un marcatore significativo o un artefatto di decodifica **non è deciso**: serve leggere i byte del file di lingua, ed è l'indagine successiva. Senza i due conteggi separati, «22 939 righe importate» sarebbe sembrato un successo pieno. | **Claude** | Q-123 | `ReferenceInfoCommand.cs`, `ClientUpdateCommand.cs`, `Program.cs` | **DONE — Integrated**; il 3,8 % **resta aperto** |
| Q-127 | AP-05 | A1+A5 (**Claude**) — **il 3,8 % dei nomi era una chiave scritta in due modi.** Seguito di Q-126. Letto il file di lingua byte per byte attraverso la catena vera (`NosArchive` → `NosDataTable.Decode`): `monster.dat` porta `zts1002e` e `_code_it_monster.txt` porta `zts100*e` — stessa posizione, un `2` da una parte e un segno di punteggiatura dall'altra. Il marcatore è **nel file**, non un artefatto di decodifica: la riga grezza è `zts100*e` + TAB + `Criceto v…`. Combaciavano solo le chiavi corte, quelle senza marcatore: 102. Regola a due passi — confronto esatto, e in mancanza confronto senza il carattere che precede la `e` finale da entrambi i lati — **misurata prima di scriverla: 2663 su 2705, zero ambigui**. Lo zero è la prova che conta: se il marcatore distinguesse due voci, toglierlo ne farebbe collassare due sulla stessa chiave, e non succede mai. Il codice ne risolve **2657** perché `LENGTH` di SQLite conta i byte, e il numero che vale è quello che il codice riporta. I dodici vnum delle registrazioni mantengono il nome che avevano; i mancanti rispondono — vnum 99 «Criceto velenoso debole», 2362 «Caverna dei Conigli». **Ambiguo resta null** (`HAVING COUNT(*) = 1`): scegliere uno di due nomi possibili sarebbe inventarlo. 42 entità restano senza testo e non sono indovinate. | **Claude** | Q-126 | `GameReferenceDatabase.cs` | **DONE — Integrated**; chiude l'aperto di Q-126 |
| Q-128 | AP-02 | A1+A3 (**Claude**) — **il regime di geometria su ogni riga dei campioni schermo.** Decisione dell'utente, implementata da Claude perché DeepSeek non era comandabile e il formato doveva esistere prima del ricampionamento. Un filtro c'era già — stessa larghezza e altezza dell'ultimo campione — e non poteva bastare: le due sessioni mescolate erano **entrambe 1024×768**. La riga porta ora sette campi, il settimo è il DPI, cioè il terzo componente di `GeometryShape`, tipo che il repository già definiva con il commento che dice a cosa serve. Il formato vecchio è **rifiutato intero** (`screen_samples_version_unsupported`), non letto mettendo DPI zero: fabbricare il regime sarebbe fabbricare l'unica cosa che serve sapere. Il rapporto dice **quale** regime ha scartato, non solo quanti campioni. Cinque prove, e quella che conta è la quarta: due regimi che differiscono **solo** per il DPI sono riconosciuti come due — la forma esatta della miscela reale, e l'unica che il formato precedente non poteva vedere. I dodici campioni esistenti non sono stati né indovinati né scelti per sottoinsieme: sceglierli a posteriori sarebbe scegliere i dati che confermano. | **Claude** | Q-123 | `ScreenProjectionProbe.cs`, `ScreenProjectionAutoCalibrator.cs`, `ScreenProjectionWatcher.cs`, `ScreenSampleRegimeTests.cs` (nuovo) | **DONE — Integrated**; il ricampionamento a zoom costante resta all'operatore (T-15) |
| Q-129 | AP-06 | A1+A5 (**Claude**) — **l'archivio di lingua ha dodici tabelle e ne leggevamo cinque.** Elencando `NSlangData_IT.NOS` per un'altra ragione il conto non tornava. Le sette non lette contengono, misurate aprendole: **374 nomi di mappa** («NosVille», «Prati di NosVille»), **3639 testi di missione** («Cattura un Pollo al di là del ponte»), **22 358 battute di NPC** e 222 nomi di punto mappa. **Il repository documentava il testo di missione e di dialogo come bloccato da OCR/ML**: la premessa era giusta (nessun opcode porta il testo), la conseguenza no — il testo sta nei file del client, in italiano. Importate tutte e sette: il catalogo passa da 22 939 a **50 704** voci. Tenute separate da `LanguageTables` perché non hanno un'entità dietro: sono testo indicizzato, e l'indice arriva da altrove. **Quello che non risolve, scritto nel codice**: avere il testo non è sapere quale testo vale adesso — gli id sul filo (`sayi`, `msgi`) non sono ancora incrociati, e nemmeno la corrispondenza id→chiave è stabilita (per i mostri è vnum+1, per queste serve il proprio riscontro). Nessun decoder toccato. | **Claude** | Q-127 | `ReferenceImporter.cs`, `DialogWindowStateComposer.cs`, `DEEPSEEK_TASKS.md` | **DONE — Integrated**; il collegamento id→testo **resta aperto**, con l'evidenza disponibile |
| Q-130 | Trasversale | A1+A5 (**Claude**) — **`Goal`: due concetti che si contendevano un nome.** L'ultimo «da decidere» del registro dei duplicati, deciso rinominando come i due `SafetyGate` e i due `SequenceGuard` prima di lui. Quello di `NosAi.Runtime.Autonomy` è **`HuntGoal`**: porta i vnum da cercare, un luogo e la motivazione che l'operatore legge; quello del World Model è l'obiettivo strategico di `NOSAI_AUTONOMOUS_PLAYER_SPEC.md` § 4.8. **Il costo era misurabile ed è sparito**: `GameplayObservationProjector` importava tre tipi per alias invece del namespace, e il commento diceva perché — importarlo avrebbe fatto collidere i due `Goal`. Quei tre alias erano l'unica prova che il duplicato costasse qualcosa. Rinomina verificata dal compilatore e non dal grep: rinominata la sola dichiarazione, la build ha indicato ogni uso reale — un file di produzione e cinque di test. | **Claude** | Q-128 | `Autonomy/GoalStack.cs`, `Gate3Runtime.cs`, `GameplayObservationProjector.cs`, `DuplicateTypeNameTests.cs`, `PIANO_DI_RIORDINO.md`, cinque file di test | **DONE — Integrated** |
| Q-131 | AP-05 | A2+A4 (**DeepSeek**) — **`su` porta la vita del bersaglio e il decoder si ferma al quarto campo.** `DecodeHit` legge attaccante e bersaglio (`fields[1..4]`) ed emette un `CombatHit` senza un numero dentro; `PROTOCOLLO_NOSTALE.md` documenta gli ultimi due campi come **confermati** dal 2026-09-06. Misurato prima di scrivere la specifica, su 212 pacchetti `su` di quattro catture: `fields[16] <= fields[17]` con `fields[17] > 0` **212 su 212**, `fields[17]` costante per id bersaglio su **40 bersagli distinti**, e `fields[11] == 1` equivalente a `fields[16] > 0` **senza un controesempio**. Quest'ultima e' il task: `fields[11]` e' un flag di presenza, e i **40 pacchetti (18,9%)** che hanno zero al campo 16 dicono *non riferito*, non *morto* — le stesse catture hanno **5** `die` in tutto. Un decoder che leggesse il campo senza il flag pubblicherebbe quaranta morti inventate. Secondo mezzo: `--wire-inspect --fields`, perche' le tre misure qui sopra sono state fatte in `awk` per il terzo task di fila, e perche' oggi un'opzione sconosciuta viene **ignorata in silenzio** (`--summary` ha stampato sei righe come se non fosse stato scritto). | **DeepSeek** | — | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_su_target_vitals.md` | **DONE — Integrated** (`7923f40`), verificato: `--fields` riproduce le misure della specifica, l'opzione ignota e' rifiutata |
| Q-132 | AP-09 | A2+A4 (**DeepSeek**) — **il runtime impara a ogni ciclo e dimentica a ogni chiusura.** `Gate3ExecutionOrchestrator` scrive nel `PredictionLedger` a ogni giro (`Predict` riga 1414, `Record` riga 1474) e **nessuno legge**: `CalibrationOf` e `Weakest` non hanno un chiamante in `src/`. Lo stato sono tre `Dictionary` in memoria — nessuna persistenza, la calibrazione muore col processo, e il runtime rifa' da capo a ogni avvio l'apprendimento del giorno prima. Quattro parti: export/import puri sul registro (**le previsioni aperte non si esportano** — riguardano un istante che non c'e' piu'), `PredictionCalibrationStore` sul modello di `ActionOutcomeLedgerStore`, `--learning-report` sul modello di `OutcomeReportCommand` (**mai l'accuratezza senza `Trials`**: 100% su due prove e 100% su duecento non sono la stessa affermazione), e il salvataggio **fuori dal ciclo di decisione**. Se il punto di chiusura del processo vivo non e' quello indicato, DeepSeek riferisce invece di inventare un ciclo di vita. | **DeepSeek** | — | `docs/agents/phases/AP-09/AP-09_A2A4_DEEPSEEK_learning_report.md` | **DONE — Integrated** (`5c97a01`), verificato: `--learning-report` dice `calibration_never_created` **e non crea l'archivio** |
| Q-133 | Trasversale | A5 (**Claude**) — **repository privato, calibrazioni versionate, copia fantasma eliminata.** `data/*` era escluso per intero: le calibrazioni -- la misura del client reale su cui poggia tutto cio' che il repository afferma sulla proiezione -- non erano su GitHub, e il 2026-09-07 sono quasi andate perse perche' il comando era partito da `C:\WINDOWS\system32`. Repo reso **privato** su richiesta dell'utente, `data/perception/*.calibration` e i campioni versionati. Trovata in `.claude/repair-main-20260905/` una copia completa del repository da **608 MB**, invisibile a git perche' `.claude/` e' ignorato: verificata prima di cancellarla -- stessa radice `40bde2f6`, main avanti di **334 commit**, e il suo unico commit proprio **cancellava 888 file** aggiungendo nove documenti AP-00 che main gia' conteneva in forma piu' ricca. Nessuna cattura, calibrazione o campione unico. Eliminata. | **Claude** | — | `.gitignore`, `data/perception/`, `.claude/` | **DONE** |
| Q-134 | AP-02 | A1+A3 (**Claude**) — **il campionamento schermo guida l'operatore invece di chiedergli cinque clic.** Una sessione da cinque e' stata rifiutata con `scale_not_determined:72x43pct`: i clic erano buoni (residuo 1,41 caselle contro i 2,43 dei dodici in archivio) ma cadevano tutti in una direzione e due erano lo stesso pixel -- dispersione 150 contro 893 000. `ScreenSampleCoach` applica quattro regole e dice cosa fare dopo ognuna: personaggio fermo, pixel non ripetuto, almeno sei caselle, e **px per casella implicati non sotto 8** -- soglia misurata, le sessioni buone stanno fra 11 e 41 e quella con la posizione letta in ritardo dava 1, 1, 2, 3, 3, 4, 7. Provata e scartata la regola «accordati con quelli gia' accettati»: sugli stessi nove clic i sette sbagliati arrivano per primi, si accordano, diventano la verita' e il gate butta l'unico buono. | **Claude** | Q-128 | `ScreenSampleCoach.cs`, `ScreenProjectionWatcher.cs`, `ScreenSampleCoachTests.cs` | **DONE — Integrated** |
| Q-135 | AP-02 | A1+A5 (**Claude**) — **il pannello di controllo e' la superficie dei test.** Regola dell'utente: se chiedo un test, il pannello dev'essere aggiornato prima e la rilevazione la piu' semplice possibile. Percezione ha ora la calibrazione schermo (stato letto dai file veri, raccolta, calibrazione, azzeramento, registro in diretta) e Rete ha «Registra il filo, e annota cosa hai visto» -- cattura e nota con lo stesso nome, per T-16. La radice del repository e' passata dentro il programma: **un pulsante non puo' sbagliare cartella**, che e' come i campioni erano finiti in `system32`. Trovato e corretto un difetto: «Registra equip (30s)» riscriveva `data/equip_test.noscap`, una delle cinque catture di riferimento, non versionata, copia unica. Aggiunto `Pannello.cmd`. | **Claude** | Q-134 | `MainWindow.xaml(.cs)`, `ScreenCalibrationInspect.cs`, `Pannello.cmd` | **DONE — Integrated** |
| Q-136 | AP-02 | A1+A3 (**Claude**) — **la proiezione schermo->mappa e' prospettica, e ora e' calibrata.** T-15 chiuso. Il residuo che il repository inseguiva da due giorni era errore di modello: dividendo ogni sessione fra meta' alta e meta' bassa della finestra il passo della casella cresce verso il basso in tutte e due (30,7->36,6 e 36,9->42,9 px), che e' una telecamera inclinata e nessuna affine puo' rappresentarlo. Coi due termini prospettici i dodici campioni del 3 settembre passano da 2,43 a **1,23 caselle**, e i termini misurati indipendentemente dalle due sessioni concordano (H = -0,0181 e -0,0153). Corretti per strada: l'incertezza calcolata con la matrice del modello sbagliato (13x35% contro 5x8%) e il DPI scritto come 0. **Calibrazione scritta**: passo 38,14 x 16,83, personaggio a (516, 445), 19 campioni su 20, il runtime la dichiara `usable`. Lo scarto dei fuori bersaglio passa per `ScreenProjectionAutoCalibrator`, che gia' esisteva -- ne avevo scritto una seconda copia, l'ho tolta -- con in piu' il tetto di un sesto deciso dall'utente. | **Claude** | Q-134 | `ScreenProjectionCalibration.cs`, `ScreenProjectionAutoCalibrator.cs`, `ScreenProjectionProbe.cs`, `TEST_RIMANDATI.md`, `PROTOCOLLO_NOSTALE.md` | **DONE — Verified** (calibrazione su client reale) |
| Q-137 | AP-09 | A1 (**Claude**) — **la calibrazione appresa sopravvive alla chiusura.** Q-132 aveva lasciato aperta la quarta parte: chi salva e quando. DeepSeek non ha inventato un ciclo di vita dove non ce n'era -- il `finally` che la specifica indicava sta dentro il gestore HTTP per richiesta, non alla chiusura del processo -- e ha riferito; era la scelta giusta. Il ciclo di vita c'era altrove: il registro vive nell'orchestratore, l'orchestratore vive in `Gate3DecisionLoop`, che e' gia' `IAsyncDisposable`. Rilettura in `Start`, scrittura in `DisposeAsync`, nessuna I/O dentro il ciclo di decisione. Archivio assente non e' un errore e il motivo finisce nel diario: «il runtime non ricorda» e «il disco non c'era» sono due cose diverse. | **Claude** | Q-132 | `Gate3DecisionLoop.cs`, `Gate1BootstrapHost.cs`, `Gate3LoopCalibrationLifecycleTests.cs` | **DONE — Integrated** |
| Q-138 | AP-06 | A2+A4 (**DeepSeek**) — **il testo dei messaggi di sistema e' in un archivio che non apriamo.** Meta' di T-16 e' chiusa: il campo 4 di `sayi` e' l'id del messaggio e il campo 6 il suo argomento, confermato su due catture con due vnum diversi e corroborato da `drop`, `get` e `ivn`. Manca il testo, e si sa dove non e': cercato `zts975?e` col carattere marcatore in **tutte e dodici** le tabelle di `NSlangData_IT.NOS`, per quattro id, escono solo voci scollegate. Nella stessa cartella c'e' `NScliData_IT.NOS` (313 790 byte) che nessuno ha mai aperto. Il task e' aprirlo e **dimostrare** che la tabella e' quella: il messaggio 975 compare in due catture, in entrambe subito dopo un `get` e con un vnum come argomento, quindi il suo testo deve parlare di raccogliere un oggetto. Se non ne parla, la tabella non e' quella. | **DeepSeek** | — | `docs/agents/phases/AP-06/AP-06_A2A4_DEEPSEEK_client_message_text.md`, `NosDataTable.cs`, `ReferenceImporter.cs` | **DONE — Integrated** (`a1b0842`). **Risultato in due tempi.** Primo giro: `NScliData_IT.NOS` aperto, `conststring.dat` trovato, e DeepSeek ha riferito che l'id `sayi` **non** lo indicizza — ne' diretto, ne' a scarto costante. Risultato negativo corretto, confermato due volte da Claude, e giustamente non mascherato da lookup. Secondo giro (addendum): il difetto era nel lettore, non nella tabella — `NosDataTable.Parse` saltava il byte di lunghezza anche quando quel byte era il **marcatore di numero impacchettato** (`0x8N`), e le chiavi da 100 in su uscivano come testo XOR. Corretto distinguendo il nibble alto. **Misurato dopo l'integrazione**: catalogo IT da **50 704 a 58 149 voci**, `conststring=7445`; 10666 -> «Hai raccolto [%s]:», 3099 -> «e' raccolto.», 1 -> «OK», e 975/654 correttamente **assenti**. Il collegamento `sayi`->testo resta aperto per costruzione: si chiude con una coppia osservata, che il pannello raccoglie in Rete -> «Registra il filo, e annota cosa hai visto» |
| Q-139 | AP-05 | A1+A3 (**Claude**) — **l'avvistamento porta la specie, e il tipo 2 entra nel World Model.** Il layout del tipo 2 era stato stabilito (2688 passi di `mv` con la stessa mediana e lo stesso novantesimo percentile dei mostri, quattro vnum che il catalogo risolve) e restava fuori per una ragione buona: quelle entita' sono un varco, due NPC e un pet, e ogni avvistamento veniva etichettato `"Monster"`. Ora la specie viaggia con l'avvistamento — `MonsterKind` / `BystanderKind`, letta dal campo del pacchetto e mai dedotta — arriva fino a `SelectableEntity`, e `Assess` rifiuta gli astanti con `target_is_a_bystander`, **dopo** le due prove per evidenza: se un astante ci ha colpito, quello che e' successo pesa piu' di come e' etichettato. Perche' la specie e non il catalogo: le quattro entita' hanno RaceType 0, 3, 2, 3 e nessuna l'8 che `IsSpecialNonMonsterEntity` cerca — il catalogo le chiamerebbe mostri. **Misurato**: «tipo entita' non letto» da **431 a 0** su `equip_test`, da **17 a 0** su `nostale_combat`; su `messaggi` restano 133 movimenti, tutti di tipo 1. Il World Model vede ora 33 astanti accanto a 176 mostri. Due test aggiornati invece che cancellati. | **Claude** | — | `GameTrafficObserver.cs`, `NosTaleWorldProtocolDecoder.cs`, `TargetSelector.cs`, `TargetEstablishment.cs`, `GameplayProvider.cs`, `WorldReplayCommand.cs`, `BystanderTargetingTests.cs` | **DONE — Integrated** |
| Q-140 | AP-05 | A2+A4 (**DeepSeek**) — **`out`: l'entita' esce, e uscire non e' morire.** Il decoder legge quindici opcode e `out` non e' fra questi: viene scartato in silenzio. Misurato il 2026-09-08 su tutte e sei le catture: `out` compare in **una sola** (`messaggi.noscap`), **18 pacchetti**, **due campi su 18 su 18**, tipi entita' **1 (7), 2 (5), 3 (6)** — gli stessi tre che il decoder ora legge — **15 id distinti**, di cui **13** gia' visti in `in`/`mv`/`st`/`su`/`ct`/`die` della stessa cattura. La forma e' `out <tipo> <id>`: **gli stessi due campi con cui `in` comincia**. **Il punto del task e' cosa NON fare**: le stesse diciotto occorrenze convivono con **due** `die` in tutto, quindi un `out` mappato su `EntityDeath` farebbe riportare **venti** uccisioni invece di due — diciotto vittorie mai avvenute, lette da chiunque conti le uccisioni o insegua un obiettivo `Kill`. Serve un genere di evento nuovo. Perche' cambia qualcosa: `GameplayProvider` lascia cadere le entita' per **scadenza a tempo** (`Expire`, `MaxEntityRetention` = **60 s**), quindi un'entita' andata via resta un bersaglio candidato per un minuto; `out` sostituisce quell'attesa con un'affermazione del server. Secondo mezzo, stesso schema di Q-131: `--wire-inspect` stampa il censimento o un opcode alla volta e **non sa mostrare i pacchetti in ordine cronologico attraverso opcode diversi**, percio' «un'entita' uscita riappare dopo?» non e' misurabile oggi. **Addendum dello stesso giorno — il campo 5 di `su` e' il vnum della skill, e passa da `probable` a confermato.** Misurato su **282 pacchetti `su`** di otto catture: attaccante di tipo 1 -> **sette** valori distinti (220x63, 200x34, 226x13, 222x3, 224x2, 228x2, 223x1); tipi 2 e 3 -> **sempre `0`**, 164 su 164, zero eccezioni. Tutti e sette esistono nella tabella `skill` del catalogo del client (Ritmo, Colpo di base, Colpo furioso, Colpo preciso, Energia della spada, Terremoto, Attacco Doppio); lo `0` non c'e'. Tre strutture indipendenti che il filo non poteva sapere: i due giocatori hanno repertori **disgiunti** ({220,222,223,224,226,228} e {200}); 200 e 220 sono entrambi in **posizione 0** della rispettiva classe — l'attacco base di due classi diverse, esattamente la coppia dei due giocatori; e il rapporto `ct`/`su` e' **1,00 per sei valori su sette**, con l'unica eccezione a **6,50** su `226`, che nel catalogo e' **l'unica delle sette con raggio d'area** — un lancio, molti colpi. `ct` porta lo stesso insieme al campo **7** (non al 6). Limite dichiarato: nell'intervallo 0-228 `monster` e `card` rispondono a 229 vnum su 229, quindi la sola presenza sarebbe debole — cio' che rompe la parita' e' che i vnum di mostro davvero visti in quelle catture sono `{2,9,20,24,36,39,40,45,48,49,96,333}`, **intersezione vuota**. `ct[5]`/`ct[6]` restano `Unknown` per scelta. | **DeepSeek** | — | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_out_entity_left.md` | **GIA' FATTO — Integrated** (`550d8ac`). Riassegnato per errore il 2026-09-08 e delegato davvero: DeepSeek ha riferito che il worktree conteneva gia' l'implementazione e **non ha scritto nulla** (confronto SHA-256 del server: nessun file toccato). Verificato da Claude nel sorgente: `GameEventKind.EntityLeft = 5` (`GameTrafficObserver.cs:38`), dispatch `"out"` -> `DecodeLeave` (`NosTaleWorldProtocolDecoder.cs:123`, `:321`), `--timeline` (`WireInspectCommand.cs:72`, `Timeline()` a `:256`), e i tre file di test. **La misura della Parte 5, che mancava, ora esiste**: `--wire-inspect data/messaggi.noscap --timeline in,mv,st,out` su 20 992 righe -- degli 8 id che ricompaiono su 18, per tipo sono 5 su 6 fra i mostri contro 1 su 7 fra i giocatori, quindi `out` e' uscita dalla vista e non sparizione |
| Q-141 | AP-06 | A1+A3 (**Claude**) — **la tabella `text` si rilegge, e il pannello mostra le due meta' di T-16 insieme.** `GameReferenceDatabase` aveva su quella tabella solo `ImportText` e `TextCount`: scrittura e conteggio, **nessuna lettura**. I nomi delle entita' la raggiungono da un'altra strada (`DisplayName` parte da un vnum e passa per `entity.name_key`) che le tabelle di solo testo non hanno, non descrivendo nessuna entita': le **7445** righe di `conststring` importate lo stesso giorno erano contate e illeggibili. Aggiunti `TextValue`, `SearchText` e `TextCount` per tabella, di sola lettura, con `ESCAPE` perche' una nota scritta a mano puo' contenere `%` senza volerlo dire. Nel pannello, appena la registrazione finisce, compaiono gli id che il filo ha detto **e** le righe del catalogo che contengono le parole della nota — le due meta' nello stesso momento, che e' l'unico in cui la seconda esiste. **Il pannello non collega niente**, e sette test lo fissano. Due scelte che valgono il pezzo: solo `sayi` e non `msgi`, il cui tracciato non e' misurato (sette campi contro nove; leggerlo con gli indici di `sayi` darebbe un id pari a zero mostrato accanto a quelli veri) — difetto scritto e corretto prima del commit; e il **campo 5 decide se cercare il nome**, perche' senza guardarlo l'argomento 50 del messaggio 697 uscirebbe dal catalogo degli oggetti con un nome plausibile e falso. | **Claude** | Q-138 | `GameReferenceDatabase.cs`, `WireMessageInspect.cs`, `MainWindow.xaml(.cs)`, `GameTextReadbackTests.cs`, `WireMessageInspectTests.cs` | **DONE — Integrated** (`bf499fe`, `4bdbd46`) |
| Q-142 | AP-05 | A2+A4 (**DeepSeek**) — **il catalogo delle abilita' incontra il filo.** Task grande, sei misure e due costruzioni, **da prendere dopo Q-140** (usa lo strumento cronologico della sua Parte 4 e tocca lo stesso `Program.cs`). `SkillReferenceDecoder` esiste dal 2026-09-06, decodifica **trenta campi** per abilita', ed e' **codice morto**: nessun file sotto `src/` chiama `Decode`, solo i test. Il suo tracciato viene da una fonte di comunita' e le sue stesse remarks dichiarano che **otto campi su trenta** sono incrociati e tutto il resto e' provvisorio. Fino al 2026-09-08 non c'era modo di fare di meglio; ora c'e', perche' il campo 5 di `su` e' il vnum dell'abilita' usata. **La misura piu' importante e' una contraddizione gia' trovata**: il decoder mappa `CpCost = COST[0]` e `MpCost = DATA[8]`, che per le cinque abilita' non base danno numeri diversi (5/7/8/15/7 contro 15/25/30/28/22); il filo indica `COST[0]` — la raffica di 226 cade fra due letture di `stat` che distano esattamente **15** — ma la finestra e' sporca perche' la rigenerazione risale a scatti di **+24**, quindi non conclude. Secondo riscontro trovato scrivendo la specifica: **`sr <n>` porta 0, 2 e 6**, che sono esattamente i `TYPE[1]` delle abilita' usate in quelle catture (220->0, 222->2, 226->6) — se regge, promuove `TYPE[1]` e rende misurabile la ricarica come tempo fra l'uso e l'`sr` della sua posizione. Le altre misure: portata contro le posizioni note (scartando quelle stantie), raggio d'area contro il rapporto `ct`/`su` (**6,50** per 226 contro **1,00** per le altre sei), classe contro i repertori disgiunti dei due giocatori. Poi `SkillCatalogue` e `--skill-report`, con il vincolo che **un campo provvisorio non si legga come confermato** e che **nessun campo si rinomini su un indizio**: se A1 non decide, `MpCost` e `CpCost` restano come sono e il report dice perche'. Nessun consumatore nella pianificazione: sapere quanto costa un'abilita' e decidere di usarla sono due decisioni diverse. | **DeepSeek** | Q-140 | `docs/agents/phases/AP-05/AP-05_A2A4_DEEPSEEK_skill_catalogue_meets_the_wire.md` | **GIA' FATTO — Integrated** (`df8c726`). Verificato il 2026-09-08: `src/NosAi.Runtime/GameData/SkillCatalogue.cs` e `src/NosAi.Runtime/Observability/SkillReportCommand.cs` esistono, `--skill-report` e' registrato in `Program.cs:1171`. Non ridelegato |
| Q-143 | AP-02 | A2+A4 (**DeepSeek**) — **il pannello dice il falso in quattro punti**, e si puo' fare **in parallelo a Q-140** (nessun file in comune). Da un audit del 2026-09-08 che ne ha trovati quindici; Claude ne ha corretti cinque (`a976c66`), questi sono i piu' gravi dei rimasti. **T5 e' sempre `Blocked`**: cerca `mapWorld`/`entities` alla radice dello snapshot, dove non ci sono — stanno in `client.gameplayBaseline.value.entities` — e mostra come evidenza un percorso inesistente, quindi il test fallisce anche con la lista piena. **T8 dichiara `character_inventory_state_not_published`** mentre `ToWire()` pubblica `inventory` con kind, slot, vnum, quantita' e rarita', popolato dal vivo. **Un valore mai osservato si disegna come cella vuota**: `Unwrap` fa `ToString()` su un `JsonValueKind.Null` e butta il `failureReason` che sta nello stesso oggetto — e sulla stessa vista la provenienza mostrata e' una stringa scritta a mano, mentre il campo `source` del JSON (LIVE/DERIVED/CACHED/SIMULATED) non viene mai letto: un valore CACHED e' indistinguibile da uno LIVE. **Confidenza e rischio sono due `switch` costanti** stampati come `Confidence 50% · Risk 50%`, e nessuno dei due dati esiste nel runtime — la specifica vieta di inventare una formula: il dato non c'e', e va detto UNKNOWN. | **DeepSeek** | — | `docs/agents/phases/AP-02/AP-02_A2A4_DEEPSEEK_panel_tells_the_truth.md` | **GIA' FATTO — Integrated** (`cfa22ff`). Riassegnato per errore e delegato davvero: le quattro parti risultano gia' chiuse -- la logica dei verdetti e' confluita in `PracticalTestCenter.cs`, e `CognitiveMemoryWindow` stampa `UNKNOWN (nessuna misura)` invece delle percentuali. DeepSeek non ha modificato codice di produzione e ha prodotto sette test nuovi; Claude li ha **messi da parte senza integrarli**, perche' tutti e sette duplicano test gia' presenti (`PracticalTestCenterVerdictTests.cs`, `LiveFieldRenderingTests.cs`, `CognitiveDecisionDisplayTests.cs`) |
| Q-144 | AP-02 | A2+A4 (**DeepSeek**) — **Percezione, Mappa e Bersaglio: cinque fatti storti**, parallelo a Q-140 e Q-143 (nessun file in comune con nessuno dei due). Dallo stesso audit. **«Ritagli HUD» e' marcato LIVE anche quando non e' stato scritto niente**: `HudCropWriter.TrySave` restituisce la cartella incondizionatamente e `WriteBmp` esce senza scrivere quando il ritaglio e' vuoto, cioe' ogni volta che la ROI cade fuori dal fotogramma — su disco convivono bitmap di due sessioni diverse a sette ore di distanza sotto un'unica etichetta LIVE e senza data. **Un motivo costante falso**: nel ramo «nessun fotogramma acquisito» le righe HP dicono `ocr_glyphs_not_trained` mentre l'OCR non e' mai partito — anche con l'atlante addestrato direbbero cosi', mandando a indagare la cosa sbagliata. **«Identita' verificata» non puo' mai essere si'**: `MapInspect` passa sempre `currentIdentity: null`, quindi «griglie identiche al manifest» e «impronta del client ignota» danno la stessa stringa, mentre il ritaglio 31x31 viene disegnato proprio da quelle griglie — e meta' dell'identita' (777 file `.grid` piu' `maps.manifest`) e' calcolabile dal solo disco. **L'istante di osservazione viene rifabbricato con l'orologio del pannello** (`Cached(value, DateTime.UtcNow)`) mentre `observedAtUtc` e' nel JSON accanto al valore: latente oggi, ma `ObservationLastHp` e' l'etichetta con cui si addestrano i glifi HUD e quel percorso non sa quanto e' vecchia. **Due fatti veri letti e mai mostrati**: le quattro frazioni della ROI bersaglio e l'id di processo che decide se gli indirizzi nudi valgono ancora. | **DeepSeek** | — | `docs/agents/phases/AP-02/AP-02_A2A4_DEEPSEEK_perception_map_target_truth.md` | **GIA' FATTO — Integrated** (`9a9ae99`), **piu' un residuo chiuso il 2026-09-08**. Le cinque parti erano gia' fatte, `PerceptionMapTargetTruthTests.cs` incluso. Delegato davvero, DeepSeek ha trovato e corretto l'unico difetto rimasto: in `PerceptionProbe.cs` il fallback costante `"ocr_glyphs_not_trained"` della riga riassuntiva e' diventato `"unclassified"`, coerente con `BarField`/`FormatVital`. Verificato da Claude: `ScreenVitalReader` allega sempre un motivo, quindi il ramo era morto e, se raggiunto, avrebbe accusato l'OCR senza prove |
| Q-145 | Trasversale | A2+A4 (**DeepSeek**) — **tre rifiuti di `--unequip` senza prova ne' dichiarazione.** `RefusalReasonRegisterTests` era **rosso**: S8 aveva introdotto `unequip_equip_feed_unavailable`, `unequip_input_backend_not_gated` e `unequip_slot_not_resolved` senza ne' un test che le coprisse ne' una voce nel registro. Incarico scelto **partendo dalla prova che fallisce**, non da una riga di documento. DeepSeek ha verificato la raggiungibilita' di ciascuno e li ha dichiarati tutti e tre irraggiungibili, con la motivazione tecnica nella famiglia giusta; nessun test finto scritto per fingere copertura. Verificato da Claude nel sorgente, indipendentemente dalle citazioni del rapporto: `RunWindows` e' `private static` (i primi due sono fuori portata di qualunque unit test) e `RuntimeComposition.cs:50` costruisce sempre `GatedInputBackend`; per il terzo, `InventoryPanelRoiCalibration.Confirmed` lancia se manca un solo slot (`:124`), `Load` passa da `Confirmed` (`:245`), e `Resolve` restituisce `null` solo per calibrazione assente — gia' rifiutata prima con `NotCalibratedReason` — o area a estensione zero, esclusa dal controllo di risoluzione del passo 2. Il commento del codice di produzione lo dichiarava gia' difensivo. | **DeepSeek** | — | `tests/NosAi.Runtime.Tests/RefusalReasonRegisterTests.cs` | **DONE — Integrated** (build 0/0; Runtime **2731/2731 verdi** contando i due Guard rilanciati da soli; ControlPanel 159/0) |

## Regola per Q-014/Q-015/Q-016/Q-017/Q-018 e per tutte le fasi successive

I comandi dettagliati per i task oltre Q-008 **non sono ancora scritti di proposito**: scriverli ora, prima che i contratti di AP-01 esistano davvero, rischierebbe di fissare dettagli sbagliati che poi vanno disfatti (esattamente il tipo di "casino" da evitare). La regola è: **quando una voce `PENDING` diventa la prima della coda, Claude scrive il suo comando dettagliato (stile dei file già prodotti oggi, non gli stub telegrafici originali) prima di farla partire**, poi la esegue lui stesso o la assegna a DeepSeek a seconda della colonna Esecutore.

## Fasi successive — nessuna: AP-00→AP-10 hanno tutte almeno un task A1 in coda

Dalla roadmap canonica (`docs/ROADMAP_ESECUTIVA.md`). AP-03 è `Integrated` (Q-024); AP-04 ha A1+A3 `Present` (Q-025/Q-026/Q-027, eccezione dichiarata) e **A2+A4 consegnati da DeepSeek il 2026-09-06** (Q-028/Q-029: `MovementVerificationProjector` + comando operatore `--scout` — `ActuationAuthority.Commanded`, chiama `WalkCommand.Execute` reale con il solo parametro opzionale additivo `onStepVerified`). Routing multi-mappa via portali resta rimandato (nessuna fonte dati reale, vedi `AP-04_A1_STATUS.md`). Non è un bridge verso `Gate3Runtime` (indagine conclusa: quella pipeline è chiusa/hardcoded e le mancano tre pezzi reali, lavoro futuro di AP-08 "Strategic Autonomy + HTN", non di AP-04). **A5+A6 completati (Q-043/Q-044, 2026-09-07)**: audit indipendente ha trovato 1 difetto reale (`onStepVerified` invocato anche per un passo mai emesso, un rifiuto guard a metà cammino — origine nella specifica di Claude, non una deviazione di DeepSeek), corretto in integrazione con un test di regressione dedicato. **AP-04 è ora `Integrated`.** Vedi `AP-04_STATUS.md`.

AP-05 ha A1 `Present` (Q-030) e A3 parziale `Present` (Q-031: generazione candidati + hard constraints reali; simulazione/combo rimandati, nessun dato reale di danno/costo skill). Indagine su `Gate3Runtime` per skill/attacco **conclusa**: stessa classe di problema del movimento (selezione skill hardcoded, predizione placeholder, Guard che decide su rischio fabbricato) — nessun bridge verso `Gate3Runtime`. A differenza del movimento, non esiste un `WalkCommand`-equivalente già reale per l'esecuzione combattimento, e la verifica reale che esiste (`PostConditions.cs`) è accoppiata a metodi privati di `Gate3Runtime` non riusabili; verificare un attacco riuscito richiederebbe anche l'HP del mob bersaglio, mai fuso nel World Model canonico (gap AP-02, OCR/ML). **Decisione presa (Q-037): percorso (a)** — verifica combattimento solo-vitali-player, onesta ma parziale (conferma il costo risorsa, non il colpo sul bersaglio), scelta perché il percorso (b) resta bloccato indefinitamente sul gap OCR/ONNX. Contratto mancante `CombatExecutionEvidence`/`CombatExecutionResult` scritto e testato; **A2+A4 consegnati da DeepSeek il 2026-09-06 (Q-038/Q-039, `--engage`)** — `CombatVerificationProjector` + `EngageCommand` (esecuzione `UseSkill` via keybind confermato + `GatedInputBackend`, verifica `TryReadPlayerVitals` prima/dopo). Limiti dichiarati nel comando: `BasicAttack` rifiutato per design (nessun costo risorsa osservabile), nessun ponte al Safety Gate reale per un atto senza pixel (il gate con commit point richiede uno scope per atti puntuali; il round riporta il rifiuto onestamente — verificato vero da A5, non solo dichiarato). **A5+A6 completati (Q-045/Q-046, 2026-09-07)**: audit indipendente ha trovato 1 difetto reale (`Run` lanciava un'eccezione non gestita su un argomento vuoto invece di `[REFUSED]`), corretto in integrazione con 3 test di regressione dedicati. **AP-05 è ora `Integrated`.** **Addendum `--recover` (Q-050/Q-051/Q-052/Q-054/Q-055)**: risposta Survival reale (`UseConsumable`, sibling di `--engage`), consegnata da DeepSeek e integrata senza correzioni necessarie. Vedi `AP-05_STATUS.md`.

AP-06 ha A1+A3 `Present` (Q-032, eccezione dichiarata: dipende solo da AP-01). Grafo quest (obiettivi tipizzati, prerequisiti, reward) e logica di avviabilità/soddisfazione obiettivi reali, senza dati inventati. `Quest`/`QuestObjective` (AP-01) lasciati invariati per lo stesso principio già seguito in AP-04/AP-05. Indagine mirata su "semantic extraction OCR/UI/**network**" (Q-040): il canale network per `Collect` è reale e già fuso (`ivn`/`get`/`drop` decodificati, `GameplayObservationProjector` già `Integrated`) — non bloccato come l'OCR/ML lo è per gli altri obiettivi. `Travel` non richiede nulla di nuovo (`--walk` già lo fa); `Kill`/`Dialogue`/`Interact`/`Deliver` restano bloccati (nessuna classificazione mob/NPC da rete, nessuna primitiva di interazione NPC). `CollectCommand` consegnato da DeepSeek (Q-041) e **integrato (Q-047/Q-048)**: A5 ha trovato 1 difetto reale (stesso pattern di AP-05: `Run` lanciava un'eccezione su `vnum` vuoto), corretto in A6. **AP-06 è ora `Integrated`.** Limite dichiarato: nessuna scoperta automatica di ground item (estensione futura), e lo stesso limite di occupazione condiviso con `--walk`/`--scout` (nessun feed live, vedi `AP-06_A5_AUDIT.md`). Vedi `AP-06_STATUS.md`.

AP-07 ha A1+A3 (parziale) `Present` (Q-033, eccezione dichiarata). `LoadoutPlanner.GenerateUnequipCandidates`/`GenerateUpgradeCandidates`/`CheckHardConstraints` reali da dati già noti; generazione automatica di candidati Equip bloccata (nessun dato di categoria item in `InventoryItem`); `LoadoutEvaluation` resta quasi tutta non popolata (nessuna statistica reale item/upgrade, stesso gap dati di AP-05) tranne `QuestRelevance`, calcolata incrociando onestamente il Quest Graph di AP-06. Indagine mirata su A2+A4 (Q-042): a differenza di AP-05/AP-06, qui **nessun** percorso onesto parziale esiste — né una primitiva di esecuzione (equip/unequip/upgrade non modellati nemmeno nel vecchio `ActionType`, nessuna hotkey, nessuna calibrazione UI pannello) né un canale di verifica rete (`InventoryKind` non distingue equipaggiato da zaino). Non specificabile per DeepSeek; serve prima infrastruttura nuova (calibrazione screen-space del pannello, o un opcode di rete equip mai identificato). **Seconda indagine (Q-084, 2026-09-06)**: un varco reale trovato — la calibrazione screen-space del pannello è ora specificabile con lo stesso schema di `TargetRoiCalibration`/`DialogRoiCalibration`; consegnato `InventoryPanelRoiCalibration` (Claude/A1). **Q-085**: `EquipmentSlot` corretto da segnaposto 8 valori ai 18 valori reali NosTale (fonte esterna incrociata). **Q-086 (2026-09-06, DeepSeek, livello `Present`)**: comando `--calibrate-inventory-panel` consegnato e riallineato ai 18 valori in integrazione — vedi riga Q-086 per l'evidenza completa. **Q-089 (2026-09-06): la calibrazione è ora `Verified`** — l'operatore ha confermato sul client reale la griglia 3×6 dei 18 slot ed eseguito il comando con successo (`InventoryPanelRoiCalibration` scritta, nessun `[REFUSED]`). Resta aperta solo la seconda metà di T-12: una cattura dedicata deve ancora confermare quale `InventoryKind` (`Wear=8` candidato) produce un equip reale; `--equip`/`--unequip` restano non specificabili finché quella non esiste. Vedi `AP-07_A1_STATUS.md`.

AP-08 ha A1+A3 (parziale) `Present` (Q-034, eccezione dichiarata): solo la tappa "Strategic Utility" (selezione del contesto strategico più urgente), da 3 dei 7 contesti della DoD con dati reali oggi (Survival, QuestUrgency, Exploration). Indagine mirata su A2+A4 (Q-049): la risposta Survival reale è lavoro AP-05 (`--recover`), il vero gap era l'assenza di un orchestratore — costruito su decisione esplicita dell'utente con ambito delimitato (Q-053, `--autoplay`, dispatch solo Survival/Exploration, tetto rigido di 20 cicli). **A5+A6 completati (Q-056/Q-057)**: 1 difetto reale trovato (footprint di esplorazione non propagato tra cicli), corretto. **AP-08 è ora `Integrated`.** HTN/GOAP restano non affrontati di proposito: dipendono dai gap di esecuzione ancora aperti in AP-07. Vedi `AP-08_STATUS.md`.

AP-09 ha A1+A3 `Present` (Q-035, eccezione dichiarata). Scoperta importante: `src/NosAi.Core/Memory/`+`Knowledge/` esistevano già, reali e testati (Adaptive Knowledge Expansion della DoD in gran parte già coperta) — non rifatti. Colmato solo un gap reale non coperto (Action-outcome ledger + simulazione locale deterministica onesta) ed estesa additivamente `MemoryType`. Segnalata, non risolta, una duplicazione preesistente (`KnowledgeScope`/lifecycle in due namespace diversi, stesso genere del gap `DataSourceKind` di AP-01/A5). **Indagine A2+A4 (Q-058)**: genuinamente bloccato per gap di contratto (nessun produttore di `WorldAction`, `Outcome` senza spazio per Unknown), non per wiring mancante. **Correzione applicata (Q-060)**: `ActionOutcomeLedgerEntry.Outcome` ora `WorldFact<ActionOutcome>`; nuovo `WorldActionProjector` proietta `CombatExecutionEvidence`/`MovementExecutionEvidence` in un `WorldAction` reale. **A2+A4 consegnato e integrato (Q-061/Q-063)**: `ActionOutcomeLedgerStore` (persistenza, stile `MapModelStore`) + wiring in `--engage`/`--recover`/`--scout`/`--autoplay` — solo nei rispettivi `RunWindows`, nessuna firma di `ExecuteOneRound`/`ExecuteOneCycle` toccata. `--collect` escluso di proposito (la sua evidenza prima/dopo non è né `CombatExecutionEvidence` né `MovementExecutionEvidence`). Nessun percorso di lettura wired in questo task. **A5 (Q-063): nessun difetto reale trovato** — seconda consegna DeepSeek pulita di questa sessione dopo `--recover`. **AP-09/A2+A4 è ora `Integrated`.** Vedi `AP-09_A1_STATUS.md`.

AP-10 ha A1 `Present` (Q-036): contratti di certificazione (`VerificationLevel`/`CertificationStage`/`CertificationStageResult`/`CertificationReport`) e il report onesto e citato dei 14 stadi della DoD — nessuno `Verified` per lavoro di questa sessione (i due marcati `Verified` ereditano quella classificazione dal sistema Gate 1-6 preesistente, dichiarato come non riverificato qui). `OverallLevel` complessivo = `Present`. **Aggiornato (Q-059)** dopo `--recover`/`--autoplay`: stadi 8 (Combat)/12 (Recovery) rivisti a `Integrated, parziale`; `OverallLevel` invariato (l'anello debole resta `Present`, sugli stadi 5/6/7/9/10/11); nessun secondo passo A2+A4 per AP-10 stessa — l'orchestratore candidato esiste già come `AutoplayCommand` (AP-08), il blocco reale resta l'assenza di un client Windows reale in questo ambiente. **Verifica dedicata stadio 14 (Q-062)**: `SqliteEventJournal` (hash-chain SHA256) confermato reale e cablato nel vero composition root Gate 1 (`NosAiHost`), non una libreria isolata — test rieseguiti freschi in questa sessione (`SqliteEventJournalTests` 6/6 incluso rilevamento manomissione, `NosAiHostTests` 2/2). Stadio 14: `Integrated`, non `Verified` (nessuna sessione hardware di questa fase ha ri-verificato un journal fisico — `STATO_IMPLEMENTAZIONE.md` stesso non promuove mai questo sottosistema oltre `Present`/`Integrated`). `OverallLevel` invariato. Stadi 1-2 restano non riverificabili in questo ambiente (Real-environment rule). Vedi `AP-10_A1_STATUS.md` per la tabella completa e le citazioni.

**Con Q-036 la sequenza AP-00→AP-10 ha almeno un task A1 (contratti fondanti) completato per ogni fase. AP-04, AP-05, AP-06 e AP-08 hanno A2+A4 consegnati e ora integrati (AP-04: `--scout`; AP-05: `--engage`+`--recover`; AP-06: `--collect`; AP-08: `--autoplay`, il primo orchestratore — tutte e quattro `Integrated`, un difetto reale trovato e corretto in ciascuna consegna DeepSeek tranne `--recover`).** Il lavoro reale che resta non è "quale fase manca" ma i gap dati/esecuzione già segnalati in ogni singolo status doc (OCR/ONNX, dati skill/item reali, fonte portali, riconciliazione `KnowledgeScope`/`DataSourceKind`, validazione contro un client reale per tutto ciò che è ancora `Present`/`Integrated`) — nessuno di questi si chiude scrivendo altro codice di fase, servono dati reali, hardware reale o una decisione esplicita dell'utente. AP-07/A2+A4 (Q-042) è stato indagato e trovato genuinamente bloccato — nessuna specifica DeepSeek prodotta, serve prima nuova infrastruttura (calibrazione UI pannello o un opcode equip identificato). **Q-086 (2026-09-06, DeepSeek, livello `Present`)**: il comando `--calibrate-inventory-panel` (calibrazione screen-space del pannello, 18 `EquipmentSlot`) è consegnato e riallineato all'enum reale in integrazione — riga Q-086 sopra, follow-up operatore in T-12 (`docs/TEST_RIMANDATI.md`).

## Nota ambiente (2026-09-06, Q-086) — suite Runtime: 5 fallimenti non correlati al diff

Su questo host la suite completa `NosAi.Runtime.Tests` riporta 2178/2183: 5 test rossi, **tutti estranei a Q-086** e nessuno tocca i file di Q-085/Q-086:

- `ScreenVitalsCaptureTests.Capture_WithAProcessId_OnANonWindowsHost_FailsClosed_BeforeTouchingClientWindowLocator` e `TryEnsurePipeline_OnANonWindowsHost_FailsClosed_WithDxgisOwnReason`: asseriscono `OperatingSystem.IsWindows() == false` (percorso fail-closed non-Windows), ma l'host è Windows — falliscono per costruzione qui; verdi su CI non-Windows.
- `CommitPointTests.InsideAScopeWithEveryConditionMetTheActReachesTheBackend`: fallito in una sola esecuzione su 3 (flaky; in alcune run rientrava tra i 3 non-Windows e non compariva tra i `[FAIL]`).
- `MapReconstructionSourceTests.Resolve_NoStoreAvailable_StillReconstructsInMemoryForTheSession`: il suo commento assume che il volume `NOSAI-SSD` **non** sia attaccato ("which is not attached on this test host"), ma su questo host `D:` risulta etichettata `NOSAI-SSD`: la costruzione apre davvero il DB sul volume, non logga alcun errore, e `Assert.Single(logger.Errors)` fallisce. Non è una regressione di Q-086 né di Q-085.

Da ri-verificare con il file in un checkout pulito (`c145e9e`/`f94e0be`) quando un operatore lo riterrà utile; il lavoro Q-086 resta `Present` (limite dichiarato nella specifica: serve comunque l'operatore per `Verified`).

## Criterio di avanzamento fase

Una fase (AP-XX) non inizia finché la precedente non è almeno `Integrated` (build+test combinati verdi) — mai `Verified` come precondizione di partenza, dato che `Verified` richiede hardware reale non disponibile in questo ambiente di sviluppo. Questo è il criterio già in uso oggi (AP-00 Q-006 come gate prima di iniziare AP-01 Q-007).

**Eccezione dichiarata (2026-09-05):** i soli contratti A1 di una fase possono partire prima che la fase precedente sia `Integrated`, quando dipendono solo da contratti di una fase ancora più a monte già `Integrated` (non da come la fase precedente li popola a runtime) — questo per permettere a Claude e DeepSeek di lavorare in parallelo invece che in sequenza rigida (istruzione esplicita dell'utente: "programmare tutti insieme e in multi-agente per finire prima"). Esempio concreto: AP-04/A1 dipende da `MapModel`/`Tile`/`TileCoordinate`/`TileTraversability`/`Portal`, stabili da AP-01, non dal lavoro ancora aperto in AP-03/A5-A6. L'implementazione A2/A4 di una fase resta invece gated: non parte finché la fase precedente non è `Integrated`, perché quella sì dipende dal comportamento a runtime, non solo dalla forma dei contratti.

## Chi fa cosa, in breve

**Principio (istruzione esplicita dell'utente, 2026-09-05): Claude gestisce il lavoro — ownership, contratti fondanti, audit, integrazione — ma continua anche lei a scrivere codice reale, su compiti più piccoli; DeepSeek riceve il carico più pesante di sviluppo/compilazione file. Lavorano in parallelo, non in sequenza rigida, per finire prima.** Dettaglio operativo completo in `docs/agents/DEEPSEEK_TASKS.md`.

- **DeepSeek: ATTIVO dal 2026-09-05, prende il ruolo di Cursor INTERAMENTE** — ogni riga "A2 Cursor"/"A4 Cursor" in `docs/agents/AGENT_COMMAND_REGISTRY.md`, per qualunque fase, è ora "A2 DeepSeek"/"A4 DeepSeek" (v1.1). Storicamente in questo progetto A2+A4 sono anche stati il carico di lavoro più pesante per fase (es. AP-03/A4 da solo: 8 file, 1421 righe) — coerente con l'assegnare a DeepSeek il lavoro più pesante. Appena il contratto A1 minimo di cui A2/A4 hanno bisogno esiste, DeepSeek parte subito, in parallelo col resto del lavoro di Claude sulla stessa fase — non in coda dopo. I task già completati da Claude "al posto di Cursor/DeepSeek" prima dell'attivazione (Q-002, Q-004, Q-008, Q-014→Q-018, Q-020, Q-022) restano `DONE`/`Integrated` così come sono — non vengono rifatti. **Vincolo assoluto (istruzione esplicita dell'utente, 2026-09-06, `DEEPSEEK_TASKS.md` "REGOLA ASSOLUTA #2"): DeepSeek scrive solo ed esclusivamente codice/test, compila, testa, e riporta il minimo richiesto — mai documentazione, mai analisi, mai aggiornamenti a questo file o a `DEEPSEEK_TASKS.md`.** Aggiornare questa coda dopo una consegna DeepSeek resta sempre compito di Claude.
- **Claude**: scrive i comandi dettagliati per ogni task; scrive/esegue A1/A3 (contratti/algoritmi, tipicamente i pezzi più piccoli e per primi, per sbloccare DeepSeek il prima possibile) e A5/A6 (audit indipendente, integrazione finale). Continua a programmare in ogni fase, non solo a scrivere specifiche.
- **Cursor**: non più assegnato a nulla di nuovo dalla data sopra. Se torna disponibile, i ruoli possono essere ridistribuiti su richiesta esplicita dell'utente.
